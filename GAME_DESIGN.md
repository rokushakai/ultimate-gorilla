# 究極ゴリラ ゲーム企画書 (GAME_DESIGN.md)

> **このファイルはゲーム仕様のSingle Source of Truth(最上位の仕様書)です。**
> プロデューサーから新しいアイデアが共有された場合、まずこのファイルへ
> 仕様として整理してから [TODO.md](TODO.md) に実装予定を登録し、実装します。
> 実装した内容は [CHANGELOG.md](CHANGELOG.md) に記録します。
> 遊ぶ人向けの説明は [README.md](README.md) を参照してください(本書は企画・設計ドキュメントです)。

ステータス表記について: 各項目に **[実装済み]** / **[未実装・将来機能]** を付記しています。


## 1. ゲームコンセプト

スマホ1画面で延々とプレイできる、**UMA収集ブラウザRPG**。

- 育つのは主人公だけ。**UMAは育成しない。** 捕まえて図鑑に登録する・売ってお金にする「収集・コレクション対象」として扱う。
- ドラクエ風の2Dフィールドを歩き、ランダムエンカウントで戦う「歩く→戦う→強くなる」のループを繰り返す。
- 外部ライブラリ・サーバー不要。HTML/CSS/JavaScriptのみで動作し、GitHub Pagesで配信できる静的サイトとして実装する。
- 操作はスマホ前提(十字キー/スワイプ)。PCでも矢印キーで遊べる。


## 2. 世界観

**[実装済み(最小限)]**

究極のUMA「究極ゴリラ」🦍 を探し求める勇者の子孫たちが、UMA(未確認生物)が
潜む世界を旅する。世界には伝説のUMAも紛れており、低レベルの旅人がいきなり
ラスボス級のUMAに遭遇することもある。

現状はテキスト/絵文字による最小限の世界観のみで、固有の地名・歴史などの
詳細な設定は今後追加予定(詳細未定)。


## 3. ストーリー

**[実装済み]**

- ゲーム開始時、王様からのオープニングメッセージを表示する(初回起動時のみ)。
  「おお、勇者よ……いや勇者の子孫よ。ついにこの時が来た。伝説のUMA
  究極ゴリラを捕まえるのじゃ！」という導入で、プレイヤーに目的を伝える。
  一度見るとセーブデータに記録され、次回以降は表示しない。
- オープニングの直後、初回のみ「目的説明画面」を表示する(§3.5)。
- プレイヤーは「勇者の子孫」。村を出発し、UMAを探す旅に出る。
- 探索中、伝説のUMA「究極ゴリラ」に低確率で遭遇できる。
- **最終目標は「究極ゴリラを捕まえること」。** 通常の「つかまえる」では捕まえられない。
  レベル99・「女神のウクレレ」の所持・HPを1〜10まで削った状態で
  「うたう」コマンドを使うことで捕獲できる(Version 0.6で実装。詳細は §14.5)。
  初遭遇時は専用モーダルを表示し「出会えたこと」を祝う(§14)。
- 究極ゴリラの通常討伐はレベル99・最強装備クラスでも
  ほぼ不可能な難易度にしてある(詳細は §14)。


## 3.5 目的説明画面

**[実装済み]**

初回プレイ時、ユーザーテストで「何を目指せばいいか分からない」という
フィードバックがあったため追加した。オープニングモーダルを閉じた直後に
1回だけ自動で表示する。

内容:

```
目的:
伝説のUMA「究極ゴリラ」を捕まえよう！

そのためにやること:
・フィールドを歩いてUMAを探す
・UMAを倒す、逃げる、捕まえることで経験値を得る
・レベルを上げる
・装備を集める
・UMA図鑑を埋める
・最終的に究極ゴリラに挑む
```

「OK」ボタンで閉じる。一度見るとセーブデータに記録され(`seenGoal`)、
自動表示は二度目以降出ないが、⚙️設定モーダルの「🎯目的を見る」ボタンから
いつでも再表示できる。


## 3.6 現在の目標表示(Version 0.6.1で実装)

**[実装済み]**

ステータス確認画面の最上部に「🎯 現在の目標」セクションを追加した。
ゲームの進行状態に応じて、プレイヤーが次に何をすべきか自動的に表示する。

| 状態 | 表示内容 |
|---|---|
| gameCleared | 🏆 クリア済み！称号：「森に歌を届けし者」図鑑・装備集めを続けよう |
| Lv.99 & ウクレレ所持 | HP1〜10まで削って「うたう」でクリア！ |
| Lv.99 のみ | 女神のウクレレ🪗をフィールドで探そう |
| Lv.50〜98 | メタルゴリラ系で経験値稼ぎ・ウクレレも探そう |
| Lv.20〜49 | 装備を集めよう・宝箱を探そう |
| Lv.1〜19 | フィールド探索・UMA収集・実家/酒場を活用しよう |


## 3.7 ヒントシステム(Version 0.6.1で実装)

**[実装済み]**

実家(🏠)に帰って「休む」選択時、モーダル内にランダムなヒントを表示する。
女神のウクレレ・メタルゴリラ・究極ゴリラ捕獲条件などの
ヒントを9種類用意し、毎回ランダムに1つを表示する。

ネタバレしすぎない文章で、プレイヤーが自然に目標を理解できるよう設計した。


## 3.8 レベル99到達マイルストーン演出(Version 0.7.1で実装)

**[実装済み]**

プレイヤーが初めてレベル99に到達した戦闘が終わった後、専用のマイルストーンモーダルを表示する。
究極ゴリラ捕獲の直前条件を満たしたことを祝い、次のアクションを明示する。

### 表示タイミング
- 戦闘中のレベルアップでLv99になり、戦闘終了(「OK」ボタン押下)後に表示する。
- `state.pendingLv99 = true` を立てておき、`finishBattle()` 内で判定して開く。
- クリアモーダル(`pendingClear`)との同時発生時は **クリアを優先** し、Lv99演出は出さない。

### 表示内容(`openLv99Modal()`で動的生成)
- 固定テキスト: 「勇者の子孫の力は、限界まで高まった！」
- 女神のウクレレ**未所持**の場合: ウクレレを探すよう案内
- 女神のウクレレ**所持済み**の場合: うたうでクリアできると案内

### 初回のみ表示
- `state.player.level99Shown`(bool)をセーブデータに保存し、一度表示したら再表示しない。
- ニューゲームでリセットされる。
- デバッグ用に「🎖 Lv99演出を再生」ボタン(`?debug=1`)で強制表示できる。

### 設計メモ
- `levelUp()` 内で `p.level === 99 && !p.level99Shown` を判定してフラグを立てる。
- `finishBattle()` でフラグを確認してモーダルを開く(§28エンディングと同じパターン)。


## 4. ゲームループ

**[実装済み]**

1. フィールドを歩く(十字キー/スワイプ/長押し)
2. 草原を歩くとランダムでエンカウント(UMA or 雑魚モンスター)
3. 戦闘: たたかう / まほう / アイテム / つかまえる / にげる
4. 倒す・逃げる・捕まえる、いずれでも経験値を獲得 → レベルアップ
5. レベルアップ時にランダムでまほうを1つ習得
6. フィールドの武器/回復アイテムを取得して強化
7. 商人で売買、神社で転職して立ち回りを調整
8. 1〜7を繰り返しながら、低確率の伝説UMA・究極ゴリラとの遭遇を目指す


## 5. フィールド/マップ

**[実装済み]**

- 1枚のマップ(13列×18行)。ドラクエ風の2D見下ろし、絵文字タイル表示。
- 地形: 草原(エンカウントあり)/村の道(エンカウントなし)/木・水(進入不可)
- 村の中に 🏪 商人、⛩️ 神社(転職)、🍺 酒場(§9.5)を配置。
- マップ上に武器🗡️・回復アイテム🧪が落ちており、触れると自動取得。
- カメラはプレイヤー中心に追従し、マップ端でクランプする。

**[未実装・将来機能]** 複数マップ/ダンジョン/ワールドマップ間の移動。


## 5.6 実家(ホーム)

**[実装済み]**

フィールド上の 🏠 タイル(H)がプレイヤーの「実家」。スタート地点と同じ位置に
あり、旅の途中で戻ると回復・セーブができる。

- 🏠 タイルに触れると「実家に帰ってきた。」イベントが発生し、
  「休む / やめる」の選択肢が表示される。
- **「休む」を選ぶと:**
  - HPとMPを全回復
  - 状態異常(アレルギー/におい等)をすべて治療
  - オートセーブ(「セーブしました。」と表示)
- **「やめる」を選ぶ:** 何もせずにモーダルを閉じる。
- 旅で危なくなったら実家に戻ることで立て直しができる。

### 設計メモ

- フィールドの `movePlayer()` 内で tile==='H' を検知して `openHomeModal()` を呼ぶ。
- 回復はプレイヤーが能動的に「休む」を選んだ時のみ発動(自動ではない)。
- フィールドドロップ案(§5.7参照)が実装された場合、実家付近にセーフゾーンを
  維持する設計を考慮すること。


## 5.7 宝箱システム(Version 0.4.4で実装)

**[実装済み]**

フィールド上に宝箱シンボル(`B` タイル、表示: 🎁)を配置した。
プレイヤーが宝箱に触れると中身を入手できる。

### 宝箱の挙動

- 初回接触: 「🎁 宝箱を開けた！ ○○を手に入れた！」とトーストで通知する。
- 再接触: 「📦 宝箱は空だった…」と表示し、何も入手しない。
- 開封済みの宝箱は `📦` に見た目が変わる。
- 開封状態はセーブデータ(`state.openedChests`)に保存される。

### 宝箱の中身(重み付き乱択)

| 種別 | 内容 | 備考 |
|---|---|---|
| お金 | 5G / 10G / 20G / 50G | 最も出やすい |
| 回復アイテム | コーヒー / パン / お弁当 / せき止めシロップ / デオドラントスプレー | 出やすい |
| 低〜中ランク装備 | ワイヤーブラシ/ノコギリ/サバイバルナイフ/鉄の棒/木製バット/テニスラケット/ロックT/革ジャン/鉄のたて/ヘルメット | 出にくい |

装備はすでに所持済みの場合、代わりに売却額相当のゴールドを獲得する。

### 宝箱の配置(初期: 4箱)

マップ上の草原エリアに4つ配置。全探索のご褒美として機能する。
将来的に箱の数や配置は増やす予定。

### 設計メモ

- `RAW_MAP` 内の `B` 文字が宝箱タイルを表す。
- `state.openedChests["x,y"] = true` で開封済みを管理。
- `movePlayer()` で `tile === "B"` を検知し `openChest(x, y)` を呼ぶ。
- 宝箱タイルはエンカウントが発生しない安全タイル(SAFE_TILE)。
- `CHEST_DROPS` 配列で重み付き抽選し、`weightedPick()` を共用する。


## 5.8 フィールド上でのアイテム使用(Version 0.4.4で実装)

**[実装済み]**

画面上部ステータスバーの「🎒アイテム」ボタンを押すと、
フィールド上(戦闘外)でアイテムを使用できるようになった。

### 使用可能なアイテム

| 種別 | アイテム | 効果 |
|---|---|---|
| 回復 | やくそう | HP+15 |
| 回復 | コーヒー | HP+10 |
| 回復 | パン | HP+20 |
| 回復 | お弁当 | HP+40 |
| 回復 | ラーメン | HP全回復 |
| 治療 | せき止めシロップ | アレルギーを治す |
| 治療 | デオドラントスプレー | においを治す |

### 制約

- HP満タン時は回復アイテムを使えない(「今は使う必要がない。」と表示し消費しない)。
- 対応する状態異常でない場合は治療アイテムを使えない(同上)。
- 戦闘中はボタンを押しても何もしない。
- 使用後はアイテム画面の所持数が即時更新される。
- セーブが自動で実行される。

### 設計メモ

- `openModal("field-item-modal")` でフィールドアイテムモーダルを開く。
- `renderFieldItemBody()` で ITEM_DATA を動的に読み込んでリスト表示。
- `useFieldHealItem(itemId)` / `useFieldCureItem(itemId)` で実際に使用する。
- 使用後は `renderFieldItemBody()` で再描画して所持数を更新する。


## 5.5 横スクロールマップ案(将来の大型改修候補)

**[未実装・将来機能]** ※プロデューサー提案。v0.9以降または専用ブランチで検討。

現在の固定グリッド1枚マップ(上下左右移動)から、将来的に横スクロール型
フィールドへ変更する案。

### 構想(v0.7.1時点の整理)

- **イメージ**: 「ボコスカウォーズ」風のレトロ横スクロール。
- **ステージ構成**: 3面〜5面程度を左右に行き来できる。
  - 面ごとに敵の種類・強さ・宝箱・NPC・施設(商人/神社/酒場/実家)を配置。
  - 面が変わるほど難易度が上がり、奥の面に究極ゴリラが出やすくなる。
- **移動方向**: 主に横方向。縦移動は補助的に残す(または省く)。
- **狙い**: 冒険感・レトロゲーム感の強化。「次の面に進む」達成感。

### 影響範囲(大規模改修)

| 影響箇所 | 現状 | 変更後 |
|---|---|---|
| `RAW_MAP` | 1枚固定グリッド | 複数マップの配列 |
| `renderField()` | 固定ビューポート | 横スクロール対応 |
| カメラ | X/Y クランプ | 横スクロール追従 |
| エンカウント | 草原タイル均一 | 面ごとに敵テーブル切替 |
| セーブ座標 | 未保存 | 現在面+座標をセーブ必須 |
| 施設配置 | 固定タイル | 面ごとのレイアウト |

### 実装方針

- **v0.9以降**または**専用ブランチ**で着手する大型改修として扱う。
- 既存の1枚マップ版は安定しているため、横スクロール版はブランチを切って開発する。
- 現行コードへの互換パッチ方式ではなく、マップ関連モジュールを書き直す想定。


## 6. UMA・モンスターの分類

**[実装済み]**

このゲームは「敵を倒すゲーム」ではなく、**UMAや敵を弱らせる → 捕まえる、
または(力尽きて)逃げられる → 経験値や報酬を得る** というUMA収集RPGである
(§13参照)。これを踏まえ、敵は3つに分類する。すべて `type` フィールドで
区別し、データは `script.js` の `UMA_DATA` / `NON_UMA_DATA` に集約する
(メタル系も実装上は `NON_UMA_DATA` に含め、`type: "metal"` で区別する)。

| 分類 | type | 図鑑/捕獲/売却 | 目的 |
|---|---|---|---|
| UMA | `"uma"` | 対象 | 収集・コレクション(§1) |
| 通常モンスター | `"monster"` | 対象外 | 経験値・お金・アイテム目的の道中の敵 |
| メタル系 | `"metal"` | 対象外 | 経験値稼ぎ用のボーナス的な敵(§6.3) |

### 6.1 通常モンスター(図鑑対象外)

| id | 名前 | 最低Lv | HP | 攻撃力 | 防御 | EXP | 特殊行動 |
|---|---|---|---|---|---|---|---|
| slime | スライム | 1 | 10 | 3 | 1 | 5 | - |
| bat | コウモリ | 1 | 9 | 4 | 0 | 5 | - |
| mosquito | 蚊 | 1 | 6 | 2 | 0 | 3 | 低確率でアレルギーを付与 |
| snake | 蛇 | 1 | 11 | 4 | 1 | 6 | - |
| wilddog | **のらいぬ** | 1 | **18** | **8** | **2** | **12** | **序盤の強敵。startMsgで警告を表示(§40)** |
| yabuka | ヤブ蚊 | 2 | 9 | 5 | 0 | 7 | 蚊より高確率でアレルギーを付与 |
| wanderingman | さまようおやじ | 2 | 16 | 5 | 1 | 8 | - |
| powerpointguy | パワポ野郎 | 3 | 17 | 5 | 2 | 10 | 低確率でMPを少し吸う |
| scammer | 詐欺師 | 3 | 18 | 6 | 1 | 11 | 低確率で所持金を少し盗む |
| bandit | 山賊 | 3 | 22 | 8 | 2 | 14 | 低確率でにおいを付与 |
| marathonman | マラソンマン | 4 | 19 | 6 | 2 | 12 | 素早い(プレイヤーが逃げにくい) |
| bumpman | ぶつかりおじさん | 4 | 20 | 7 | 2 | 13 | 不意打ち(戦闘開始時に先制攻撃) |
| oni | 鬼 | 5 | 30 | 10 | 4 | 20 | - |
| powerharassmentsenpai | パワハラ先輩 | 5 | 24 | 13 | 3 | 18 | 攻撃力高め |

**v0.8.7 (§40) で追加した通常モンスター — 序盤**

| id | 名前 | 最低Lv | HP | 攻撃力 | 防御 | EXP | 特殊行動 |
|---|---|---|---|---|---|---|---|
| campgirl | キャンプ女子 | 1 | 8 | 3 | 0 | 5 | - |
| xiaolongbao | 小籠包 | 1 | 7 | 2 | 0 | 4 | - |
| streetguitarist | 弾き語り女子 | 1 | 9 | 3 | 0 | 6 | - |
| rudeperson | 失礼な人 | 1 | 10 | 4 | 0 | 6 | - |

**v0.8.7 (§40) で追加した通常モンスター — 中盤**

| id | 名前 | 最低Lv | HP | 攻撃力 | 防御 | EXP | 特殊行動 |
|---|---|---|---|---|---|---|---|
| wannabeninja | 忍者かぶれ | 3 | 20 | 7 | 3 | 12 | - |
| strongarmcatcher | 強肩キャッチャー | 3 | 22 | 8 | 2 | 13 | - |
| hangure | 半グレ | 4 | 24 | 9 | 2 | 15 | 低確率でにおい付与 |
| bangya | バンギャ | 3 | 18 | 6 | 1 | 11 | - |
| vintageguy | 古着屋兄さん | 3 | 16 | 6 | 2 | 10 | - |
| teacher | 先生 | 4 | 20 | 7 | 3 | 12 | - |
| foodsnob | グルメ気取り | 3 | 17 | 5 | 2 | 10 | - |
| chikan | 痴漢 | 3 | 15 | 6 | 1 | 11 | 不意打ち |

**v0.8.7 (§40) で追加した通常モンスター — 後半**

| id | 名前 | 最低Lv | HP | 攻撃力 | 防御 | EXP | 特殊行動 |
|---|---|---|---|---|---|---|---|
| andre | アンドレ | 8 | 38 | 12 | 5 | 28 | - |
| deathmatch | デスマッチレスラー | 10 | 45 | 14 | 4 | 35 | - |
| mitakadrunk | 三鷹のよっぱらい | 8 | 32 | 11 | 3 | 25 | 不意打ち |
| viceprincipal | 教頭 | 8 | 34 | 10 | 5 | 26 | - |
| principal | 校長 | 10 | 38 | 11 | 6 | 30 | - |
| fakescriptwriter | いんちき放送作家 | 9 | 35 | 9 | 4 | 27 | 低確率で所持金を盗む |
| pseudoscreenwriter | エセ脚本家 | 9 | 30 | 8 | 3 | 24 | 低確率でMPを吸う |
| implantdentist | インプラント歯医者 | 10 | 36 | 10 | 5 | 28 | 低確率でMPを吸う |
| psychicdetective | 霊界探偵 | 9 | 33 | 9 | 4 | 26 | 低確率で所持金を盗む |
| karatesisters | 空手姉妹 | 10 | 40 | 13 | 4 | 32 | - |
| graviaidol | グラビアアイドル | 8 | 30 | 10 | 3 | 24 | - |
| alien | 宇宙人 | 12 | 42 | 13 | 5 | 38 | - |
| stranger | 異邦人 | 10 | 38 | 12 | 4 | 30 | - |
| visitor | 来訪者 | 11 | 40 | 13 | 5 | 34 | - |

### 6.2 特殊行動の仕様

- **アレルギー/におい付与**: 攻撃が命中した時、低確率で状態異常を付与する
  (§13.5と同じ仕組み)。`inflicts: { id, chance, duration }` で定義。
- **MP吸収**: 攻撃時、低確率でプレイヤーのMPを一定値減らす。
  `drainsMp: { chance, amount }` で定義。
- **所持金を盗む**: 攻撃時、低確率でプレイヤーの所持金を一定額減らす。
  `stealsGold: { chance, amount }` で定義。
- **不意打ち(先制攻撃)**: 戦闘開始時、プレイヤーが行動する前に1回だけ
  攻撃してくる。`ambush: true` で定義。
- **素早い**: プレイヤーが「にげる」を選んだ時の成功率(`fleeRate`)を
  通常より低く設定することで表現する。新しい仕組みは追加していない。

### 6.3 メタル系(経験値稼ぎ用のボーナス敵。Version 0.6.1で調整)

**[実装済み]**

防御力が非常に高く通常攻撃はほとんど通らないが、経験値が大きい
「捕まえなくてもよい」ボーナス敵。出現率は低く、`METAL_ENCOUNTER_CHANCE`
(既定0.04)で専用に抽選する(UMAのレア抽選とは別枠)。図鑑・捕獲対象外。

| id | 名前 | 最低Lv | HP | 攻撃力 | 防御 | EXP | 想定タイミング |
|---|---|---|---|---|---|---|---|
| metalgorilla | メタルゴリラ | 1 | 8 | 3 | 25 | 120 | 序盤〜中盤の経験値ボーナス |
| haguremetalgorilla | はぐれメタルゴリラ | 10 | 12 | 5 | 40 | 400 | 中盤〜終盤の経験値ボーナス |
| fullmetalgorilla | フルメタルゴリラ | 20 | 16 | 8 | 60 | 1000 | 終盤向けの高経験値ボーナス |

**Version 0.6.1でEXPを大幅増量**: レベル99到達のテンポ改善のため
メタルゴリラ: 40→120、はぐれ: 120→400、フルメタル: 300→1000 に増量。
出現率も METAL_ENCOUNTER_CHANCE: 0.04→0.06 に微増。

逃げる必要はなく(攻撃力が低い)、HPを0にすると(§13の方針どおり)
「○○は逃げていった！！」表示で経験値を獲得する。捕獲も理論上は可能だが
捕獲率は非常に低い(0.03〜0.05)。遭遇時は「キラリと光った！経験値のチャンスだ！」
という専用メッセージを表示する(v0.6.1追加)。

### 6.4 UMA(収集・捕獲対象)

| id | 名前 | レア度 | 最低Lv | HP | 攻撃力 | 捕獲率 | EXP | 売却額 |
|---|---|---|---|---|---|---|---|---|
| kappa | カッパ | コモン | 1 | 16 | 6 | 0.40 | 10 | 8G |
| tsuchinoko | ツチノコ | コモン | 2 | 18 | 7 | 0.38 | 12 | 10G |
| hibagon | ヒバゴン | アンコモン | 3 | 24 | 8 | 0.30 | 16 | 16G |
| mothman | モスマン | アンコモン | 4 | 22 | 9 | 0.30 | 18 | 18G |

### 6.5 レアUMA(レベル無関係・低確率で出現)

| id | 名前 | HP | 攻撃力 | 捕獲率 | EXP | 売却額 | 逃走成功率 |
|---|---|---|---|---|---|---|---|
| bigfoot | ビッグフット | 40 | 11 | 0.18 | 35 | 60G | 0.80 |
| nessie | ネッシー | 42 | 11 | 0.16 | 38 | 65G | 0.80 |
| yeti | イエティ | 45 | 12 | 0.15 | 42 | 70G | 0.80 |
| jerseydevil | ジャージーデビル | 46 | 13 | 0.14 | 44 | 75G | 0.80 |
| **ultimategorilla** | **究極ゴリラ** | 5000 | 150 | 0.005(上限2%) | 300 | 99999G | 0.95 |

エンカウント抽選(`script.js` の `chooseEnemy()`): まず
`RARE_ENCOUNTER_CHANCE`(既定0.08)でレアUMA枠を判定(該当すれば重み付き
抽選)。外れた場合、次に `METAL_ENCOUNTER_CHANCE`(既定0.04)でメタル系枠
(§6.3)を判定。どちらにも該当しなければ、通常モンスター+通常UMAの中から
プレイヤーレベル以下の `minLevel` を持つ敵を重み付き抽選する。

**[未実装・将来機能]** 新規UMAの追加は随時このセクションに追記していく。


## 7. 図鑑

**[実装済み]**

UMAごとに3段階の状態を持つ。売却してもこの記録は消えない(「捕まえた実績」)。

- **未発見**: 一度も遭遇していない(「？？？」表示)
- **発見済み**: 戦闘で遭遇したが、まだ捕獲に成功していない
- **捕獲済み**: 一度でも捕獲に成功した

画面右上「📖図鑑」ボタンから一覧モーダルを開く。


## 8. アイテム/武器(レガシー仕様)

**[実装済み]**

データは `script.js` の `ITEM_DATA` / `WEAPON_DATA` に集約。

| id | 名前 | 効果 | 購入価格 | 売却価格 |
|---|---|---|---|---|
| potion | やくそう | HP+15回復 | 10G | 4G |
| rope | 捕獲ロープ | 捕獲率+25%で即捕獲を試みる | 15G | 5G |
| coughsyrup | せき止めシロップ | アレルギーを治す(§13.5) | 12G | 4G |
| deodorant | デオドラントスプレー | においを治す(§13.5) | 12G | 4G |

| id | 名前 | 効果 | 入手方法 |
|---|---|---|---|
| fieldsword | つるぎ | こうげき力+3 | フィールドに落下(取得で即時加算) |
| ironsword | 鉄の剣 | こうげき力+6 | 商人で購入(30G、購入で即時加算) |

これらは「装備の付け外し」の概念がなく、取得/購入した瞬間に永続加算される
簡易仕様(以前からの実装をそのまま維持しているレガシー機能)。
本格的な装備スロットは §8.5 を参照。

### その他アイテム(データのみ。使用ロジックは未実装)

`ITEM_DATA` に以下を追加済み。購入/使用UIにはまだ出てこない、企画段階のデータ。

| id | 名前 | 想定用途(未実装) |
|---|---|---|
| coffee | コーヒー | (未定。気付け・MP回復などを想定) |
| bread | パン | (未定。軽い腹持ち・HP少量回復などを想定) |
| bento | お弁当 | (未定。HP中回復などを想定) |
| ramen | ラーメン | (未定。HP大回復などを想定) |

**Version 0.4.3で実際に使えるようになった。** `trackable: true` に変更し、
商人で購入でき、戦闘の「🧪アイテム」メニューから使用できる。
HP満タンの場合は「今は使う必要がない」と表示し、アイテムは消費しない。

| id | 名前 | 効果 | 購入価格 | 売却価格 |
|---|---|---|---|---|
| coffee | コーヒー | HP+10回復 | 5G | 1G |
| bread | パン | HP+20回復 | 10G | 3G |
| bento | お弁当 | HP+40回復 | 20G | 7G |
| ramen | ラーメン | HP全回復 | 40G | 15G |

せき止めシロップ(15G)/デオドラントスプレー(15G)は Version 0.4.1 から使用可能。


## 8.5 装備システム

**[実装済み(所持制・商人購入対応)]**

武器に加えて、防具・盾・兜の装備スロットを追加した。データは
`script.js` の `EQUIP_WEAPON_DATA` / `ARMOR_DATA` / `SHIELD_DATA` /
`HELMET_DATA` に集約。§8のレガシー武器(`WEAPON_DATA`)とは別の配列・別の
ボーナス枠として加算される(レガシー仕様を壊さないため意図的に分離)。

- 各スロット(武器/防具/盾/兜)につき1つ装備できる。
- 各リストの先頭の項目(木の棒/Tシャツ/段ボールのたて/男塾ハチマキ)は
  ボーナス0の初期装備で、最初から所持・装備している。
- **Version 0.4.1で所持制に変更。** プレイヤーは `player.ownedWeapons` /
  `ownedArmors` / `ownedShields` / `ownedHelmets`(idの配列)に持っている
  装備だけを装備できる。所持していない装備は装備変更画面で
  「未所持」と表示され、選択できない(ゲームバランス対応)。
  Version 0.4で自由に選べていた状態からの移行(セーブ済みプレイヤー)は、
  読み込み時にその時点で装備していたものを所持品として自動的に
  引き継ぐ(既存プレイヤーが装備を失わないようにするための救済措置)。
- 画面右上「🎽装備」から装備変更画面を開き、所持している項目に
  いつでも切り替えられる。
- ステータス再計算(`recomputeStats()`)は
  `攻撃力 = ベース + 職業補正 + レガシー武器加算 + 装備武器のatkBonus`、
  `防御力 = ベース + 職業補正 + 防具/盾/兜のdefBonusの合計`
  に拡張した。武器の `mpBonus` や防具の `hpBonus` がある場合はMP/HPの
  上限にも加算される。

### 入手手段(Version 0.4.1で追加)

商人(§9)で一部の装備を購入できる。購入すると所持品に追加され、
装備変更画面で選べるようになる(購入してもまだ装備中にはならない。
装備変更画面で改めて装備する必要がある)。Version 0.4.3で販売品を大幅拡充した。

| 区分 | 商品 | 価格 |
|---|---|---|
| 武器 | ワイヤーブラシ(8G) / ノコギリ(15G) / 魔法のステッキ(40G) / サバイバルナイフ(25G) | - |
| 武器 | 鉄の棒(35G) / ブーメラン(40G) / テニスラケット(45G) / バールのようなもの(45G) | - |
| 武器 | 手裏剣(55G) / ヌンチャク(60G) / 木製バット(65G) / 斧(75G) / 金属バット(85G) | - |
| 鎧 | ロックT(10G) / 革ジャン(20G) / 武者よろい(60G) / 西洋風よろい(90G) | - |
| 盾 | 鉄のたて(22G) / ドラゴンのたて(100G) | - |
| 兜 | ヘルメット(10G) / 鋼鉄のかぶと(35G) / キグナスのかぶと(70G) | - |

**Version 0.4.5で装備の売却を実装。** 商人の「🔧 装備を売る」から所持装備を売却できる(§9)。最強装備の入手手段は§8.6参照。

### 武器(EQUIP_WEAPON_DATA、こうげき力ボーナス)

木の棒(0,初期) / ワイヤーブラシ(2) / 石(3) / ノコギリ(4) / 魔法のステッキ(5・MP+5) /
サバイバルナイフ(6) / 鉄の棒(8) / ブーメラン(9) / バールのようなもの(10) /
テニスラケット(10) / 手裏剣(11) / ヌンチャク(12) / 木製バット(13) / 斧(15) /
金属バット(17) / 斬岩剣(22) / 斬鉄剣(27) / メガトンハンマー(33) / 霊剣(38) /
アンドロメダの鎖(44) / チェーンソー(50) / 如意棒(58)

### 防具(ARMOR_DATA、防御力ボーナス)

Tシャツ(0,初期) / ロックT(2) / 革ジャン(4) / 武者よろい(8) / 西洋風よろい(12) /
信長のよろい(16・HP+10) / ペガサスのよろい(14・HP+5) / 亀の武道着(20・HP+15)

### 盾(SHIELD_DATA、防御力ボーナス)

段ボールのたて(0,初期) / 鉄のたて(5) / ドラゴンのたて(12) / 六連のたて(20)

### 兜(HELMET_DATA、防御力ボーナス)

男塾ハチマキ(0,初期) / ヘルメット(2) / 鋼鉄のかぶと(5) / キグナスのかぶと(8) /
信玄のかぶと(11) / 宇宙のかぶと(15)


## 8.6 最強装備の将来入手案(今回は未実装)

**[未実装・将来機能]** ※プロデューサー提案。

以下の最強クラス装備は今回の商人では販売しない。将来的なイベント/
ドロップ/ボス報酬などで入手する想定とする。

| 装備 | 種別 | ボーナス | 想定入手手段 |
|---|---|---|---|
| ペガサスのよろい | 鎧 | def+14, HP+5 | イベント/特定UMA捕獲報酬 |
| 信長のよろい | 鎧 | def+16, HP+10 | イベント/ドロップ |
| 亀の武道着 | 鎧 | def+20, HP+15 | レアドロップ/ボス報酬 |
| 六連のたて | 盾 | def+20 | イベント/特定UMA報酬 |
| 信玄のかぶと | 兜 | def+11 | ドロップ |
| 宇宙のかぶと | 兜 | def+15 | レアイベント |
| 斬岩剣以降の武器 | 武器 | def+22〜58 | 各種イベント/ドロップ |
| 女神のウクレレ | 特殊(重要アイテム) | 究極ゴリラ捕獲に必須(§14.5) | マップの特別な宝箱(🪗)から入手(v0.6実装) |


## 9. 商人

**[実装済み]**

村の 🏪 タイルに接触するとメニューが開く。

- 🛍️ 買う(Version 0.4.3で拡充: 鉄の剣・やくそう・捕獲ロープ・コーヒー・パン・
  お弁当・ラーメン・せき止めシロップ・デオドラントスプレー)
- ⚔ 武器を買う / 🥋 防具を買う / 🛡 盾を買う / ⛑ 兜を買う
  (§8.5の装備を購入し、所持品に追加する。Version 0.4.3でラインナップ大幅拡充)
- 📤 アイテムを売る(やくそう・捕獲ロープ・コーヒー・パン・お弁当・ラーメン・
  せき止めシロップ・デオドラントスプレー)
- 🔧 装備を売る(Version 0.4.5で追加。所持している装備を売却できる。
  売却価格は buyPrice/2 を基本とし、イベント入手品等 buyPrice なしの場合は 5G)
  - 現在装備中の装備は売れない(「装備中なので売れない」と表示)
  - 売却後は所持品リストから削除され、装備変更画面から選べなくなる
- 🦍 UMAを売る(所持UMAを1匹単位で売却、図鑑の捕獲済み記録は残る)
- やめる

所持金は `player.gold` で管理。購入/売却で即時に増減する。

**[未実装・将来機能]** 在庫の限定/補充、商人ごとの取り扱い品の差別化、複数の商人NPC。


## 9.5 酒場

**[実装済み(Version 0.5 / Version 0.5.1で加入演出強化)]**

村の 🍺 タイルに接触すると酒場メニューが開き、仲間の勧誘・確認・解雇ができる。

### メニュー構成

- **🤝 仲間を探す**: 仲間候補(§10)の一覧を表示し、加入を試みる。
  - 仲間候補ごとに固有の `joinRate` があり、**成功/失敗の2分岐**がある(Version 0.5.1)。
    - 成功時: 固有の会話メッセージを表示してパーティーに加入。「○○が仲間になった！」トースト + セーブ。
    - 失敗時: 固有の断り台詞を表示。候補は酒場から去らず、**何度でも再挑戦できる**。
  - すでに仲間の場合は「同行中 ✓」と表示し加入ボタンを非表示。
  - パーティー上限(`COMPANION_MAX` = 2)に達している場合は「上限です。仲間を外してから来てください。」と表示。
  - effectDesc(効果説明)を仲間候補リストに明示表示。
- **👥 仲間を見る**: 現在の仲間を名前・特色・効果・同行中ステータスと共に確認。
- **👋 仲間を外す**: 仲間を選んでパーティーから外す。
  - 「○○は酒場に戻った。」トースト表示 + セーブ。
  - 外した仲間は再度「仲間を探す」から加入できる。
- **やめる**: 酒場メニューを閉じる。

**[未実装・将来機能]** 酒場限定アイテム/会話、会話結果による3分岐(加入/断る/戦闘)(§10参照)。


## 10. 仲間システム

**[実装済み(Version 0.5 酒場加入版 / Version 0.5.1 加入演出強化版)]**

### v0.5 / v0.5.1 で実装した仕様

- 酒場(§9.5)で仲間候補から加入・確認・解雇ができる。
- 仲間は最大 `COMPANION_MAX`(= 2)人まで。
- 仲間ごとにパッシブ効果(補正値)があり、戦闘計算に自動で反映される。
- 仲間情報は `player.companions`(idの配列)としてセーブデータに保存される。
- UMAと異なり、仲間は売却/図鑑登録の対象ではない。
- **[v0.5.1追加]** 加入に `joinRate` によるランダム成否判定あり。失敗しても候補は残り何度でも再挑戦できる。
- **[v0.5.1追加]** 成功・失敗それぞれに仲間固有の台詞を表示。

### 仲間候補と効果(COMPANION_DATA)

| id | 名前 | 特色 | 効果 | joinRate |
|---|---|---|---|---|
| juritani | ジュリタニ | 会心の一撃の確率が高い | 攻撃時に会心の一撃が出やすくなる(確率+20%) | 70% |
| shurittani | シュリタニ | UMAを捕まえるのが得意 | 捕獲率+0.10 | 65% |
| norio | ノリオ | 逃げるのがうまい | 逃走成功率+0.15 | 75% |
| harumi | ハルミ | まほうが得意 | まほう効果(ダメージ/回復量)+20% | 60% |

### 仲間固有台詞(joinMsgs / failMsgs)

| 名前 | 成功台詞 | 失敗台詞 |
|---|---|---|
| ジュリタニ | 「ジュリタニは拳を鳴らした。」「面白そうだな。付き合ってやるよ。」 | 「ジュリタニは腕を組んだ。」「まだお前の実力を見せてもらってないな。」 |
| シュリタニ | 「シュリタニは捕獲ロープを確認した。」「UMA探しなら任せて。」 | 「シュリタニは地図を見つめている。」「今は準備が足りないみたい。」 |
| ノリオ | 「ノリオは出口の場所を確認した。」「危なくなったら俺についてこい。」 | 「ノリオはすでに逃げる準備をしている。」「今日はやめておこう。」 |
| ハルミ | 「ハルミは静かに呪文を唱えた。」「魔法で支えます。」 | 「ハルミは首をかしげた。」「魔力の流れがまだ合わないみたい。」 |

### 仲間の補正が適用される箇所

- **ジュリタニ(critBonus)**: `doFight()` の通常攻撃時に `Math.random() < critBonus` でクリット判定。
  クリット時はダメージ × 1.5(切り捨て)になり「💥 会心の一撃！」と表示される。
- **シュリタニ(captureMod)**: `attemptCapture()` の成功率に `getCompanionBonus("captureMod")` を加算。
  職業補正と同じ仕組みで加算する。
- **ノリオ(fleeMod)**: `doRun()` の逃走成功率に `getCompanionBonus("fleeMod")` を加算。
- **ハルミ(spellMod)**: `castSpell()` の攻撃まほうダメージ・回復まほう回復量に
  `(1 + getCompanionBonus("spellMod"))` を乗算する。

### 将来仕様(今回は未実装)

- NPCがフィールドを歩くシンボルエンカウント方式への変更
- 仲間加入時の会話結果3分岐(加入/断る/戦闘) ← 現在は2分岐(加入/断る)のみ
- 仲間のフィールド追従表示
- 仲間固有イベント・専用装備・成長要素
- 仲間上限の拡張(3人以上への対応)


## 11. 職業(部活)

**[実装済み]**

⛩️ 神社に接触すると転職メニューが開く。無料・回数制限なしでいつでも
転職できる。データは `script.js` の `JOB_DATA` に集約。

| id | 名前 | 補正 | 説明 |
|---|---|---|---|
| baseball | 野球部 | atk+3 | こうげき力が少し高い |
| swim | 水泳部 | HP+8 | 最大HPが少し高い |
| tennis | テニス部 | 捕獲+0.10 | UMAを捕まえやすい |
| home | 帰宅部 | 逃走+0.15 | 戦闘から逃げやすい |
| brass | 吹奏楽部 | まほう習得+0.35 | レベルアップ時に追加でまほうを覚えやすい |
| soccer | サッカー部(初期職業) | atk+1,HP+3,逃走+0.03,捕獲+0.03 | バランス型 |
| rugby | ラグビー部 | HP+12,atk+5,逃走-0.20 | HPと攻撃力が高いが逃げにくい |
| track | 陸上部 | 逃走+0.25 | とても逃げやすい |
| magicwarrior | 魔法戦士 | atk+2,MP+4,まほう習得+0.15 | まほうと攻撃のバランス型 |

ステータスは「ベース値(レベルアップでのみ成長) + 職業補正 + 武器加算」を
都度再計算する(`recomputeStats()`)。転職を繰り返しても値が累積しない設計。

**[未実装・将来機能]** 転職コスト、職業限定の専用まほう/わざ。


## 12. まほう

**[実装済み]**

レベルアップ時にランダムで未習得のまほうを1つ習得(職業「吹奏楽部」
「魔法戦士」は追加でもう1つ覚える可能性がある)。

攻撃まほうと回復まほうをデータ上で分離した: `ATTACK_SPELL_DATA` /
`HEAL_SPELL_DATA`。既存コードとの互換のため、両方を結合した
`SPELL_DATA`(= `ATTACK_SPELL_DATA.concat(HEAL_SPELL_DATA)`)も残しており、
習得・詠唱のロジックはこれまでと同じまま動作する。

### 攻撃まほう(ATTACK_SPELL_DATA)

| id | 名前 | MP消費 | 威力 |
|---|---|---|---|
| fire | ファイア | 4 | 9 |
| hazukashigatame | はずかし固め | 3 | 6 |
| leftHook | 左フック | 5 | 11 |
| thunder | サンダー | 6 | 13 |
| highKick | ハイキック | 7 | 15 |
| backdrop | バックドロップ | 9 | 19 |
| kidoClutch | キドクラッチ | 11 | 24 |
| sleeperHold | 魔性のスリーパー | 14 | 30 |
| parsley | パクチー | 4 | 8 |
| gooPunch | グーパンチ | 6 | 12 |
| homerun | ホームラン | 10 | 22 |

### まほう・必殺技の将来候補(データのみ未追加。名前のみ検討中)

せきしきめいかいは / ろざんしょうりゅうは / カリツォー / ここはひとつガマン /
いちげき必殺ともか蹴り / ドラゴンロケット / タイガータックル / ドライブシュート /
サイクロン / 顔面ブロック / サヨナラホームラン / フリーキック / ペナルティキック /
ロシアンフック / ローリングクレイドル / フランケンシュタイナー / ときめきスピアー

これらは追加するならこのセクションの表に追記し、`ATTACK_SPELL_DATA` に
要素を足すだけで対応できる。

### 回復まほう(HEAL_SPELL_DATA)

| id | 名前 | MP消費 | 回復量 |
|---|---|---|---|
| poimi | ポイミ | 2 | 6 |
| heal | ヒール | 5 | 14 |
| popoimi | ポポイミ | 7 | 20 |
| megaheal | メガヒール | 9 | 30 |
| popomalar | ポポマラー | 12 | 40 |
| popomazun | ポポマズン | 16 | 55 |

**[未実装・将来機能]** 状態異常・バフ/デバフ系のまほう、まほうの強化/上位版。


## 12.5 うたうコマンド

**[実装済み(Version 0.6)]**

戦闘コマンドの1つとして追加。どのタイミングでも使用できる(習得条件なし)。

### 通常敵/UMAへの効果
- 「次の捕獲の成功率+0.05」効果が1ターンのみ発動する。消費アイテムなし。
- ハルミが仲間にいる場合、ボーナスが +0.08 に増加し、専用メッセージが出る。

### 究極ゴリラへの効果(特別判定)
通常の「つかまえる」は究極ゴリラに通用しない。「うたう」を使った場合のみ
以下の全条件を判定し、捕獲成否を決定する(§14.5参照)。

### 将来案(現在は未実装)
- 特定レベルで習得、または女神のウクレレ入手後にのみ使用可能にする
- ハルミと連携してさらに効果アップ
- 音楽イベントで強化


## 13. 戦闘

**[実装済み]**

- コマンド: ⚔たたかう / ✨まほう / 🧪アイテム / 🪤つかまえる / 🎵うたう / 💨にげる
- **Version 0.4.2より、「倒す」という表現をやめた。** このゲームは
  「敵を倒すゲーム」ではなく「UMAや敵を弱らせる→捕まえる、または
  逃げられる→経験値や報酬を得る」というUMA収集RPGであるという方針を
  明確にするため、敵のHPが0になっても撃破ではなく
  **「○○は逃げていった！！」** という表現にした(v0.8.7 §40でメッセージ改善)。
  「逃げられた」(被害表現)から「逃げていった」(現象描写)に変更し、
  プレイヤーが勝った感じ・倒した感じが出るよう改善した。結果自体(経験値が入る)は
  変わらず、さらに少額の所持金を獲得するようにした(「○○Gを手に入れた！」)。
  捕獲・逃走(プレイヤー側)の結果には影響しない、HP0到達時の表現のみの変更。
  バリエーションメッセージ: 「は逃げていった！！」「はあわてて逃げていった！！」
  「はフラフラしながら逃げていった！！」「は戦意を失って逃げていった！！」
  からランダムに選択する。
- 倒す(逃げられる)・逃げる・捕まえる、いずれの結果でも経験値を獲得する。
  HPが0になった場合(逃げられた場合)のみ、追加で所持金も獲得する
  (`Math.ceil(敵のEXP / 2)` G)。
- ダメージ計算: `max(1, 攻撃力 + 乱数 - 防御力)` を基本とする。
- **捕獲成功率(Version 0.4.2で強化):**
  `捕獲率ベース + (1-敵HP比率)×HP連動係数 + 職業補正 + アイテム補正 - におい補正`
  を0.05〜0.95にクランプする。HP連動係数は通常の敵が0.50、レアUMA
  (`isRare`)は0.25(例外的に難しくする)。究極ゴリラのみ追加で0.02を
  上限にする(§14)。HPを1〜10程度まで減らすと、通常の敵はかなり
  捕まえやすくなる。
- 逃走成功率: `敵ごとの基本逃走率 + 職業補正` を0.05〜0.97にクランプ。
  「素早い」敵は基本逃走率を低めに設定することで、プレイヤーが
  逃げにくいようにしている(§6.2)。
- HPが0になると「倒れてしまった」表示の上で村に全回復で送還される
  (ペナルティは軽め)。
- **Version 0.4.5より、戦闘終了時に必ず「OK」ボタンで結果を確認してから
  フィールドに戻る。** 戦闘ログに結果(逃げられた/捕まえた/逃げ切った +
  経験値 + 獲得ゴールド)が表示された後、OKボタンを押すと画面が切り替わる。
  これにより、スクロールして結果を見逃す問題を解消した。

状態異常との関係は §13.5 を参照。敵の特殊行動(不意打ち/MP吸収/
所持金を盗む)は §6.2 を参照。

**[未実装・将来機能]** 複数体との同時戦闘、仲間の戦闘参加(§10)。


## 13.7 戦闘開始直後の誤タップ防止(Version 0.4.5で実装)

**[実装済み]**

スマホで十字キーを連打しているとエンカウントが発生し、画面切り替わりの
直後に指の位置にある戦闘コマンドを誤って押してしまう問題があった。

- 戦闘開始から **800ms** の間、すべての戦闘コマンドボタンを `disabled` にする。
- 敵の登場メッセージ(「○○が現れた！」)を読めるようにするための猶予時間。
- 800ms後に `setBattleLocked(false)` + 「どうする？」ログを表示してから
  コマンドを受け付ける。
- 不意打ち(`ambush: true`)の敵の場合は、戦闘開始時から `setBattleLocked(true)`
  にしたまま `enemyTurn()` に入り、敵の攻撃処理の後に `setBattleLocked(false)`
  を呼ぶ(敵の攻撃アニメーションが入力ロック期間を代替する)。


## 13.8 スマホ操作改善(Version 0.4.5で実装)

**[実装済み]**

スマホブラウザで長押しすると出る「コピー/検索/翻訳」メニューや
テキスト選択ハイライトを抑制した。

### CSS対応

- `* { user-select: none; -webkit-user-select: none; -webkit-touch-callout: none; }`
  全要素のテキスト選択・ iOS の「コールアウト」メニューを無効化。
- `button { touch-action: manipulation; }` ボタン全般のダブルタップズームと
  長押しコンテキストメニューを抑制。
  (十字キー `.dpad-btn` の `touch-action: none` はより高い特異度で維持される)

### JavaScript対応

- `#game` 要素の `contextmenu` イベントに `preventDefault()` を追加。
  長押しで出る右クリックメニュー(Androidで多い)を抑制する。


## 13.5 状態異常

**[実装済み(基盤+2種類)]**

状態異常システムの基盤(`player.statusAilments`: id→残りターン/歩数)を実装し、
そのうえで以下の2種類を実装した。

| id | 名前 | 効果 | 持続 | 主な原因 |
|---|---|---|---|---|
| allergy | アレルギー | フィールドを1歩歩くごとにHPが1減る(HP1未満にはならない) | 12歩 | ビッグフットの攻撃を受けた時、低確率で発症 |
| smell | におい | 捕獲成功率が-0.15される | 3回の戦闘 | 山賊の攻撃を受けた時、低確率で発症 |

- 画面上部のステータス欄に、状態異常がある間だけアイコンと名前を表示する。
- 持続ターン/歩数が尽きると自然に治る。
- **Version 0.4.1で治療アイテムと連携。** 戦闘の「🧪アイテム」メニューから
  せき止めシロップ(アレルギーを治療)/デオドラントスプレー(においを治療)を
  使える。該当する状態異常でない時に使うと「今は使う必要がない」と
  表示し、アイテムは消費しない(ターンも経過しない)。治療に成功した
  場合は「アレルギーが治った！」「においが消えた！」のように表示し、
  アイテムを1個消費してターンを経過させる。
- セーブデータにも状態異常を保存する。

**[未実装・将来機能]** フィールド上(戦闘外)でのアイテム使用、状態異常を
持つ追加のUMA、バフ系(良い効果)の状態異常。


## 13.6 RPGメッセージシステム

**[実装済み]**

ドラクエ風の定型メッセージを、状況に応じてフィールド上部のトースト
(`showToast()`)または戦闘ログ(`log()`)に表示する。

- 「○○が現れた！」(戦闘開始)
- 「○○を見つけた！」(UMAを初めて発見した時。図鑑が「発見済み」になる瞬間)
- 「○○は逃げていった！！」「○○を捕まえた！」「うまく逃げ切った！」(戦闘の結果。§13参照)
- 「経験値○○を獲得！」「○○Gを手に入れた！」(戦闘終了時)
- 「レベルが上がった！」「まほう「○○」を覚えた！」(レベルアップ)

新しいメッセージを増やす場合は、既存の `log()` / `showToast()` を
そのまま使えば良い(戦闘中は `log()`、フィールドでは `showToast()` が目安)。


## 14. ラスボス: 究極ゴリラ

**[実装済み]**

- ゲームにおける唯一の「ラスボス級」UMA。レベルに関係なく低確率で出現する。
- HP 5000 / 攻撃力150 / 防御60。レベル99・最強装備クラスでも
  通常プレイでは現実的に勝てない強さに調整している。
- 攻撃は毎ターン「かいしんのいちげき」級の特大ダメージ(防御力を
  ほとんど無視する特殊計算)。
- 捕獲成功率は通常の捕獲式の結果に関わらず常に2%を上限にする
  (HPを減らしても上がりやすくしない)。
- 逃走成功率は0.95と高めに調整し、「出会ったら逃げて生き延びる」を
  前提にしたバランスにしている。
- 初遭遇時は専用の発見モーダルを表示し、「ゲームの最終目標を達成した」
  という体験を演出する(倒さなくても発見した時点で目標達成という位置づけ)。

**[検討中]** 将来的に「真に討伐可能にする」エンドコンテンツ(専用装備や
クリア後強化など)を追加するかどうかは未定。`TODO.md` の Version 1.0 候補として
仮置きしている。


## 14.5 究極ゴリラ捕獲条件

**[実装済み(Version 0.6)]**

通常の「つかまえる」コマンドでは究極ゴリラを捕獲できない
(「究極ゴリラには普通の捕獲は通用しない！」メッセージを表示し、
ロープ使用時も同様にブロックする)。

### 捕獲成功条件
- プレイヤーがレベル99以上
- 「女神のウクレレ」を所持している(`state.player.hasUkulele`)
- 究極ゴリラのHPが1〜10の範囲
- 戦闘コマンド「うたう」を使用する

### 条件未達時のメッセージ
| 条件 | メッセージ |
|---|---|
| レベル不足 | 「歌声は森に響いた……しかし、まだ力が足りない。レベル99になれば届くかもしれない。」 |
| 女神のウクレレなし | 「歌おうとしたが、何かが足りない。伝説の楽器が必要なようだ。」 |
| HP高すぎ(>10) | 「究極ゴリラはまだ荒ぶっている！もっと弱らせなければ歌は届かない。」 |

### 捕獲成功時の演出メッセージ(バトルログ)
1. 「勇者の子孫は女神のウクレレを奏でた。」
2. 「森にやさしいメロディが響きわたる。」
3. 「究極ゴリラの怒りが静まっていく……」
4. 「究極ゴリラは静かに目を閉じた。」
5. 「究極ゴリラを捕まえた！」

### 暫定クリア
- 捕獲成功時に `state.gameCleared = true` を保存(セーブデータに記録)。
- OKボタンでフィールドに戻った後、暫定クリアモーダルを表示する。
- 「しかし、究極ゴリラは森へ帰ることを望んでいる。いつかまた、森の奥で会えるだろう。」
- クリア後もゲームは続けて遊べる(2周目要素は未実装)。

### 女神のウクレレ
- 重要アイテム。消耗しない(使用回数無制限)。売却不可。
- `state.player.hasUkulele`(bool)としてセーブ。
- マップに専用タイル 'U'(🪗)を追加し、一度開けると再入手不可。
- ステータス画面で所持状態を確認できる。

### 将来仕様
- BGM/SE・アニメーション付き本格エンドロール(v0.7で暫定実装済み。§28)
- 2周目要素・クリア後の強化コンテンツ
- レベル99到達時の専用演出(§17参照)


## 15. 設定

**[実装済み]**

画面右上「⚙️設定」から主人公の歩く速さ(遅い/普通/速い)を変更できる。
十字キーの押しっぱなしで設定速度の連続移動、タップは常に1歩。

**Version 0.4.1で追加:** ユーザーテストで「セーブ方法が分からない」
「目的が分からない」というフィードバックがあったため、設定モーダルに
以下を追加した。

- 「💾 オートセーブ中」の説明文(行動するたびに自動保存される旨)
- 「💾 今すぐセーブ」ボタン(押すと即座に`saveGame()`を呼び、
  「セーブしました」と表示する)
- 「🎯 目的を見る」ボタン(§3.5の目的説明画面を再表示)
- 「❓ ヘルプ」ボタン(§15.5のヘルプ画面を開く)

**Version 0.4.5で追加:** 設定モーダルに以下を追加した。

- 「🔄 ニューゲーム(セーブデータをリセット)」ボタン
  - 押すと確認ダイアログ(confirm)を表示:
    「本当に最初から始めますか？\n現在のセーブデータは削除されます。」
  - 「OK」を押すと `localStorage.removeItem(SAVE_KEY)` → `location.reload()`
  - キャンセルなら何もしない
  - 危険操作のため赤系テキスト・枠線で表示し、「⚠️ 危険な操作:」の注意文を添える
  - リセット後は初期状態で起動し、オープニングと目的説明が再表示される


## 15.5 ヘルプ画面

**[実装済み]**

⚙️設定モーダルの「❓ヘルプ」ボタンから開く静的な説明モーダル。
初めて遊ぶ人が迷わないよう、以下を1画面にまとめて記載している。

- ゲームの目的
- 操作方法
- セーブについて
- 戦闘について
- 捕獲について
- 装備について
- 状態異常について

内容はHTML側に直接記述した静的テキスト(動的生成ではない)。


## 16. セーブ/ロード

**[実装済み(一部)]**

`localStorage` による自動セーブ。保存対象: レベル/経験値/HP・MP/
覚えたまほう/所持金/所持アイテム数/所持UMA/UMA図鑑/現在の職業/歩く速度/
装備(装備中+所持品)/状態異常/オープニング・目的説明の既読フラグ。

行動(移動でのアイテム取得・戦闘終了・買い物・転職・装備変更・設定変更)の
たびに自動保存される。Version 0.4.1で「⚙️設定」モーダルに手動セーブボタンと
オートセーブ中である旨の説明を追加し、ユーザーに分かるようにした(§15)。

**Version 0.4.4で追加:** 宝箱の開封状態(`openedChests`: "x,y" -> true)も
セーブ対象に含めた。

**未対応(TODO)**: フィールド上のプレイヤー座標。再読込時は村の入口からスタートする。


## 16.5 ステータス確認画面

**[実装済み]**

画面上部「📊ステータス」ボタンから、現在のプレイヤー情報をまとめて
確認できるモーダルを開く。表示内容:

- 名前/レベル/HP・MP/経験値/所持金/職業/状態異常
- 現在の装備(武器/防具/盾/兜)
- 覚えているまほう
- 所持アイテム数(やくそう/捕獲ロープ/せき止めシロップ/デオドラントスプレー)
- 捕まえたUMAの総数、UMA図鑑の進捗(発見済み+捕獲済み/全体)


## 17. 今後追加予定(設計レベルで検討中のものを含む)

- 仲間システム拡張(§10。v0.5で酒場加入版を実装済み。将来: NPCシンボル/3分岐/追従等)
- 酒場機能拡張(§9.5。v0.5で基本メニュー実装済み。将来: 限定アイテム/会話等)
- 最強装備の入手手段(§8.6。イベント/ボス/ドロップ経由)
- 宝箱の増設・配置拡張(§5.7。現在4箱。マップ拡張に合わせて追加予定)
- BGM/SE付き本格エンドロール・2周目要素(§14.5。v0.7で5ページエンディング実装済み。§28)
- 横スクロールマップ(§5.5。大規模改修)
- まほう・必殺技の追加候補(§12の「将来候補」表。ロシアンフック/フランケンシュタイナー等)
- 通常モンスターの未実装候補(§6.1。キャンプ女子/痴漢/忍者かぶれ/霊界探偵等)
- 複数の街・ダンジョン・ワールドマップ
- BGM/SE
- ストーリー性のあるイベント・NPCとの会話
- 究極ゴリラ討伐に関するエンドコンテンツ(§14。検討中)
- セーブ対象の拡張(座標・フィールドアイテム状況)
- 図鑑からのUMA詳細ステータス確認
- 将来の課金要素(コンティニューの課金化を想定した設計。現状は未実装。
  詳細は `README.txt` の「将来の課金要素メモ」を参照)

このセクションは、プロデューサーから新しいアイデアが共有されるたびに
更新していく。新規アイデアは原則としてここではなく、該当する章
(UMA/アイテム/戦闘 等)に仕様として追記し、このセクションには
「未着手の検討事項」のみを残す。


## 18. 今回(Version 0.4)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。仕様として
保留し、対応する章には記載しているが、コードは未実装。

- 仲間システムの実装本体(§10。仕様のみ。Version 0.5予定)
- 酒場での仲間加入(§9.5。建物のみ実装、機能はなし)
- 究極ゴリラ捕獲イベント(専用の特別な捕獲演出。未設計)
- 「うたう」コマンド(戦闘/フィールドコマンドとして未設計)
- レベル99イベント(レベル99到達時の特別な演出。未設計)
- NPC追従(仲間システムの一部。§10の「設計メモ」参照)


## 23. 今回(Version 0.5)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- NPCのフィールド追従表示・シンボルエンカウント方式(§10。将来仕様)
- 仲間加入時の会話3分岐(加入/断る/戦闘)(§10。将来仕様 ← v0.5.1で2分岐まで実装)
- 仲間固有イベント・専用装備・仲間の成長要素(§10。将来仕様)
- 究極ゴリラ捕獲イベント本体・「うたう」コマンド・レベル99イベント(§14.5)
- 女神のウクレレ実装(§8.6)
- 最強装備のイベント入手(§8.6。将来仕様のまま維持)
- 横スクロールマップ本体(§5.5。仕様記載のみ)
- 大量の新規モンスター追加(§6.1。候補名のみ)
- 大量の新規まほう追加(§12。候補名のみ)


## 26. デバッグメニュー(Version 0.6.1で実装)

**[実装済み]**

URLに `?debug=1` が付いている場合のみ、設定画面に「🛠️ 開発用テスト」セクションを表示する。
通常URLでは表示されず、プレイヤーの通常体験を妨げない。

### 開発用URL
```
https://rokushakai.github.io/ultimate-gorilla/?debug=1
```

### 実装された機能

| ボタン | 機能 |
|---|---|
| Lv.99にする | レベル・ベースステータスをLv.99相当に即設定 |
| 女神のウクレレを入手 | hasUkulele=true、宝箱を開封済みにする |
| 究極ゴリラ強制エンカウント | startBattle(ultimategorilla)を直接呼ぶ |
| 敵HPを5にする | 戦闘中のみ有効。state.enemy.hpを5に設定 |
| 9999G追加 | 所持金を9999G増やす |
| クリア・ウクレレをリセット | gameCleared/hasUkulele/openedChests["9,14"]をリセット |

### 設計メモ
- `DEBUG_MODE` は IIFE内のトップレベル定数として定義(URLパラメータを判定)。
- `renderSettingsBody()` が `if (DEBUG_MODE)` でHTML生成・ボタンバインドを切り替える。
- 本番ビルドの概念がないため、削除ではなくURLフラグで制御する設計とした。


## 28. エンディング演出(Version 0.7で実装)

**[実装済み]**

究極ゴリラ捕獲後に表示される本格エンディング。従来の「暫定クリアモーダル」を
5ページ構成のエンディングモーダルに昇格させた。

### エンディングページ構成(ENDING_PAGES)

| ページ | 絵文字 | 見出し | 内容 |
|---|---|---|---|
| 1 | 🎵 | 女神のウクレレ | ウクレレを奏でる描写・ゴリラの瞳の変化 |
| 2 | 🦍 | 究極ゴリラ、森へ帰る | ゴリラが帰る描写・旅の幕引き |
| 3 | 👑 | 王様への報告 | 王様の言葉・「帰すことも勇者の務め」 |
| 4 | 🎬 | STAFF | スタッフロール風クレジット |
| 5 | 🏆 | クリアおめでとう！ | 称号・継続プレイ案内・再視聴案内 |

「つぎへ ▶」ボタンでページを送り、最終ページで「冒険を続ける」に変わる。

### スタッフロール(ENDING_CREDITS)

スタッフ表記は `script.js` の `ENDING_CREDITS` 配列で管理する。
変更する場合はこの配列の `role` / `name` を編集する。

| 役割 | 表記 |
|---|---|
| 企画 | あばれうまのりお |
| 仕様整理 | Kai |
| 実装 | Claude Code |
| Special Thanks | プレイしてくれた皆さん |

### クリア称号(v0.7追加)

| 状態 | 称号 |
|---|---|
| `gameCleared = false` | 勇者の子孫 |
| `gameCleared = true` | 森に歌を届けし者 |

称号はステータス確認画面(📊)の最上部に表示する。

### エンディング再視聴

- **設定画面(⚙️)**: クリア後に「🎬 エンディングを見る」ボタンを表示。
- **ステータス画面(📊)**: 重要アイテム欄のクリア済み表示の下に同ボタンを表示。
- どちらから開いても同じ5ページ構成のエンディングを最初から再生する。

### 図鑑の究極ゴリラ特別表示

捕獲済みの場合、図鑑で金枠 + 「伝説のUMA」「森へ帰った」を追加表示する。

### デバッグ連携(§26)

| ボタン | 機能 |
|---|---|
| 🎬 エンディングを再生 | `openEndingModal()` を直接呼ぶ |
| 🏆 クリア済みにする | `state.gameCleared = true` にしてセーブ |

### 設計メモ

- `ENDING_PAGES` 配列でページ内容を管理。テキスト変更はこの配列のみ編集。
- `state.endingPage` で現在ページを追跡(セーブ対象外)。
- `openEndingModal()` / `renderEndingPage()` で制御。
- BGM/SE・本格アニメーションは未実装。将来追加予定。


## 29. 今回(Version 0.7)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- BGM/SE・アニメーション付き本格エンドロール
- 2周目要素・クリア後の強化コンテンツ
- 横スクロールマップ(§5.5)
- ボスダンジョン・複数マップ
- 最強装備のイベント入手(§8.6)
- NPCのフィールド追従表示・シンボルエンカウント方式(§10)
- 仲間固有イベント(§10)


## 27. 今回(Version 0.6.1)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 本格的なエンディング画面・エンドロール(§14.5。暫定クリアモーダルのみ)
- 2周目要素・クリア後の強化コンテンツ(将来仕様)
- BGM/SE・音楽演出
- 横スクロールマップ(§5.5)
- NPCのフィールド追従表示・シンボルエンカウント方式(§10。将来仕様)
- 仲間固有イベント(§10。将来仕様)
- 最強装備のイベント入手(§8.6。将来仕様)
- ボスダンジョン・複数マップ
- 大規模なレベルバランス再設計(今回は調整のみ)


## 25. 今回(Version 0.6)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 本格的なエンディング画面・エンドロール(§14.5。暫定クリアモーダルのみ実装)
- 2周目要素・クリア後の強化コンテンツ(将来仕様)
- レベル99到達時の専用演出(§17。将来仕様)
- NPCのフィールド追従表示・シンボルエンカウント方式(§10。将来仕様)
- 仲間加入時3分岐(戦闘分岐)(§10。将来仕様)
- 最強装備のイベント入手(§8.6。将来仕様)
- 横スクロールマップ(§5.5)
- BGM/SE・音楽演出
- ボスダンジョン・複数マップ


## 24. 今回(Version 0.5.1)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 仲間加入時の3分岐(加入/断る/戦闘)(§10。今回は2分岐「加入/断る」のみ。将来仕様)
- NPCのフィールド追従表示・シンボルエンカウント方式(§10。将来仕様)
- 仲間固有イベント・専用装備・仲間の成長要素(§10。将来仕様)
- 究極ゴリラ捕獲イベント本体・「うたう」コマンド・レベル99イベント(§14.5 ← v0.6で実装)
- 女神のウクレレ実装(§8.6 ← v0.6で実装)
- 最強装備のイベント入手(§8.6。将来仕様のまま維持)


## 22. 今回(Version 0.4.5)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 仲間システム本体・酒場加入・NPC追従(§10。§18〜§21から継続)
- 究極ゴリラ捕獲イベント本体・「うたう」コマンド・レベル99イベント(§14.5)
- 女神のウクレレ実装(§8.6)
- 最強装備のイベント入手(§8.6。将来仕様のまま維持)
- 横スクロールマップ本体(§5.5。仕様記載のみ)
- 大量の新規モンスター追加(§6.1。候補名を追記のみ)
- 大量の新規まほう追加(§12。候補名を追記のみ)


## 21. 今回(Version 0.4.4)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 仲間システム本体・酒場加入・NPC追従(§10。§18〜§20から継続)
- 究極ゴリラ捕獲イベント本体・「うたう」コマンド・レベル99イベント(§14.5)
- 女神のウクレレ実装(§8.6)
- 最強装備のイベント入手(§8.6。将来仕様のまま維持)
- 横スクロールマップ本体(§5.5。仕様記載のみ)
- 大規模なマップ拡張


## 19. 今回(Version 0.4.1)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 仲間システム本体、酒場での仲間加入、NPC追従(§10。§18から継続)
- 究極ゴリラ捕獲イベント、「うたう」コマンド、レベル99イベント(§18から継続)
- 新しい大量装備の追加(今回は商人で売る一部の装備のみ追加。§8.5)
- 新しいUMAの大量追加(§6は今回変更なし)


## 20. 今回(Version 0.4.2)で明示的に実装しなかったもの

プロデューサーから「今回は実装しない」と指定された機能。

- 横スクロールマップ本体(§5.5。仕様記載のみ)
- 仲間システム本体、酒場での仲間加入、NPC追従(§10。§18・§19から継続)
- 究極ゴリラ捕獲イベント本体、「うたう」コマンド、レベル99イベント、
  女神のウクレレ実装(§14.5。仕様記載のみ)
- 大量の新規装備追加(§18・§19から継続)


## §30. 伝説装備イベント (Version 0.8)

### 概要

5種の伝説装備は商人では購入できず、フィールドの特別な宝箱または実家イベントでのみ入手可能。

### isLegendary フラグ

装備データに `isLegendary: true` を付与。
- 売却不可（商人の売却画面で「売却不可」ボタン・disabled表示）
- 装備画面で `★伝説` マークを表示
- `sellEquip()` でも二重チェックし弾く

### 伝説装備一覧

| 装備名 | 種別 | 効果 | 入手条件 | タイル |
|---|---|---|---|---|
| ペガサスのよろい | 防具 | 防御+14 HP+5 | フィールド 🌟 宝箱(A)、Lv50以上 | A(11,4) |
| 六連のたて | 盾 | 防御+20 | 実家で休む、Lv60以上 | — |
| 宇宙のかぶと | 兜 | 防御+15 | フィールド ⭐ 宝箱(C)、女神のウクレレ所持 | C(11,11) |
| 如意棒 | 武器 | 攻撃+58 | フィールド 🪄 宝箱(J)、Lv70以上＋ジュリタニ同行 | J(7,15) |
| アンドロメダの鎖 | 武器 | 攻撃+44 | 実家で休む、gameCleared後 | — |

### 新タイル

- `A`: 🌟（ペガサスのよろい伝説宝箱）
- `C`: ⭐（宇宙のかぶと伝説宝箱）
- `J`: 🪄（如意棒伝説宝箱）
- 開封後は 📦 に変わる（`state.openedChests` 管理）

### セーブデータ

`state.eventFlags` オブジェクトで管理（`openedChests` と分離）:

```javascript
eventFlags: {
  pegasusArmorGot: false,
  sixfoldShieldGot: false,
  cosmicHelmetGot: false,
  nyoiboGot: false,
  andromedaGot: false
}
```

### ステータス画面

- 「★ 伝説装備 (N/5)」セクションを表示
- 各装備を「入手済 / 未入手」で表示
- 目標表示にレベル帯ごとのヒントを追加（Lv50+, Lv60+, Lv70+, クリア後）

### HOME_HINTS 追加（4件）

- 「白く光る宝箱には、強き者だけが触れられるらしい。」
- 「星の宝箱は、女神の音色に反応するという。」
- 「旅を終えた者には、王様から褒美があるそうだ。」
- 「実家には、昔から伝わる盾があるとかないとか……。」

### デバッグメニュー（?debug=1）

- `⭐ 伝説装備を全入手` — 5種すべてを所持＆フラグON
- `🔄 伝説装備フラグをリセット` — eventFlags を全て false に戻す（所持品は残る）


## §31. UMA図鑑詳細表示 (Version 0.8.1)

### 概要

図鑑一覧から発見済み・捕獲済みのUMAをタップすると詳細ステータスを確認できる。
未発見UMAは詳細を見られない（一覧上では ❔ ？？？ のまま）。

### 図鑑一覧の変更点

- 上部に進捗表示: 「発見 N/M」「捕獲 N/M」
- 発見済み・捕獲済みのUMAアイテムはタップ可能（cursor:pointer、.clickableクラス付与）
- タップ→ `openMonsterDetailModal(id, "uma")` を呼び出す
- メタル系（特殊エネミー）セクションを一覧下部に追加（常に表示、常にタップ可能）

### UMA詳細モーダル（uma-detail-modal）

発見済みまたは捕獲済みのUMAをタップすると、以下を表示:

| 項目 | 内容 |
|---|---|
| 絵文字 | 大きく表示 |
| No. | UMA_DATA内の順番（001〜009） |
| 名前 | UMA名 |
| レア度 | コモン/アンコモン/レア/伝説 |
| 分類 | UMA / 伝説UMA |
| 捕獲状態 | ✅ 捕獲済み / ❌ 未捕獲 |
| HP | 最大HP |
| 攻撃力 | 攻撃力 |
| 防御力 | 防御力 |
| 捕獲率 | % 表示 |
| 経験値 | 倒した/逃げられた時の獲得EXP |
| 売却価格 | Gで表示 |
| 特殊攻撃 | 状態異常付与がある場合のみ表示 |
| 説明文 | フレーバーテキスト（desc フィールド） |

発見済み・捕獲済みで情報量の差は設けない（両方フル表示）。

### 究極ゴリラの特別表示

- 捕獲前（発見済み）: 「普通の「つかまえる」は一切通用しない。特別な条件が必要だ。」警告を表示
- 捕獲後（クリア済み）: 金枠、「★ 伝説のUMA」「🌿 森へ帰った」バッジ、特別説明文

### メタル系の特別表示

一覧下部の「メタル系（特殊エネミー）」セクションから詳細を見られる。

| 項目 | 内容 |
|---|---|
| 絵文字 | 大きく表示 |
| 分類 | メタル系（黄色表示） |
| HP/攻撃力/防御力/経験値 | 通常通り表示 |
| 特徴 | 出現率・防御・経験値に関する説明 |
| 説明文 | desc フィールドのフレーバーテキスト |

### UMA/モンスターの説明文（descフィールド）

UMA_DATA 9種 + METAL_DATA 3種に `desc` フィールドを追加。

### ステータス画面との整合

`renderStatusBody()` の UMA セクションを更新:
- 「図鑑進捗」→「図鑑発見 N/M」「図鑑捕獲 N/M」の2行表示

### 将来の図鑑フィルター案（未実装）

- 全表示 / 発見済み / 捕獲済み / 未捕獲 / レア / メタル系 のフィルターボタン


## §32. NPC会話システム (Version 0.8.2)

### 概要

フィールドマップ上に5人の固定NPCを配置し、プレイヤーが接触するとNPC会話モーダルを表示する。
会話内容はゲーム進行状態に応じて動的に切り替わる。会話済みフラグは不要（毎回同じセリフ/状態別セリフ）。

### NPC一覧

| ID | 名前 | 絵文字 | タイル | 配置位置 | 役割 |
|---|---|---|---|---|---|
| umaDoctor | UMA博士 | 🔎 | D | (2, 2) | 図鑑収集のヒント、メタルゴリラの紹介 |
| traveler | 旅人 | 🧳 | R | (5, 5) | 伝説宝箱・フィールド探索のヒント |
| smith | 鍛冶屋 | 🔨 | K | (10, 2) | 装備・伝説装備・如意棒のヒント |
| researcher | ゴリラ研究家 | 📚 | E | (6, 9) | 究極ゴリラ捕獲条件のヒント |
| royalMessenger | 王様の使い | 👑 | S | (10, 1) | クリア後報酬（アンドロメダの鎖）の案内 |

### マップ配置

NPC タイルは新規タイル文字 D / R / K / E / S を使用する。

```
Row 1: #,,,H,,,T,,S#   (S=王様の使い, x=10)
Row 2: #,D,,,,M,,K,#   (D=UMA博士 x=2, K=鍛冶屋 x=10)
Row 5: #....R..B...#   (R=旅人 x=5)
Row 9: #..W..E.....#   (E=ゴリラ研究家 x=6)
```

- NPC タイルは SAFE_TILE に追加（エンカウントなし）
- NPC タイルは BLOCKED に追加しない（歩いて接触可能）
- TERRAIN_EMOJI: D=🔎, R=🧳, K=🔨, E=📚, S=👑

### NPC会話モーダル（npc-modal）

- 既存の `.modal` / `.modal-box` クラスを使用
- NPC絵文字（大）＋名前＋セリフを表示
- 「閉じる」ボタンで閉じる

### 状態に応じたセリフ切り替え

| NPC | 条件 | セリフ概要 |
|---|---|---|
| UMA博士 | 捕獲数 8以上 | 図鑑ほぼ完成を称賛 |
| UMA博士 | 捕獲数 4以上 | 図鑑タップで詳細確認を案内 |
| UMA博士 | それ以外 | 図鑑の基本的な使い方を案内 |
| 旅人 | hasUkulele=true | 伝説宝箱CとJへのヒント |
| 旅人 | level 50以上 | 白い宝箱の存在をヒント |
| 旅人 | それ以外 | まずは強くなるよう促す |
| 鍛冶屋 | 伝説装備1個以上取得 | 伝説装備の売却不可を告知 |
| 鍛冶屋 | ジュリタニ同行中 | 如意棒への直接ヒント |
| 鍛冶屋 | level 70以上 | 如意棒には仲間が必要とヒント |
| 鍛冶屋 | それ以外 | 装備変更の基本案内 |
| ゴリラ研究家 | gameCleared=true | クリア後・伝説装備収集を促す |
| ゴリラ研究家 | Lv99＋ウクレレ所持 | HP減らしてうたうよう案内 |
| ゴリラ研究家 | Lv99＋ウクレレなし | ウクレレ宝箱への誘導 |
| ゴリラ研究家 | level 50以上 | メタルゴリラでレベル上げを案内 |
| ゴリラ研究家 | それ以外 | 究極ゴリラ捕獲の存在を示唆 |
| 王様の使い | andromedaGot=true | アンドロメダの鎖を受け取ったことを称賛 |
| 王様の使い | gameCleared=true | 実家で休むと報酬があると案内 |
| 王様の使い | それ以外 | 究極ゴリラの報告を王様が待っていると伝える |

### 実装対象ファイル

- `script.js`: NPC_DATA オブジェクト・openNpcModal 関数・RAW_MAP・TERRAIN_EMOJI・SAFE_TILE・movePlayer
- `index.html`: `npc-modal` モーダル追加
- `style.css`: `.npc-speech` スタイル追加


## §33. 伝説装備追加・NPCヒント連動イベント (Version 0.8.3)

### 概要

v0.8.2 のフィールド NPC と連動し、NPCから得たヒントを頼りに伝説装備を探索・入手できるシステムを追加する。

### 追加伝説装備

| 装備名 | 種別 | 能力 | 入手方法 | 条件 |
|---|---|---|---|---|
| キグナスのかぶと | 兜 | 防御+12 HP+5 | フィールド ✨宝箱(X) (9,6) | Lv40以上 |
| ドラゴンのたて | 盾 | 防御+26 HP+8 | 王様の使い(S)NPC | gameCleared=true かつ未受取 |

既存データの `cygnuskabuto` / `dragonshield` を強化してイベント限定化（`isLegendary: true` / `buyPrice` 削除）。

### マップ変更

```
Row 6: #...~~~..X..#  (X=キグナスのかぶと伝説宝箱, x=9, y=6)
```

TERRAIN_EMOJI: `"X": "✨"`  
SAFE_TILE: `"X": true`

### 王様の使いのクリア後報酬

移動処理（movePlayer）でタイル S を踏んだとき：
- `state.gameCleared === true` かつ `!state.eventFlags.dragonShieldGot` → `giveKingReward()` を呼ぶ
- それ以外 → 通常の `openNpcModal("S")`

`giveKingReward()`: ドラゴンのたてを所持品に追加し `dragonShieldGot = true` をセット、alert で演出。

### セーブデータ

`state.eventFlags` に2フラグを追加:
- `cygnusHelmetGot: false` — キグナスのかぶと入手フラグ
- `dragonShieldGot: false` — ドラゴンのたて入手フラグ

`loadGame()` は古いセーブに対して各フラグを `false` で補完する。

### NPC セリフ更新

| NPC | 更新内容 |
|---|---|
| 旅人 (R) | Lv40以上で ✨宝箱の存在をヒント |
| 鍛冶屋 (K) | キグナスのかぶと・如意棒の場所ヒントを段階別に追加 |
| 王様の使い (S) | dragonShieldGot 後は「見守っている」セリフ |

### 伝説装備進捗

LEGEND_EQUIPS: 5種 → 7種。ステータス画面・目標画面の表示を `LEGEND_EQUIPS.length` に自動連動。

### デバッグ

`debugGetAllLegendary()` / `debugResetLegendary()` に新フラグ・新装備を追加。


## §34. BGM/SE・サウンド設定 (Version 0.8.4)

### 概要

Web Audio API を使った軽量 BGM/SE システムを追加する。外部音声ファイル不要。
スマホの自動再生制限に対応し、ユーザーが操作した時のみ音を鳴らす。
サウンド設定はニューゲームでリセットされない（別 localStorage キーで保存）。

### サウンド設定

- 設定キー: `"ultimateGorillaSoundV1"`（`SAVE_KEY` とは別）
- 設定項目: `soundEnabled`（全体）、`bgmEnabled`（BGM）、`seEnabled`（SE）
- デフォルト: すべて false（初回起動時は音なし）
- ニューゲームでリセットしない（プレイヤー環境設定として扱う）

### BGM 種別

| 種別 | タイプ | 説明 |
|---|---|---|
| フィールドBGM | `"field"` | 明るいレトロRPG風ループ。Cメジャー 120BPM |
| バトルBGM | `"battle"` | 緊張感のある速い曲。Aマイナー 150BPM |
| エンディングBGM | `"ending"` | 穏やかなアルペジオ風。Fメジャー 80BPM |

BGM遷移（即時切り替え。§38参照）:
- フィールド歩行（dpad/キー入力） → `updateBGM("field")`
- 戦闘開始（actuallyStartBattle） → `updateBGM("battle")`
- 戦闘終了（finishBattle） → `updateBGM("field")`
- エンディング開始（openEndingModal） → `updateBGM("ending")`
- エンディング終了 → `updateBGM("field")`

BGM切り替え方針（§38で確定）:
- フェードなし即時切り替えを標準とする。演出より安定性を優先
- 常に同時に1曲だけ鳴る
- 切り替え時は旧BGMを即停止し、新BGMを即再生

### SE 種別

| 種別 | タイプ | 発火タイミング |
|---|---|---|
| ボタン | `"btn"` | — |
| バトル開始 | `"battleStart"` | `actuallyStartBattle()` |
| 攻撃 | `"attack"` | `doFight()` |
| ダメージ | `"damage"` | `enemyTurn()` |
| 捕獲成功 | `"captureOk"` | `attemptCapture()` 成功時 |
| 捕獲失敗 | `"captureFail"` | `attemptCapture()` 失敗時 |
| レベルアップ | `"levelUp"` | `levelUp()` |
| 宝箱を開ける | `"chestOpen"` | `openChest()` / `openLegendaryChest*()` / `openUkuleleChest()` |
| アイテム入手 | `"itemGet"` | `giveKingReward()` |
| エンディング開始 | `"endingStart"` | `openEndingModal()` |

### 音量

- BGM 全体: 0.05 〜 0.06（vol フィールド）
- SE 全体: 0.08 〜 0.12（各ノートの vol フィールド）

### 技術仕様

- `AudioContext` / `webkitAudioContext` フォールバック
- OscillatorNode + GainNode の組み合わせで発音
- BGM は短いノート列を `setTimeout` で再スケジュールするループ方式
- SE は単発の短い音バースト（複数ノートを時間差で重ねる）
- `initAudioContext()` はユーザー操作イベントから呼ぶ（iOS自動再生制限対応）
- すべての Audio 処理を try/catch でラップし、失敗時はサイレント無視
- AudioContext が使えない環境でも graceful degradation

### 設定画面 UI

設定モーダルの `renderSettingsBody()` に以下を追加:

```
🔊 サウンド設定
[🔊 サウンド: ON]  [🔇 サウンド: OFF]  ← トグルボタン（現在状態を表示）
[🎵 BGM: ON]       ← soundEnabled=false の時は opacity:0.45
[🔔 SE: ON]        ← soundEnabled=false の時は opacity:0.45
```

### デバッグ機能（`?debug=1`）

- `[TEST] SEを鳴らす` — levelUp SE を再生テスト
- `[TEST] フィールドBGM` — フィールドBGM を強制再生（soundEnabled/bgmEnabled を ON にする）
- `[TEST] バトルBGM` — バトルBGM を強制再生
- `[TEST] エンディングBGM` — エンディングBGM を強制再生
- `[TEST] BGM停止` — BGM を停止

---

## §35 Lv99到達演出・成長達成感アップ（v0.8.5）

### 概要

レベル99という最終目標到達を特別な演出で強調し、プレイヤーの達成感を高める。

---

### 新フラグ

| フラグ | 場所 | 説明 |
|---|---|---|
| `state.eventFlags.level99Reached` | `state.eventFlags` | Lv99に到達したことを示す。`level99Shown`（演出表示済み）と独立管理し、デバッグリセット可能 |

---

### Lv99到達モーダル（enhanced）

- ヘッダー: `⚡ ついにレベル99！`
- 本文（`openLv99Modal()`）:
  - 「ついに、勇者の子孫はLv.99に到達した！」
  - 「長い旅路の果てに、肉体も精神も限界まで鍛え上げられた。」
  - 「いまなら、伝説のUMA「究極ゴリラ」の心に届くかもしれない。」
  - ウクレレ所持の場合: 「🪗 女神のウクレレも所持済み！条件は整った！」→ 「うたう」コマンド説明
  - ウクレレ未所持: 「だが、力だけでは足りない。」→ ウクレレの場所案内
  - 末尾: 「旅の終わりは、もうすぐそこだ。」

---

### `level99` SE（ファンファーレ）

`SE_SPECS` に `level99` キーを追加。7ノートの上昇ファンファーレで `levelUp` より長く・大きい音。

---

### ステータス画面 称号

優先順: `gameCleared` > `level >= 99 or level99Shown` > それ以外

| 状態 | 称号 |
|---|---|
| クリア済み | 森に歌を届けし者 |
| Lv99到達（未クリア） | 究極に近づきし者 |
| それ以外 | 勇者の子孫 |

---

### ゴール表示（4ステート）

| 状態 | 表示 |
|---|---|
| クリア済み | 🏆 クリア済み！称号・伝説装備進捗 |
| Lv99 & ウクレレ所持 | Lv.99達成 & ウクレレ所持！うたうコマンド案内 |
| Lv99 & ウクレレ未所持 | 🌟 Lv.99到達済み！ウクレレを探す案内 |
| Lv99未満 | 残りレベル・メタルゴリラ等のヒント |

---

### 実家ヒント（`openHomeModal()`）

Lv99到達後はランダムヒントの代わりに文脈ヒントを優先表示:

- Lv99 & ウクレレ所持 → うたうコマンド案内
- Lv99 & ウクレレ未所持 → ウクレレ探索案内

---

### NPC更新

**UMA博士（D）**:

- Lv99到達後（未クリア）: 「素晴らしい成長じゃ。もはや君は、伝説に手を伸ばせる場所にいる。あとは女神のウクレレと歌声だけが必要だ。」
- クリア後: 「図鑑の完成まで目指してみないか。まだ捕まえていない伝説が残っているぞ。」
- それ以外: 「キラリと光るゴリラに出会ったら、経験値のチャンスじゃ！見逃すなよ。」（従来通り）

---

### デバッグ機能追加（`?debug=1`）

| ボタン | 動作 |
|---|---|
| 📉 Lv.98にする | Lv98にセット。次の戦闘でLv99到達を試せる |
| ⬆️ 次の戦闘でLvUP(EXP設定) | `p.exp = p.nextExp - 1` に設定 |
| 🔄 Lv99到達フラグをリセット | `level99Shown` と `eventFlags.level99Reached` を false に戻す |
| 🎖 Lv99演出を再生（既存） | `level99` SE + モーダル表示 |
| 📈 Lv.99にする（既存・改修） | 初回ならSE + モーダル表示、再実行なら従来通りトースト |

---

## §36 BGM重なり修正（v0.8.6）

### 問題

BGM切り替え時に前のBGMが停止せず、複数BGMが重なって再生されていた。

**原因**: `stopBGM()` は setTimeout をクリアするが、Web Audio API でスケジュール済みのオシレーターノードは AudioContext タイムライン上にあるため、JavaScript の変数制御では止まらない。

### 修正方針

| 追加変数 | 説明 |
|---|---|
| `bgmGeneration: number` | BGM起動ごとにインクリメント。旧ループの setTimeout コールバックが世代不一致で自動失効する |
| `activeBgmNodes: OscillatorNode[]` | 現在ループ中の全オシレーターを追跡。`stopBGM()` で一括 `.stop()/.disconnect()` |

### 変更関数

- **`stopBGM()`**: `bgmGeneration++` → `clearTimeout` → `activeBgmNodes` 全ノードを止める → `activeBgmNodes = []`
- **`startBGM(type)`**: `stopBGM()` 後に `gen = bgmGeneration` を取得し `_scheduleBGMLoop` に渡す
- **`_scheduleBGMLoop(type, startTime, gen)`**: 先頭で `gen !== bgmGeneration` チェック → 生成したノードを `activeBgmNodes.push(osc)` → setTimeout コールバック内でも世代チェック
- デバッグBGMボタン: `bgmCurrentType = null` を `stopBGM()` に変更（正しくノードを停止するため）

---

## §37 攻略ペーパービュー屋（v0.8.6）

### 概要

フィールドの村エリアに新NPC「攻略ペーパービュー屋」を追加。ゲーム内通貨でプレイヤーの現在進行状況に応じたヒントを購入できる。

### マップ配置

| フィールド文字 | 絵文字 | 位置 | タイル種別 |
|---|---|---|---|
| `N` | 📰 | (4, 3) | 安全地形（SAFE_TILE）、進入可能 |

### 購入メニュー

| 選択肢 | 料金 | 内容 |
|---|---|---|
| ぼんやりヒント | 10G | 方向性のみ示す曖昧なヒント |
| 具体的ヒント | 50G | 次にやるべき行動を示すヒント |
| ほぼ答え | 100G | 場所・条件・手順を明示した回答 |
| 閉じる | 無料 | 何もせずに閉じる |

所持金が足りない場合はボタンが無効化される（購入不可）。

### ヒント判定優先順（`getHintPriority()`）

| 優先度 | 条件 |
|---|---|
| 0 | ゲームクリア済み |
| 1 | Lv < 40 |
| 2 | Lv ≥ 40、キグナスのかぶと未入手 |
| 3 | キグナスのかぶと入手済み、ペガサスのよろい未入手 |
| 4 | ウクレレ未所持 |
| 5 | Lv ≥ 70、ジュリタニ同行、如意棒未入手 |
| 6 | Lv < 99 |
| 7 | Lv ≥ 99（クリア未完） |
| 8 | その他（フォールバック） |

### モーダル

- ID: `hint-shop-modal`
- 動的コンテンツ: `renderHintShopMenu()` / `renderHintResult()`
- ヒント購入時: `playSE("itemGet")`、所持金を減らし `saveGame()`
- 購入後、「もう一度買う」で `renderHintShopMenu()` に戻れる

### 主要関数

| 関数名 | 役割 |
|---|---|
| `getHintPriority()` | 現在のゲーム状態からヒント優先度(0〜8)を返す |
| `getProgressHint(tier)` | 優先度 × tier(1〜3) でヒント文字列を返す |
| `openHintShopModal()` | ヒントショップモーダルを開く |
| `renderHintShopMenu()` | 3段階メニューをレンダリング |
| `buyHint(tier, cost)` | 所持金チェック・減算・SE・ヒント表示 |
| `renderHintResult(hint, tier, cost)` | ヒント結果をレンダリング |

## §38. BGM即時切り替え修正 (Version 0.8.6.2)

### 問題

v0.8.6 実装の `stopBGM()` にて、`_scheduleBGMLoop` で各ノートに `osc.stop(t + dur)` を呼び済みのOscillatorNodeに対して、再度 `stop()` を呼ぶと Web Audio API が `InvalidStateError` をスローする。これを `try-catch` で握りつぶしていたため、古いBGMノードが止まらず約2秒間（最長1ループ分）のBGM重複再生が発生していた。

### 解決策

**マスターゲインノード（`bgmMasterGain`）** を導入。

すべてのBGMオシレーターを `audioCtx.destination` に直接接続せず、`bgmMasterGain → audioCtx.destination` 経由で接続する。BGMを停止するときは `bgmMasterGain.disconnect()` で音声グラフから切断する。接続が切れたノードは発音できないため即座に消音される。

### 変数

| 変数 | 型 | 役割 |
|---|---|---|
| `bgmMasterGain` | `GainNode or null` | 全BGMノードの共通出力先。`stopBGM()`で切断・null化、次の `startBGM()` で再生成 |

### 関数

| 関数 | 説明 |
|---|---|
| `getOrCreateBgmMasterGain()` | `bgmMasterGain` がなければ作成して返す |
| `stopBGM()` | `stopBGMHard()` の後方互換エイリアス（v0.8.6.3以降） |
| `startBGM(type)` | `stopBGMHard()` 後に `bgmSessionId`/`bgmGeneration` をキャプチャし `_scheduleBGMLoop` を呼ぶ |
| `_scheduleBGMLoop()` | `{osc, gain}` ペアを `activeBgmNodes` に追跡。タイマーIDを `activeBgmTimers` に追跡 |

### BGM切り替えの期待動作

- 戦闘開始: フィールドBGM即停止 → 戦闘BGM即再生（重複なし）
- 戦闘終了: 戦闘BGM即停止 → フィールドBGM即再生（重複なし）
- エンディング移行・終了も同様

### デバッグログ（`?debug=1` 時のみ）

```
[BGM] immediate switch: field -> battle
[BGM] stop immediate: field
[BGM] play: battle
```

---

## §39. BGMノード完全停止・予約音キャンセル (Version 0.8.6.3)

### 問題

v0.8.6.2 の `stopBGM()` は `bgmMasterGain.disconnect()` で即消音していたが、`activeBgmNodes` に `osc` 単体のみ保存していたため `gain.gain` のスケジュール済み音量変化をキャンセルできなかった。また `activeBgmTimers` が未実装で古い `setTimeout` がキャンセルされず、旧セッションのループタイマーが発火して新BGMに `_scheduleBGMLoop` が二重実行され約5秒の重複が発生していた。

### 解決策

**`stopBGMHard()` を新設**し、次の4段階消音を行う。

1. `bgmSessionId++` / `bgmGeneration++` でセッション世代を更新 → 古いコールバックは全てスキップ
2. `activeBgmTimers` の全 setTimeout を `clearTimeout()` でキャンセル
3. `activeBgmNodes` の各 `{osc, gain}` に対して `gain.gain.cancelScheduledValues()` + `gain.gain.setValueAtTime(0, now)` + `gain.disconnect()` で消音
4. `bgmMasterGain` も `gain=0` + `disconnect()` + null化

`stopBGM()` は `stopBGMHard()` のエイリアスに変更（既存呼び出し箇所はそのまま動作）。

### 変数

| 変数 | 型 | 役割 |
|---|---|---|
| `bgmSessionId` | `number` | BGMセッションID。`stopBGMHard()` で +1。古いタイマーコールバックが比較してスキップ |
| `activeBgmNodes` | `Array<{osc, gain}>` | 実行中BGMノードペア。`stopBGMHard()` で一括消音 |
| `activeBgmTimers` | `Array<number>` | 実行中BGMタイマーID。`stopBGMHard()` で全 `clearTimeout()` |

### 関数

| 関数 | 説明 |
|---|---|
| `stopBGMHard()` | 4段階消音。セッションID更新 + タイマー全キャンセル + ノード消音 + マスターゲイン破棄 |
| `stopBGM()` | `stopBGMHard()` への後方互換エイリアス |
| `startBGM(type)` | `stopBGMHard()` 後に `session = bgmSessionId` / `gen = bgmGeneration` をキャプチャして渡す |
| `_scheduleBGMLoop(type, startTime, gen, session)` | `session`パラメータ追加。各ノートを `{osc, gain}` で追跡。タイマーIDを `activeBgmTimers` に push |

### デバッグログ（`?debug=1` 時のみ）

```
[BGM] stop hard session: 2 active nodes: 12 active timers: 1
[BGM] stop hard complete, active nodes after: 0
[BGM] new session: 2 battle
[BGM] schedule skipped old session: 1 2
[BGM] loop timer skipped old session: 1 2
```

### デバッグボタン

- `btn-debug-bgm-hard-stop`: `stopBGMHard()` を呼び出し。`activeBgmNodes` 件数をトーストに表示

---

## §40. のりお指令: モンスター追加・のらいぬ調整・戦闘メッセージ改善 (Version 0.8.7)

### 背景

プロデューサー「のりお」からの指令を反映した。通常モンスターの追加、
のらいぬの序盤強敵化、HP0時の戦闘終了メッセージ改善を一括実施。

### HP0時の戦闘終了メッセージ変更 (v0.8.7)

- **変更前**: 「○○に逃げられた！」(被害者目線の表現)
- **変更後**: 「○○は逃げていった！！」ランダムバリエーション4種

バリエーション:
1. `○○は逃げていった！！`
2. `○○はあわてて逃げていった！！`
3. `○○はフラフラしながら逃げていった！！`
4. `○○は戦意を失って逃げていった！！`

変更方針: 「敵が死亡/撃破」ではなく「逃げていった(現象描写)」を維持しながら、
プレイヤーが「勝った感じ」を得られるようにする。経験値・所持金獲得は従来通り。

### のらいぬの序盤強敵化 (v0.8.7)

**変更前**: HP 13, 攻撃 5, 防御 1, EXP 7, 出現重み 8

**変更後**: HP 18, 攻撃 8, 防御 2, EXP 12, 出現重み 5

方針:
- Lv1の通常攻撃1〜2発では倒せない耐久力(序盤の1発: 5+0~3-2=3~6ダメ)
- 攻撃8は序盤では怖い(プレイヤーdef=2で6〜8ダメ。3発で致死級)
- EXPを増やして「頑張って倒した甲斐」を強調
- 出現重みを下げてやや稀にする(序盤から連続遭遇しないよう)
- `startMsg` フィールドを追加。専用出現メッセージを表示: 「のらいぬが低くうなっている……！ レベルが低いうちは逃げるのが賢明かもしれない。」

### 出現バランス方針

- 序盤モンスター(minLevel 1-2): 出現重み8〜10。スライム・蚊・キャンプ女子等
- 中盤モンスター(minLevel 3-6): 出現重み5〜6。忍者かぶれ・先生・グルメ気取り等
- 後半モンスター(minLevel 7-12): 出現重み3〜4。アンドレ・空手姉妹・宇宙人等
- `chooseEnemy()` の `minLevel <= lv` フィルタにより自然に出現タイミングが調整される

### startMsg フィールド (v0.8.7)

モンスターデータに `startMsg` フィールドを追加可能にした。
`actuallyStartBattle()` で、`monster.startMsg` が存在する場合に
出現メッセージの直後に1行追加表示する。

### デバッグボタン (v0.8.7追加)

- `btn-debug-encounter-wilddog`: のらいぬ強制エンカウント
- `btn-debug-encounter-random`: ランダム通常モンスター強制エンカウント


## §41. NPCセリフ拡充・攻略ヒント強化 (Version 0.8.8)

### 背景

v0.8.7 で追加されたモンスターやバランス調整を、NPC会話・ヘルプ・ヒントで
自然に説明し、プレイヤーに伝わるようにする。新機能追加はなし。

### NPC役割の再整理

| NPC      | 役割                        | 強化方針                                         |
|----------|-----------------------------|--------------------------------------------------|
| UMA博士  | 図鑑・UMA全般               | UMA vs 通常モンスター説明、メタルゴリラヒント追加 |
| 旅人     | 探索・道案内・逃げ作戦      | 逃げても良いこと、装備重要性を強化               |
| 鍛冶屋   | 装備・伝説武器              | 序盤の装備アドバイス、のらいぬ対策追加           |
| ゴリラ研究家 | ゴリラ種類・究極ゴリラ  | 序盤向けゴリラ種類説明を追加                     |
| 王様の使い | 目的・進行方向              | クリア前ヒントをレベル段階で多段化               |

### NPC_DATA 変更方針

各 NPC の `getLines()` をレベル段階別に拡充する。

- D(UMA博士): capturedCount 既存分岐維持 + lv<5/lv5-9/lv≥10 のレベル段階追加
- R(旅人): 既存 hasUkulele/lv≥50/lv≥40 維持 + lv≥15 段階追加 + else 内容強化
- K(鍛冶屋): 既存分岐維持 + lv<10 向け装備アドバイス追加 + lv<30 伝説テーザー追加
- E(ゴリラ研究家): 既存分岐維持 + lv<10 向けゴリラ種類説明 + lv10-49 メタルゴリラヒント
- S(王様の使い): クリア前をlv≥50/else で2段階化し、具体的な目標を提示

### HOME_HINTS 追加 (v0.8.8)

既存13件に6件追加して19件にする:

1. 「のらいぬが怖い時は、無理に戦わず逃げてもいいよ。」
2. 「昔は勝てなかった敵にも、レベルが上がると勝てるようになるよ。」
3. 「装備を整えると、同じレベルでもずっと楽になるよ。」
4. 「メタルゴリラを見つけたら経験値チャンスだよ。」
5. 「敵が逃げていっても経験値が入るなら、勝ったも同然だよ。」
6. 「宝箱を見つけたら忘れずに開けておこう。」

### ヘルプ画面追加セクション (v0.8.8)

`index.html` の help-modal に2セクションを追加:

- **💡 戦闘のコツ**: 逃げ作戦・のらいぬ警告・HP0での経験値獲得・レベルアップ効果
- **👾 モンスターの種類**: 通常モンスター・UMA・メタルゴリラ系の特徴と違い

### デバッグボタン追加 (v0.8.8)

NPC会話テスト用にレベル設定ボタンを追加:

- `btn-debug-lv1`: Lv.1にする
- `btn-debug-lv5`: Lv.5にする
- `btn-debug-lv10`: Lv.10にする


## §42. 緊急バグ修正: ステータスメニュー復旧・攻略ペーパービュー屋購入不可修正 (Version 0.8.8.1)

### 背景

v0.8.8 リリース後、スマホ実機テストで2件のバグを確認した。

### バグ1: ステータスメニュー非表示 (iOS セーフエリア問題)

**原因**: `<meta viewport-fit=cover>` を設定しているため、iOS Safari では
viewport が Dynamic Island / ノッチの裏まで拡張される。
`#game` に `padding-top: env(safe-area-inset-top)` がないため、
status-bar コンテンツがノッチの裏に隠れて見えない。

**修正**: `style.css` の `#game` に `padding-top: env(safe-area-inset-top, 0px)` を追加。
`#dpad` に `padding-bottom: env(safe-area-inset-bottom, 0px)` も追加(iPhone ホームバー対策)。

### バグ2: 攻略ペーパービュー屋の所持金 undefined・購入不可

**原因**: `renderHintShopMenu()` と `buyHint()` が `p.money` を参照しているが、
ゲーム内の所持金プロパティは `p.gold` (state.player.gold) である。
`p.money` は undefined なので、表示が `undefinedG` になり、
`undefined >= 10` が false になるため購入ボタンも disabled になる。

**修正**: `renderHintShopMenu()` と `buyHint()` 内の `p.money` を全て `p.gold` に変更。

### バグ3: 攻略ペーパービュー屋のラベル縦崩れ

**原因**: `.shop-menu-btn` が `width: 100%` を持つため、`.shop-row` の flex コンテナ内で
ボタンが全幅を占有し、ラベル `<span>` のスペースが 0 に近くなってテキストが縦方向に折り返される。

**修正**: `style.css` に `.shop-row .shop-menu-btn { width: auto; flex: 0 0 auto; }` を追加。


## §43. 横スクロールマップ試作 (Version 0.9)

### 概要

通常の俯瞰マップとは別に「横スクロールマップ」モードを試作追加する。
横スクロールマップは将来的なマップ拡張のプロトタイプ実装であり、
既存の通常マップおよびゲームシステムには一切影響を与えない。

### 状態管理

```
state.mapMode     : "normal" | "side"  (デフォルト: "normal")
state.sideMap.x   : 横マップ上のプレイヤーX座標
state.sideMap.y   : 横マップ上のプレイヤーY座標 (プロトタイプではy=1固定)
state.sideMap.stage : ステージ番号 (現在: 1 のみ)
state.sideMap.openedChests : 開封済み宝箱 { "x,y": true }
```

### マップデータ仕様

| 定数 | 値 | 説明 |
|---|---|---|
| SIDE_MAP_WIDTH | 40 | マップ横幅 |
| SIDE_MAP_HEIGHT | 3 | マップ縦幅(y=0:空, y=1:地面, y=2:水) |
| SIDE_VIEW_COLS | 9 | 画面表示列数 |
| SIDE_VIEW_ROWS | 3 | 画面表示行数 |

### タイル文字の意味

| 文字 | 絵文字 | 意味 |
|---|---|---|
| `g` | 🟩 | 草原(ランダムエンカウントあり) |
| `f` | 🟫 | 安全地帯(エンカウントなし) |
| `#` | 🌳 | 木・壁(進入不可) |
| `~` | 🟦 | 水(進入不可) |
| `c` | 🎁 | 宝箱(開封後: 📦) |
| `n` | 🧭 | 旅の案内人NPC |
| `m` | 🏪 | 商人(既存 openMerchantModal を流用) |
| `e` | ⚡ | 固定エンカウント(接触で即戦闘) |
| `.` | ☁️ | 空(視覚のみ) |

### ステージ1「はじまりの草原」レイアウト

```
y=0 (空): #..#..#..#...#..##...#..#...#.#..#...#..  (40文字)
y=1 (地): ggggcggnggmgggeggggggfggcggggggegggggggg  (40文字)
y=2 (水): ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  (40文字)
```

イベント配置: x=4(宝箱) x=7(NPC) x=10(商人) x=14(固定戦闘) x=21(安全地帯) x=24(宝箱) x=31(固定戦闘)

### 実装関数

- `renderField()` — mapMode === "side" の場合、--cols/--rows を更新して `renderSideField()` へルーティング
- `renderSideField()` — 横スクロールマップを field-viewport に描画。カメラをプレイヤー中心に合わせる
- `moveSidePlayer(dx, dy)` — プロトタイプでは縦移動(dy)は無効。タイルイベントを判定
- `openSideChest(x, y)` — 宝箱を開封してゴールドを付与(30〜100G)
- `openSideNpcModal()` — 旅の案内人NPCの台詞を既存 `npc-modal` に注入して開く
- `switchToSideMap()` / `switchToNormalMap()` — モード切り替え

### デバッグメニュー (debug=1)

- 「⬇️ 横スクロールマップへ移動」ボタン: `switchToSideMap()` を呼び出す
- 「⬆️ 通常マップへ戻る」ボタン: `switchToNormalMap()` を呼び出す

### セーブ互換

新規フィールド: `mapMode`, `sideMapX`, `sideMapY`, `sideMapStage`, `sideMapChests`
古いセーブには存在しないため `|| デフォルト値` で補完。

### 今回実装しないもの (将来機能)

- 横スクロールマップへの完全移行・既存通常マップの削除
- 横スクロール専用BGM追加・BGM制御の大改修
- 複数ステージ間の移動(ステージクリア→次ステージ)
- 縦移動(y=0 樹上エリアの探索)
- モバイル最適化・フキダシ演出

### 壊してはいけないもの

BGM即時切り替え / stopBGMHard() / bgmSessionId / activeBgmNodes / activeBgmTimers /
ステータスメニュー表示 / safe-area対応 / 通常フィールド移動 / NPC会話 / 商人 /
実家 / 酒場 / 宝箱 / 図鑑 / 装備 / アイテム / ステータス / ヘルプ / エンディング / debug=1開発メニュー


## §44  横スクロールマップ 探索性アップデート (v0.9.1)

### 目的

横スクロールマップを「ただ右に行くだけ」から、少しゲームらしい探索モードにする。

### マップ設計 — ステージ1「はじまりの草原」40×3

| タイル | 記号 | 意味 |
|--------|------|------|
| 草原  | g | ランダムエンカウントあり |
| 安全地帯 | f | エンカウントなし |
| 木・壁 | # | 進入不可 |
| 水    | ~ | 進入不可 |
| 宝箱  | c | アイテム入手(未開封) |
| 案内人NPC | n | 旅の案内人(NPC1) |
| 商人  | m | ショップ |
| 固定敵 | e | 接触で即戦闘(撃破後は消滅) |
| 旅人NPC | p | 旅人NPCで新規追加(NPC2) |
| ゴール | G | ステージクリア(50G報酬、一回のみ) |

#### row 0 (y=0) 高路 — 安全、迂回路A/B を提供
```
#f#ff#ff#fffffff#ffpff#fffffffff#ff#ff#f
```
- x=19: 旅人NPC 'p'（低路の宝について示唆）

#### row 1 (y=1) メインルート — 開始地点
```
ggggcggnggm##ggggfggpgggcgg##ggeggfgggGg
```
- x=4: 宝箱 #1、x=7: NPC1、x=10: 商人
- x=11〜12: 木ブロック＝迂回路Aへの誘導
- x=20: NPC2、x=24: 宝箱 #2
- x=27〜28: 木ブロック＝迂回路Bへの誘導
- x=31: 固定敵 e（撃破後はgになる）
- x=38: ゴール G

#### row 2 (y=2) 低路 — リスク高・報酬多
```
~~~ggcg~~~ggggegg~~~gfgcgggggggc~~g~~~gg
```
- x=5: 宝箱 #3、x=14: 固定敵 e（低路リスク）
- x=23: 宝箱 #4、x=31: 宝箱 #5

### 迂回路

| ルート | 通過点 | 条件 |
|--------|--------|------|
| 迂回路A 高路経由 | y=1,x=10 → y=0,x=11〜13 → y=1,x=13 | y=0 が f タイル |
| 迂回路A 低路経由 | y=1,x=10 → y=2,x=11〜13 → y=1,x=13 | y=2 が g タイル |
| 迂回路B 高路経由 | y=1,x=26 → y=0,x=27〜29 → y=1,x=29 | y=0 が f タイル |
| 迂回路B 低路経由 | y=1,x=26 → y=2,x=27〜29 → y=1,x=29 | y=2 が g タイル |

### 固定敵管理

- `state.sideMap.defeatedEnemies` — { "31,1": true, "14,2": true } 形式で撃破済みを記録
- `finishBattle()` のフックポイントで `sideMapPendingFixedKey` を参照して確定
- 撃破済み 'e' タイルは描画時に 'g' として扱う

### ステージクリア

- ゴール(G)タイルに踏み込むと `openSideGoalModal()` を呼び出す
- `state.sideMap.stageCleared["1"]` が true の場合は50G報酬をスキップ
- モーダルに「通常マップへ戻る」ボタン → `switchToNormalMap()`

### セーブ互換

| キー | 型 | デフォルト |
|------|------|---------|
| sideMapDefeated | object | {} |
| sideMapCleared | object | {} |

### 今回実装しないもの

- BGM変更・複数ステージ・ボスダンジョン・仲間トラッキング・アニメーション

## §45. のりお指令②: 中ボスゴリラ・敵再調整・のりお効果変更 (v0.9.2)

### 目的

1. HP0時の敵逃走表現を全体で統一（既存動作確認）
2. のりおの仲間効果を「逃走成功率+0.15」から「獲得経験値×2」に変更
3. 敵HP・EXPを全体的に底上げして成長感を強化
4. 横スクロールステージ1に「中ボスゴリラ」を追加（固定ボス）
5. 将来の5〜6面＋フロアボス構成をGAME_DESIGN.mdに整理

### HP0 → 敵逃走の統一表現

**[実装済み・確認のみ]**

- winBattle() でHP0になった敵は必ず「逃げる」演出を出す
- バリエーション: 「～は逃げていった！！」「～はあわてて逃げていった！！」など4種
- 中ボスゴリラは `customEscapeMsgs` で専用メッセージを設定:
  - 「はじまりの草原に静けさが戻った。」
  - 「中ボスゴリラは草むらの奥へ消えていった。」

### のりお能力変更

| 項目 | 変更前 | 変更後 |
|------|-------|-------|
| emoji | 💨 | 📈 |
| feature | 逃げるのがうまい | 経験値が2倍になる |
| effectDesc | 逃走成功率+0.15 | 獲得経験値×2 |
| fleeMod | 0.15 | (削除) |
| expMod | (なし) | 2 |
| joinMsgs | 逃げに関するメッセージ | EXP成長に関するメッセージ |

- gainExp(baseExp): EXP取得の共通ヘルパー。のりお同行時は `expMod` を参照して2倍処理
- winBattle / doRun / attemptCapture / doSingUltimateGorilla すべてで gainExp を使用
- 酒場の仲間説明もノリオ欄を更新

### 敵HP・EXP再調整

**[実装済み v0.9.2]**

目的: 「強い敵を倒した時の成長感」を出す。序盤はHPが低くサクサク、後半は手応え感。

| カテゴリ | HP倍率 | EXP倍率 | 対象例 |
|---------|--------|--------|-------|
| 序盤 (minLevel1-2) | ×1.5〜1.6 | ×1.6 | スライム、コウモリ、のらいぬ |
| 中盤 (minLevel3-7) | ×1.7 | ×1.7 | 山賊、パワポ野郎、半グレ |
| 後半 (minLevel5-12) | ×2.0〜2.1 | ×2.0〜2.1 | 鬼、宇宙人、デスマッチレスラー |
| UMA (コモン〜レア) | ×1.2 | ×1.2 | カッパ〜ジャージーデビル |
| メタル系 | 変更なし | 変更なし | メタルゴリラ系 |
| 究極ゴリラ | **変更なし(HP5000固定)** | 変更なし | 仕様上の意図的難易度 |

### 中ボスゴリラ仕様

**[実装済み v0.9.2]**

| 項目 | 値 |
|------|---|
| id | midboss_gorilla |
| name | 中ボスゴリラ |
| emoji | 🦍 |
| type | boss |
| weight | 0 (通常エンカウントプールに入れない) |
| HP | 150 |
| 攻撃力 | 20 |
| 防御力 | 5 |
| EXP | 160 |
| 捕獲率 | 0 (捕獲不可) |
| 逃走率 | 0.30 (逃げにくい) |
| customEscapeMsgs | 専用メッセージ2種 |

**配置**: 横スクロールステージ1 row1 x=36 に 'b' タイルとして配置
- タイル文字 'b': emoji 💢 (ちょうど既存の「ぶつかりおじさん」と異なる。マップ上は 💢)
- SIDE_NO_ENCOUNTER に登録 (踏み込んだ時に triggerFixedEncounter を呼び出す)
- 撃破後は `defeatedEnemies["36,1"]` = true で草原 'g' に変化（e と同じ仕組み）
- **迂回路**: y=0 を通ることで回避可能。y=2 は x=36 が水 '~' のため回避不可

**ステージ1 row1 更新後**:
```
ggggcggnggm##ggggfggpgggcgg##ggeggfggbGg
```
(x=36 が 'b' に変化)

### 横スクロールステージ将来計画（5〜6面構想）

**[未実装・将来機能]**

| ステージ | テーマ | フロアボス | 想定必要レベル |
|---------|--------|-----------|-------------|
| ステージ1 | はじまりの草原 | 中ボスゴリラ (実装済み) | Lv5〜15 |
| ステージ2 | 深い森 | ボスゴリラ | Lv20〜35 |
| ステージ3 | 古い町 | 魔王ゴリラ | Lv40〜55 |
| ステージ4 | 険しい山道 | 大魔王ゴリラ | Lv60〜75 |
| ステージ5 | 魔王城 | ラスボス級ゴリラ | Lv80〜95 |
| ステージ6 | 聖域 | チンパンジー (最終決戦) | Lv99 |

- ステージ間移動: ステージクリアで次ステージが解放される予定
- 各ボスは独自の `customEscapeMsgs` を持つ
- ステージ2以降は今後の実装予定

### デバッグ機能追加

- `btn-debug-midboss-encounter`: 中ボスゴリラ強制エンカウント
- `btn-debug-companion-norio`: ノリオを仲間にする
- `btn-debug-side-reset-midboss`: 中ボスゴリラ撃退フラグをリセット

### 今回実装しないもの

- 横スクロールステージ2〜6の実装
- ボスゴリラ以降の実戦配置
- チンパンジー戦の実装
- 究極ゴリラの仕様変更（HP5000は固定）
- 新BGM追加・BGM制御の大改修

## §46. 中ボスゴリラ捕獲不可・実機確認補強 (v0.9.2.1)

### 目的

v0.9.2 の安全補強。新機能追加ではなく既存実装の正確性を高める。

### ボス系モンスター — 捕獲不可設計

**[実装済み v0.9.2.1]**

- `canCapture: false` フィールドを敵データに追加することで、捕獲不可を明示する
- `attemptCapture()` の冒頭で `canCapture === false` を確認し、判定をスキップ
- 捕獲不可メッセージを2行表示して敵ターンへ移行する（攻撃を受ける）
- `clamp(0.05, 0.95)` の下限により `captureRate:0` でも5%捕獲チャンスが残る問題を解消
- 現在の対象: 中ボスゴリラ (`canCapture: false`)
- 将来のボス敵も同様に `canCapture: false` を設定することで捕獲不可になる
- UMA・通常モンスター・究極ゴリラの捕獲ロジックには影響しない

#### 捕獲不可メッセージ例

```
中ボスゴリラはUMAではない！
捕まえる相手ではなく、道をふさぐ強敵だ！
```

### のりおEXP2倍 — ログ表示改善

**[実装済み v0.9.2.1]**

- `gainExp()` のログ表示を改善し、「EXP2倍効果あり」と「最終EXP量」が分かるようにする
- 表示例: "ノリオ効果！ EXP 2倍！" → "✨ 経験値 160 を獲得！" (元80 × 2)
- state.player.exp への加算・レベルアップ判定は正常動作確認済み

### 中ボス撃退後のNPCセリフ変化

**[実装済み v0.9.2.1]**

- `defeatedEnemies["36,1"]` フラグを参照してNPCセリフを変化させる
- 横スクロール案内人: 撃退後に専用セリフ表示
- 横スクロール旅人NPC: 撃退後に専用セリフ表示  
- ゴリラ研究家(通常マップ): 中ボスゴリラに関するヒント追加

### ヘルプ画面追記

**[実装済み v0.9.2.1]**

- 「ボス系モンスター」セクションを追加: UMAではない/捕獲不可/HP0で逃げる
- 「🍺 仲間システム」のノリオ説明を「経験値2倍」に更新

### 今回実装しないもの

- 横スクロールステージ2以降
- チンパンジー戦
- 新BGM・BGM大改修
- 究極ゴリラ仕様変更

## §47. ステージ1クリア体験強化 (v0.9.3)

### 目的

横スクロールステージ1「はじまりの草原」のゴール到達時に達成感のある演出を追加し、
中ボス撃退の有無でゴール演出・報酬を分岐させる。

### ゴール到達演出（openSideGoalModal 改修）

**[実装済み v0.9.3]**

#### 中ボス撃退済みで初回ゴール（最高報酬）

```
🏁 はじまりの草原を制覇した！

中ボスゴリラを退かせ、草原の道を切り開いた。
はじまりの草原に、少しだけ平和が戻った。

報酬：100G ＋ パン ×1
```

#### 中ボス未撃退で初回ゴール（部分報酬）

```
🏁 はじまりの草原を抜けた！

君は中ボスゴリラを避けながら、草原の出口へたどり着いた。
逃げること、避けること、進むこと。
それもまた勇者の判断だ。

報酬：30G
```

#### 中ボス未撃退クリア後→中ボス撃退→再ゴール（差分報酬）

```
🏁 草原の真の制覇者よ！

中ボスゴリラも退かせたか！
草原の覇者として認められた。追加の報酬を受け取れ。

追加報酬：70G ＋ パン ×1
```

#### 報酬受取済み（2回目以降）

```
🏁 はじまりの草原
（報酬は受け取り済み）
```

全報酬取得後は「あやしい森」ステージ2の予告を表示する。

### 報酬二重受け取り防止

`state.sideMap.stage1RewardLevel`（整数）で管理：
- `0`：未クリア（報酬なし）
- `1`：中ボス未撃退でクリア（30G受取済み）
- `2`：全報酬受取済み（100G or 30G+70G + パン）

### ゴールモーダルボタン

- 初回到達時：「通常マップへ戻る」「探索を続ける」
- クリア済み再訪：「通常マップへ戻る」「もう一度草原を探索する」

### ステータス画面への横スクロール進捗表示

`renderStatusBody()` に「横スクロール進捗」セクションを追加：

```
横スクロール進捗：
はじまりの草原  ✅ クリア済み / 未クリア
中ボスゴリラ    ✅ 撃退済み   / 未撃退
横スクロール称号：中ボスゴリラを退かせし者 / 草原を越えし者
```

### NPC セリフ変化（v0.9.3追加分）

#### 横スクロールマップ内 NPC（openSideNpcModal）

旅の案内人(n) と 旅人(p) に、「ステージ1クリア済み + 中ボス撃退済み」の組み合わせでセリフ分岐を追加。
クリア済み状態に応じて4パターン切り替え。

#### 通常マップ 旅人(R) — NPC_DATA R

ステージ1クリア後、末尾に「草原を越えたらしいな。次は『あやしい森』が待っているという噂だ。」を追加。

### ステージ2予告テキスト

全報酬取得後のゴールモーダルに以下を表示：
```
この先には「あやしい森」が広がっているらしい。
中ボスゴリラよりも手ごわい「ボスゴリラ」が待つという噂がある。
```

ステージ2本体（あやしい森）は今回実装しない。予告のみ。

### ヘルプ追記

「🏁 横スクロールマップのゴールとクリア」セクションを追加：
- ゴール到達でステージクリアになること
- ボスを避けても進めるが報酬が違うこと

### デバッグメニュー追加（?debug=1）

| ボタン | 機能 |
|---|---|
| ✅ ステージ1クリアフラグON | `stageCleared["1"] = true` |
| ✅ 中ボス撃退済みにする | `defeatedEnemies["36,1"] = true` |
| 🔄 横スクロール: クリア・撃破フラグをリセット | 既存ボタンに `stage1RewardLevel = 0` も追加 |

### セーブデータ

| キー | 型 | 説明 |
|---|---|---|
| `sideMapStage1Reward` | number(0/1/2) | `state.sideMap.stage1RewardLevel` を保存 |

古いセーブは `data.sideMapStage1Reward || 0` でデフォルト0として補完。
ニューゲームではlocalStorageを削除するため、自動的に0にリセットされる。

### 今回実装しないもの

- 横スクロールステージ2本体（あやしい森）
- ボスゴリラ実装
- チンパンジー実装
- 新BGM追加・BGM制御の大改修
- 仲間フィールド追従
- 究極ゴリラ仕様変更
- 敵HP/EXPの大幅再調整


---

## §48 横スクロールステージ2「あやしい森」 (v0.10)

### ステージ2マップ仕様

| 項目 | 値 |
|---|---|
| 名称 | あやしい森 |
| 幅×高さ | 40×3 (SIDE_MAP_WIDTH/HEIGHT と同一) |
| 開始位置 | x=1, y=1 |
| ゴール位置 | x=38, y=1 |
| ボスゴリラ | x=35, y=1 (タイル: 'b') |
| NPC-A | x=20, y=0 (高路の森の住人) |
| NPC-B | x=4, y=1 (メイン路の旅人) |
| 宝箱 | x=17,y=0 / x=4,y=2 / x=26,y=2 |
| 固定敵 | x=14,y=1 / x=12,y=2 / x=32,y=2 |

#### マップデータ (40文字×3行)

```
row0: #ff#ff#fffffff#ffcffpfff#fff#fff#fff#ff#
row1: ggggpgg#gggfggegg##gggg##ggggggggggbggGg
row2: ~~ggcgg~~~ggegg~gggg~~ggggcgg~~gegg~~gg~
```

- **row0 (y=0) 高路**: 安全な迂回路。x=17に宝箱、x=20にNPC-A。
- **row1 (y=1) メイン**: x=4 NPC-B、x=7 ブロックA、x=14 固定敵1、x=17-18 ブロックB、x=23-24 ブロックC、x=35 ボスゴリラ(b)、x=38 ゴール(G)
- **row2 (y=2) 下路**: 水(~)で一部通行止め。x=4 宝箱、x=12 固定敵2、x=26 宝箱、x=32 固定敵3

### ボスゴリラ仕様

| 項目 | 値 |
|---|---|
| id | `boss_gorilla` |
| 名前 | ボスゴリラ |
| emoji | 🦍 |
| type | "boss" |
| HP | 250 |
| ATK | 26 |
| DEF | 8 |
| EXP | 290 |
| captureRate | 0 |
| canCapture | false |
| fleeRate | 0.20 |
| 通常エンカウント | なし (weight: 0) |

### ステージ切り替え

- ステージ1ゴールモーダルに「🌲 あやしい森へ進む」ボタンを追加（stage1クリア時に表示）
- ボタン押下時: `state.sideMap.stage = 2`, x/y をステージ2スタート位置にリセット、saveGame, renderField

### ゴール演出・報酬分岐 (stage2RewardLevel)

| 状態 | 報酬 | newRewardLevel |
|---|---|---|
| 初回クリア + ボス撃退 | 150G + 🍱お弁当×1 | 2 |
| 初回クリア + ボス未撃退 | 50G | 1 |
| rewardLevel=1 でボス撃退後に再ゴール | 追加100G + 🍱お弁当×1 | 2 |
| rewardLevel=2以上 (受取済み) | なし | 変更なし |

### イベントキーの名前空間

- ステージ1: `"x,y"` (変更なし)
- ステージ2: `"2:x,y"` (`getSideKey(nx, ny)` ヘルパーで自動選択)
- ボスゴリラ撃破確認キー: `"2:35,1"`

### NPC セリフ分岐 (stage2)

`openStage2NpcModal(nx, ny)` でボスゴリラ撃退済みフラグにより分岐。
- x=4,y=1 (旅人): ボス撃退前「右の奥に大きなゴリラがいるって噂だよ」/ 後「ボスゴリラが静かになった！ありがとう！」
- その他 (森の住人): ボス撃退前「上路は安全/下路に宝箱がある」/ 後「平和になったね」

### ステータス画面追加

`renderStatusBody()` にステージ2クリア状況・ボスゴリラ撃退状況・称号「森の制覇者」を表示。

### セーブデータ追加

| キー | 型 | 説明 |
|---|---|---|
| `sideMapStage2Reward` | number(0/1/2) | `state.sideMap.stage2RewardLevel` |

### v0.9.1互換補正 (loadGame)

`stageCleared["1"] === true && sideMapStage1Reward未定義(0)` の場合、
`stage1RewardLevel` を `defeatedEnemies["36,1"]` に応じて 1 or 2 に補正。

### デバッグボタン追加 (debug=1)

| ボタンID | 機能 |
|---|---|
| `btn-debug-side-stage2-enter` | stage=2 に切り替え、スタート地点へ |
| `btn-debug-side-stage2-near-goal` | stage=2 の x=34,y=1 へ移動 |
| `btn-debug-side-stage2-clear-reset` | ステージ2フラグ (stageCleared["2"], defeatedEnemies["2:35,1"], stage2RewardLevel) をリセット |
| `btn-debug-side-set-bossgori` | `defeatedEnemies["2:35,1"] = true` |
| `btn-debug-boss-gorilla-encounter` | ボスゴリラ強制エンカウント |

### ヘルプ追加

「🌲 ステージ2「あやしい森」」セクションを追加。

### 今回実装しないもの

- ステージ3以降
- 魔王ゴリラ・大魔王ゴリラ・ラスボス級ゴリラ
- チンパンジー
- 横スクロール専用BGM・BGM制御の大改修
- 仲間フィールド追従
- 究極ゴリラ仕様変更
- 通常マップの削除


---

## §49 攻略ペーパービュー屋ヒント拡張・ステージ別固定敵改善 (v0.10.1)

### 攻略ペーパービュー屋ヒント — 横スクロールステージ対応

#### ヒント優先度 (`getHintPriority()` 拡張)

| 優先度 | 状態 | 説明 |
|---|---|---|
| 0 | gameCleared | 通常マップクリア済み (既存) |
| 9 | s1Cleared && s2Cleared | 両ステージクリア済み → 先の予告 |
| 10 | s1Cleared && !s2Cleared | ステージ2ガイド |
| 11 | !s1Cleared && sideVisited | ステージ1ガイド (サイドマップに入った記録あり) |
| 1-8 | (既存) | 通常マップ進行 |

`sideVisited` = `sideMap.openedChests` or `sideMap.defeatedEnemies` にエントリがある場合

#### 横スクロールヒント内容

**Priority 11: ステージ1ガイド**

| tier | ヒント内容 (中ボス未撃退) | ヒント内容 (中ボス撃退済み) |
|---|---|---|
| 1 | 横に長い草原では、まっすぐ右へ進むだけが正解とは限らない。 | (同左) |
| 2 | 草原には中ボスゴリラが道をふさいでいる場所がある。上の道から迂回できる。 | 中ボスゴリラを退かせた！ゴールへの道は開けている。 |
| 3 | 中ボスゴリラはゴール手前x=36にいる。撃退→ゴールで報酬増加。上ルートで先にゴールも可。 | 撃退済み！ゴール(x=38)へ進もう。100G+パンの報酬がある。 |

**Priority 10: ステージ2ガイド**

| tier | ヒント内容 (ボス未撃退) | ヒント内容 (ボス撃退済み) |
|---|---|---|
| 1 | あやしい森では、木や水で道がふさがれている。上下の道をよく見れば、進める道がある。 | (同左) |
| 2 | あやしい森の奥にはボスゴリラがいる。UMAではないので捕獲できない。装備を整えてから挑もう。 | ボスゴリラを退かせた！ゴールへの道は開けている。 |
| 3 | ボスゴリラはゴール手前x=35にいる。撃退→ゴールで150G+お弁当。下路に宝箱3個あり。 | 撃退済み！ゴール(x=38)へ進もう。下路に宝箱も残っているかもしれない。 |

**Priority 9: 両ステージクリア済み**

| tier | ヒント内容 |
|---|---|
| 1 | 今行ける横スクロールステージはかなり進んでいる。次に待つのは、さらに危険な道だ。 |
| 2 | はじまりの草原とあやしい森を越えた者は、もう初心者ではない。次のアップデートでさらに強いゴリラが待つかもしれない。 |
| 3 | ステージ1と2を制覇した。次のステージは未実装だが、通常マップにはまだ究極ゴリラが待っている。 |

#### ヒント結果表示の改善

`renderHintResult()` を更新: 購入タイトルを「📄 ぼんやりヒントを購入した！(10G)」形式に変更。
ヒント本文から `「」` 括弧を除去し、読みやすくする。

### 横スクロール固定敵ステージ別管理 (`SIDE_FIXED_ENCOUNTERS`)

```javascript
var SIDE_FIXED_ENCOUNTERS = {
  "31,1":   "wilddog",       // stage1 メイン路 x=31,y=1: のらいぬ
  "14,2":   "bumpman",       // stage1 下路 x=14,y=2: ぶつかりおじさん
  "2:14,1": "wannabeninja",  // stage2 メイン路 x=14,y=1: 忍者かぶれ
  "2:12,2": "bandit",        // stage2 下路 x=12,y=2: 山賊
  "2:32,2": "oni"            // stage2 下路深部 x=32,y=2: 鬼
};
```

`moveSidePlayer()` の 'e' タイル処理で、`SIDE_FIXED_ENCOUNTERS[key]` を確認し、
エントリがあれば `triggerFixedEncounter(id)` でその敵を起動。なければ `triggerEncounter()` でランダム。

撃退フラグは `getSideKey()` により既にステージ別管理済みのため追加変更不要。

### デバッグ追加 (debug=1)

- `btn-debug-open-hint-shop`: 「📰 ヒントショップを開く」ボタン追加

### ヘルプ追記

- 攻略ペーパービュー屋の説明セクション追加
- 横スクロール固定敵の説明を更新 (敵の種類についての補足)

---

## §50 横スクロールステージ3「古びた町はずれ」 (v0.11)

### 概要

ステージ2「あやしい森」クリア後に進める横スクロールステージ3。
町の外れという不気味な雰囲気で、ステージ2より障害物が多く難易度が上がる。
ボスは「魔王ゴリラ」(UMAではなく捕獲不可)。

### マップ仕様

- サイズ: 40×5 (5行で縦の迂回路が増加)
- タイル:  `g`(草), `f`(安全路), `#`(壁・通行不可), `~`(水・通行不可), `c`(宝箱), `n`(NPC-老人), `p`(NPC-旅人), `m`(商人), `e`(固定敵), `b`(魔王ゴリラ), `G`(ゴール)
- startX: 1, startY: 2, goalX: 38

```
row0(y=0) 高路:    fffff##fffffff#fffcfff###fffff##fffcffff
row1(y=1) 上中路:  gggfgg##ggpggff##gggggg##ggggggggg###gfg
row2(y=2) 中央路:  gggmgngggg##gggeggg##gggggg##ggbgg##ggGg
row3(y=3) 下中路:  ~~gggg##ggggegg##gggggg##ggggg##ggggg~~~
row4(y=4) 下路:    ~~~~cggg##ggggg##ggcgg##gggegg##ggggg~~~
```

タイル位置:
- 商人(m): x=3, y=2
- NPC-老人(n): x=5, y=2 (撃退前後でセリフ分岐)
- NPC-怪しい旅人(p): x=10, y=1 (撃退前後でセリフ分岐)
- 固定敵(e): x=15,y=2 / x=12,y=3 / x=27,y=4
- 魔王ゴリラ(b): x=31, y=2 (bossKey="3:31,2")
- ゴール(G): x=38, y=2
- 宝箱(c): x=18,y=0 / x=35,y=0 / x=4,y=4 / x=19,y=4

### 魔王ゴリラ

```javascript
{
  id: "maou_gorilla",
  name: "魔王ゴリラ",
  emoji: "🦍",
  type: "boss",
  isUMA: false,
  hp: 400,
  attack: 34,
  def: 11,
  captureRate: 0,
  exp: 500,
  fleeRate: 0.15,
  canCapture: false,
  startMsg: "町はずれの奥から、重たい笑い声が響いた……\n魔王ゴリラが道をふさいだ！",
  customEscapeMsgs: [
    "魔王ゴリラは古びた町の奥へ逃げていった！！",
    "町はずれに、少しだけ静けさが戻った。"
  ]
}
```

### SIDE_FIXED_ENCOUNTERS (追加分)

```javascript
"3:15,2": "powerharassmentsenpai",  // ステージ3 中央路
"3:12,3": "wanderingman",           // ステージ3 下中路
"3:27,4": "deathmatch"              // ステージ3 下路
```

### ステージ3ゴール演出

報酬管理: `state.sideMap.stage3RewardLevel` (0/1/2)

| rewardLevel | 魔王ゴリラ撃退 | 報酬 |
|---|---|---|
| 0 → 2 | 済み | 220G + ラーメン×1 |
| 0 → 1 | 未撃退 | 80G |
| 1 → 2 | 済み (後追加) | 140G + ラーメン×1 |
| 2 | 受取済み | 表示のみ |

### セーブキー追加

- `sideMapStage3Reward`: state.sideMap.stage3RewardLevel

### ヒント優先度拡張 (§50 v0.11)

- 旧priority 9 (s1&s2クリア) → s3クリアで priority 9 (全クリア)
- 新priority 12: s2クリア・s3未クリア → ステージ3ヒント (魔王ゴリラ撃退フラグで分岐)

### ステータス称号 (横スクロール)

| 条件 | 称号 |
|---|---|
| s3クリア + 魔王撃退 | 町はずれの覇者 |
| s3クリア | 町はずれを越えし者 |
| s2クリア + ボス撃退 | 森の制覇者 |
| s1クリア + 中ボス撃退 | 中ボスゴリラを退かせし者 |
| s1クリア | 草原を越えし者 |

### 動的マップサイズ対応

`SIDE_MAP_HEIGHT` / `SIDE_VIEW_ROWS` をハードコードから、ステージの `rows.length` で動的計算に変更。
`SIDE_MAP_WIDTH` も `rows[0].length` を使用。renderField の `--rows` CSS変数も動的設定。

### 今後のステージ4「ゴリラ山道」構想

- 険しい山道をテーマにした次ステージ (未実装)
- 大魔王ゴリラ (魔王ゴリラより強い) をボスとする予定
- ステージ3クリア後に進める
- 本仕様はv0.11以降で検討

---

## §51 横スクロールステージ4「ゴリラ山道」構想 (v0.12 以降・未実装)

### 概要

- ステージ名: 「ゴリラ山道」
- ステージ3クリア後にアクセス可能
- テーマ: 険しい山道・岩場・渓流
- 推奨レベル: Lv60〜75

### ボス: 大魔王ゴリラ (未実装)

| 項目 | 値 |
|---|---|
| ID | `daimaou_gorilla`（予定） |
| 名前 | 大魔王ゴリラ |
| HP | 600（予定） |
| 攻撃力 | 45（予定） |
| 防御力 | 15（予定） |
| 捕獲 | 不可（canCapture: false） |
| 経験値 | 800（予定） |

### ステージ4予告テキスト（v0.11.1 実装）

ステージ3ゴール演出（全報酬受取後）に以下の予告テキストを追加：

```
次に見えてきたのは、険しいゴリラ山道だった。
そこには、魔王ゴリラよりさらに重い気配を放つ
大魔王ゴリラが待つという噂がある。
```

### 安定化項目 (v0.11.1)

- `triggerFixedEncounter()`: 未定義IDへのフォールバック追加 (`console.warn` + `triggerEncounter()`)
- `validateSideFixedEncounters()`: debug=1 専用の固定敵ID整合性チェック関数
- デバッグボタン追加: 🧪 固定敵IDチェック / 🏚️ ステージ3宝箱・固定敵リセット
- CSS確認: `aspect-ratio: var(--cols)/var(--rows)` が5行ステージで正しく機能することを確認（変更不要）

---

## §52 横スクロールマップ入口ゲート (v0.11.2)

### 目的

横スクロールマップへの行き方が分かりにくい問題を解消する。
通常マップ上に🌀ゲートタイルを設置し、踏むとモーダルで説明を受けてから横スクロールに移動できる。

### ゲートタイル仕様

| 項目 | 値 |
|---|---|
| タイル文字 | `'V'` |
| 絵文字 | 🌀 |
| 配置座標 | 通常マップ (x=2, y=3) |
| 安全地形 | true（エンカウントなし） |
| 進入可否 | 進入可能（踏むとモーダル表示） |

配置場所: 行3 `"#,V,N,,,,G,,#"` — 村エリア内、攻略ペーパービュー屋(4,3)の左隣。
プレイヤー開始位置(4,1)から最初の視野(列1〜7)に含まれるため、序盤から視認可能。

### gateExplained フラグ

- `state.sideMap.gateExplained: false` — 初期値
- 初めてゲートから横スクロールへ入った時に `true` にする
- saveGame/loadGame 両方に追加 (`sideMapGateExplained`)
- newGame時は state 初期値の `false` でリセット

### ゲートモーダル (modal-side-gate)

**初回 (gateExplained === false)**:
```
🌀 横スクロールマップへの入口

ここは「はじまりの草原」へ続く不思議な渦だ。
横スクロールマップでは草原・森・町はずれの3ステージを冒険できる。
各ステージをクリアすると報酬がもらえるぞ。
通常マップへ戻る時はゴール地点の「🏠 通常マップへ戻る」を使おう。
```

**2回目以降 (gateExplained === true)**:
```
🌀 横スクロールマップへ進みますか？
```

ボタン: 「🌀 進む」→ gateExplained=true にしてから switchToSideMap() 呼び出し
        「🏠 やめる」→ モーダルを閉じる

### NPC ゲート案内テキスト

- **UMA博士 (D)**: 横スクロール未訪問時に「村の南の🌀ゲートから草原へ行ける」旨を追加
- **旅人 (R)**: ステージ1未クリア時に「🌀ゲートから草原へ行けるらしい」旨を追加
- **王様の使い (S)**: 低レベル時に「🌀渦のゲートで草原へ」旨を追加

### UMA捕獲ヒント

UMA博士の台詞にUMA捕獲ヒントを追加（図鑑捕獲数が少ない時）:
「UMAはHPが0になると逃げてしまう。少し弱らせてから捕まえるのじゃ！」

### ヒント屋 優先度 13 追加

横スクロール未訪問かつLv40未満の時に優先度13を返す。
内容: 村の🌀ゲートから横スクロールマップへ行けることを案内。

### デバッグボタン (§52 v0.11.2)

- 🌀 ゲートタイル付近へ移動 (x=2, y=3)
- 🔄 ゲート説明フラグリセット (gateExplained = false)

### 将来の捕獲サポート技（未実装・設計メモ）

以下は将来バージョンでの実装候補。今回(v0.11.2)は実装しない。

| 技名 | 効果（案） |
|---|---|
| はずかし固め | 相手を1〜2ターン行動不能にする（捕獲補助） |
| キドクラッチ | 低固定ダメージ＋捕獲成功率アップ |
| カリツォー | 低固定ダメージ技（対UMA特効） |
| グーパンチ | 確実に低ダメージを与える（HPを削りすぎない） |
| ここはひとつガマン | 1ターン防御（HPを温存しながら捕獲チャンスを作る） |

これらは「HPをぎりぎりまで削る」捕獲難易度を下げるための補助技。
実装時はバランス調整が必要（究極ゴリラ捕獲条件との整合性に注意）。

---

## §53 横スクロール帰還導線 (v0.11.3)

### 目的

横スクロールマップへは入れるが出られない問題を修正する。
ゴールモーダルの通常マップ戻りボタンの動作確認と、ステージ途中でも帰還できる「帰還ゲート」タイルを追加。

### switchToNormalMap() の戻り位置修正

`switchToNormalMap()` 呼び出し時、プレイヤーを通常マップの安全な位置へ戻す。

| 項目 | 値 |
|---|---|
| 戻り先 X | 2 |
| 戻り先 Y | 4 |
| 理由 | 🌀ゲート(2,3)の1マス下。再接触ループを防ぐ |

### 帰還ゲートタイル 'H'

横スクロール各ステージのスタート付近に帰還ゲートを配置する。

| 項目 | 値 |
|---|---|
| タイル文字 | `'H'` |
| 絵文字 | 🏠 |
| SIDE_NO_ENCOUNTER | true（エンカウントなし） |
| 配置 | ステージ1 (x=0,y=1) / ステージ2 (x=0,y=1) / ステージ3 (x=0,y=2) |
| 効果 | 踏むと確認モーダル → 「通常マップへ戻る」or「やめる」 |

### ゴールモーダルのボタン整理

各ステージのゴール後に出るボタンを以下のように整理する。

**ステージ1ゴール (`openSideGoalModal`)**:
- 🌲 あやしい森へ進む（クリア後表示）
- 🏠 通常マップへ戻る（常時表示）
- ↩ この草原に残る（常時表示）

**ステージ2ゴール (`openStage2GoalModal`)**:
- 🏚️ 古びた町はずれへ進む（クリア後表示）
- 🏠 通常マップへ戻る（常時表示）
- ↩ この森に残る（常時表示）

**ステージ3ゴール (`openStage3GoalModal`)**:
- 🏠 通常マップへ戻る（常時表示）
- ↩ この町はずれに残る（常時表示）
- ステージ4はボタンなし（ゴールモーダル本文に予告テキストのみ）

### 帰還ゲート確認モーダル (modal-side-return-gate)

「🏠 通常マップへ戻りますか？横スクロールの進捗は保存されます。」

ボタン:
- 「🏠 通常マップへ戻る」→ `switchToNormalMap()` 実行
- 「↩ やめる」→ モーダルを閉じる

### セーブ/ロードへの影響

帰還ゲートは `switchToNormalMap()` を呼ぶだけ。既存のセーブデータ（宝箱・固定敵・報酬・ステージ進捗）は一切変更しない。

### デバッグボタン (§53 v0.11.3)

- 🏠 ステージ1帰還ゲート付近へ移動 (stage=1, x=0, y=1)
- 🏠 ステージ2帰還ゲート付近へ移動 (stage=2, x=0, y=1)
- 🏠 ステージ3帰還ゲート付近へ移動 (stage=3, x=0, y=2)
- 🏠 通常マップへ強制帰還

---

## §54 ゴールモーダルJS生成ボタン方式 + 帰還ゲート位置修正 (v0.11.3.2)

### 目的

v0.11.3 で追加した帰還導線が実機で動作しない問題を根本修正する。
原因: `hidden` クラスの付け外しに依存した静的ボタン方式が不安定だった。
帰還ゲート H が x=0 に置かれ、プレイヤーが自然に発見できなかった。

### ゴールモーダルのボタン生成方式変更

`modal-side-goal` HTML から静的ボタンをすべて削除し、
各ゴール関数内で `document.createElement` によりボタンを生成して `modal-side-goal-body` に追加する。

**ステージ1ゴール (`openSideGoalModal`)**:
1. 🌲 あやしい森へ進む
2. 🏠 通常マップへ戻る → `returnToNormalMapFromSide()`
3. ↩ この草原に残る

**ステージ2ゴール (`openStage2GoalModal`)**:
1. 🏚️ 古びた町はずれへ進む
2. 🏠 通常マップへ戻る → `returnToNormalMapFromSide()`
3. ↩ この森に残る

**ステージ3ゴール (`openStage3GoalModal`)**:
1. 🏠 通常マップへ戻る → `returnToNormalMapFromSide()`
2. ↩ この町はずれに残る
（ステージ4ボタンなし）

### `returnToNormalMapFromSide()` 共通関数

```javascript
function returnToNormalMapFromSide() {
  closeModal("modal-side-goal");
  closeModal("modal-side-return-gate");
  switchToNormalMap();
}
```

### 帰還ゲート H の位置変更

プレイヤーが自然に発見できる位置（スタート x=1 のすぐ右 x=2）へ移動する。

| ステージ | 変更前 | 変更後 |
|---|---|---|
| ステージ1 | x=0, y=1 | x=2, y=1 |
| ステージ2 | x=0, y=1 | x=2, y=1 |
| ステージ3 | x=0, y=2 | x=2, y=2 |

### デバッグボタン (§54 v0.11.3.2)

帰還ゲート移動ボタンを x=2 に更新。以下のモーダル直接表示ボタンを追加:
- 🧪 ステージ1ゴールモーダル表示
- 🧪 ステージ2ゴールモーダル表示
- 🧪 ステージ3ゴールモーダル表示
- 🧪 帰還ゲートモーダル表示

---

## §55 — 横スクロールステージ4「ゴリラ山道」(v0.12)

### ステージ概要

険しい岩場と崖が続く横スクロールステージ。ステージ3「古びた町はずれ」のゴールモーダルから進める。5路構成(40×5)、固定敵3体、大魔王ゴリラ(ボス)。

### マップ設計 (40×5)

```
row0(y=0) 高路:  安全な岩場。宝箱2個(x=8, x=28)。##で迂回路あり。
row1(y=1) 上中:  旅人NPC(p, x=12)。固定敵(e, x=31:校長)。
row2(y=2) 中央:  x=2帰還ゲート(H), 老人NPC(n, x=5), ##@x=8-9,
                  固定敵(e, x=15:空手姉妹), 商人(m, x=20), ##@x=25-26,
                  大魔王ゴリラ(b, x=33), ゴール(G, x=38)
row3(y=3) 下中:  固定敵(e, x=25:デスマッチレスラー)。宝箱(c, x=22)。
row4(y=4) 下路:  宝箱(c, x=4)。危険な水路あり。
```

```
rows[0]: "ffffffffcf##ffffffff##ffffffcff##fffffff"
rows[1]: "ggggggggggggpggg##ggggggggggggeggggggfg"
rows[2]: "ggHggngg##gggggeggggmgggg##ggggggbggggGg"
rows[3]: "~~gggg##gggggggg##ggggcggegggg##gggggg~~"
rows[4]: "~~~~cggg##ggggggggggggg##gggggggggggg~~~"
startX: 1, startY: 2, goalX: 38
```

タイル凡例: g=通路🟩, f=岩場🟫, #=木🌳(通行不可), ~=水🟦(通行不可), c=宝箱🎁, n=NPC🧭, m=商人🏪, p=旅人🧑, e=固定敵⚡, b=ボス💢, G=ゴール🏁, H=帰還ゲート🏠

### 大魔王ゴリラ (NON_UMA_DATA追加)

```
id: "daimaou_gorilla", name: "大魔王ゴリラ", emoji: "🦍", type: "boss"
hp: 700, attack: 46, def: 16, exp: 850, fleeRate: 0.10
canCapture: false (捕獲不可)
位置: row2 x=33 (b タイル)、キー: "4:33,2"
```

### 固定敵 (SIDE_FIXED_ENCOUNTERS追加)

| キー | 敵ID | 場所 |
|---|---|---|
| `"4:15,2"` | `karatesisters` (空手姉妹) | 中央路 x=15 |
| `"4:31,1"` | `principal` (校長) | 上中路 x=31 |
| `"4:25,3"` | `deathmatch` (デスマッチレスラー) | 下中路 x=25 |

### NPC

- **n タイル (x=5, y=2)**: 「山を知る老人」。大魔王ゴリラ撃退前後でセリフ分岐。
- **p タイル (x=12, y=1)**: 「逃げ腰の旅人」。撃退前後でセリフ分岐。

### 宝箱報酬 (openSideChest stage4分岐)

ステージ4の宝箱は stage3 より高い報酬テーブルを使用:
- 40%: 40〜100G (step10)
- 20%: デオドラントスプレー
- 20%: お弁当
- 20%: やくそう (フォールバック)

### ゴール演出 (openStage4GoalModal)

大魔王ゴリラ撃退キー: `"4:33,2"`

| 条件 | 報酬 | newRewardLevel |
|---|---|---|
| rewardLevel=0 + 撃退済み | 350G + ラーメン×1 | 2 |
| rewardLevel=0 + 未撃退 | 120G | 1 |
| rewardLevel=1 + 撃退済み | 230G + ラーメン×1 (追加) | 2 |
| それ以外 | 報酬なし | 変更なし |

ゴール後ボタン（ステージ5なし）:
1. 🏠 通常マップへ戻る → `returnToNormalMapFromSide()`
2. ↩ この山道に残る

### ステージ3 → ステージ4 遷移

`openStage3GoalModal()` のボタン先頭に追加:
- ⛰️ ゴリラ山道へ進む → stage=4, startX/Y へ移動

### セーブデータ追加

```javascript
// state.sideMap
stage4RewardLevel: 0   // 0=未, 1=120G, 2=全取得済み

// saveGame()
sideMapStage4Reward: state.sideMap.stage4RewardLevel || 0

// loadGame()
state.sideMap.stage4RewardLevel = data.sideMapStage4Reward || 0;
```

### ステータス画面追加 (renderStatusBody)

横スクロール進捗に追加:
- ゴリラ山道: クリア済み / 未クリア
- 大魔王ゴリラ: 撃退済み / 未撃退
- 称号:「山道を越えし者」(s4Cleared)、「ゴリラ山道の覇者」(s4Cleared && daimaouDefeated)

### 攻略ペーパービュー屋 (getHintPriority / getProgressHint)

- s4Cleared → priority 9 (全4ステージクリア済み)
- s3Cleared && !s4Cleared → priority 14 (ゴリラ山道ガイド、新規)
- priority 9 テキスト: 4ステージすべて制覇済みメッセージに更新

### 将来の改善候補（今回実装しない）

- ステージ5以降: v0.13以降で検討。


## §56 — ゴール側帰還ゲート追加 (v0.12.1)

### 目的

各横スクロールステージのゴール直前 (x=37) にも🏠帰還ゲートを追加し、ボス撃破後にいつでも通常マップへ戻れる導線を作る。スタート付近のゲート(x=2)は維持する。

### 設計方針

- **スタート側 H** (x=2): ステージ序盤・準備不足の場合に早期離脱できる。
- **ゴール側 H** (x=38): ゴール通過後にそのまま右へ進むと帰還ゲートで通常マップへ戻れる。(§58 v0.13.1 で x=37→x=38 に変更)
- **タイル 'H'** の挙動は既存と同一: `moveSidePlayer` 内の `'H'` 処理→`openSideReturnGateModal()` を呼ぶ。

### 各ステージのHゲート位置

※ v0.13.1(§58)にてゴール側H/G位置を入れ替え: ゴール(G)@x=37、帰還ゲート(H)@x=38

| ステージ | スタート側H | ゴール(G) | ゴール側H | ボス位置 |
|---|---|---|---|---|
| 1 はじまりの草原 | (x=2, y=1) | G@x=37 | (x=38, y=1) | b@x=36 |
| 2 あやしい森     | (x=2, y=1) | G@x=37 | (x=38, y=1) | b@x=35 |
| 3 古びた町はずれ | (x=2, y=2) | G@x=37 | (x=38, y=2) | b@x=31 |
| 4 ゴリラ山道     | (x=2, y=2) | G@x=37 | (x=38, y=2) | b@x=33 |
| 5 黒い城         | (x=2, y=2) | G@x=37 | (x=38, y=2) | b@x=33 |

### マップ文字列変更 (x=37 を 'g' → 'H' に置換)

```
Stage 1 row1(y=1):
  旧: "ggHgcggnggm##ggggfggpgggcgg##ggeggfgbgGg"
  新: "ggHgcggnggm##ggggfggpgggcgg##ggeggfgbHGg"

Stage 2 row1(y=1):
  旧: "ggHgpgg#gggfggegg##gggg##ggggggggggbggGg"
  新: "ggHgpgg#gggfggegg##gggg##ggggggggggbgHGg"

Stage 3 row2(y=2):
  旧: "ggHmgngggg##gggeggg##gggggg##ggbgg##ggGg"
  新: "ggHmgngggg##gggeggg##gggggg##ggbgg##gHGg"

Stage 4 row2(y=2):
  旧: "ggHggngg##gggggeggggmgggg##ggggggbggggGg"
  新: "ggHggngg##gggggeggggmgggg##ggggggbgggHGg"
```

### デバッグボタン追加 (debug=1)

各ステージに「ゴール側帰還ゲートへ移動」ボタンを追加:
- ステージ1: x=36, y=1 に移動（ゴール側Hの1マス手前）
- ステージ2: x=36, y=1 に移動
- ステージ3: x=36, y=2 に移動
- ステージ4: x=36, y=2 に移動

### 実装場所

- `SIDE_STAGE_DATA[1/2/3/4]` の該当行のみ1文字変更。
- `moveSidePlayer()` の 'H' 処理は変更不要（共通ロジック）。
- `renderSettingsBody()` の debug=1 セクションにボタン追加。

---

## §57 — 横スクロールステージ5「黒い城」(v0.13)

### 概要

4つの横スクロールステージをクリアした先に現れる最終横スクロールステージ「黒い城」(🏰)。
ステージ4のゴールモーダルに「🏰 黒い城へ進む」ボタンを追加し、任意で進入できる。

### マップ仕様 (40×5)

| row | y | 内容 |
|-----|---|------|
| row0 | 0 | 高路: 城の通路。宝箱2個 (c@x=8, c@x=30)。##城壁あり。 |
| row1 | 1 | 上中: 逃げ腰の旅人 NPC (p@x=12)。固定敵 来訪者(e@x=27)。 |
| row2 | 2 | 中央 (startY): H帰還ゲート@x=2、兵士NPC (n@x=5)、##@x=8-9、固定敵 宇宙人(e@x=14)、商人(m@x=21)、##@x=26-27、ラスボス級ゴリラ(b@x=33)、G(ゴール)@x=37、ゴール側H@x=38 (§58 v0.13.1) |
| row3 | 3 | 下中: 宝箱 (c@x=16)。固定敵 異邦人(e@x=23)。##@x=6-7, ##@x=26-27 |
| row4 | 4 | 下路: 宝箱2個 (c@x=4, c@x=17)。水路(~)。##@x=8-9, ##@x=28-29 |

startX: 1, startY: 2, goalX: 38

#### マップ文字列 (各40文字)

```
row0: "##ffffffcf##ffffffff##ffffffffcff##fffff"
row1: "gggfggggggggpgg##gggggg##ggeggggggfggggg"
row2: "ggHggngg##ggggeggggggmgggg##gggggbgggGHg"  ← §58 v0.13.1: x=37→G(ゴール), x=38→H(帰還ゲート)
row3: "~~gggg##ggggggggcggggggegg##ggggggggggg~"
row4: "~~~~cggg##gggggggcgggggggggg##ggggggggg~"
```

### ラスボス級ゴリラ (lastboss_gorilla)

| 項目 | 値 |
|------|-----|
| HP | 1000 |
| ATK | 58 |
| DEF | 22 |
| EXP | 1400 |
| 捕獲 | 不可 (canCapture: false) |
| 逃走率 | 8% |
| 登場メッセージ | 「黒い城の奥から、重すぎる気配が迫ってくる……ラスボス級ゴリラが道をふさいだ！」 |

### 固定敵 (SIDE_FIXED_ENCOUNTERS)

| キー | 敵ID | 場所 |
|------|------|------|
| "5:14,2" | alien (宇宙人) | row2 中央路 x=14 |
| "5:27,1" | visitor (来訪者) | row1 上中路 x=27 |
| "5:23,3" | stranger (異邦人) | row3 下中路 x=23 |

### ゴール報酬 (stage5RewardLevel)

| 条件 | 報酬 | newRewardLevel |
|------|------|----------------|
| 初回クリア + ラスボス撃退 | 500G + ラーメン | 2 |
| 初回クリア (ラスボス未撃退) | 200G | 1 |
| 再クリア + 撃退 (level=1→2) | 300G + ラーメン | 2 |
| その他 | なし | 変化なし |

### NPC

- **n@(5,2) 城門前の兵士**: ラスボス撃退前後でセリフ分岐
- **p@(12,1) 逃げ腰の旅人**: ラスボス撃退前後でセリフ分岐

### ナビゲーション

ステージ4ゴールモーダルの「🏰 黒い城へ進む」ボタンで進入。
ステージ5ゴールモーダルでは通常マップへ戻る / この黒い城に残る の2択のみ。ステージ6は未実装。

### セーブデータ

- `sideMapStage5Reward` キーで `stage5RewardLevel` を保存
- `state.sideMap.stage5RewardLevel: 0` を初期化

### ステータス称号

| 条件 | 称号 |
|------|------|
| s5Cleared + lastboss defeated | 黒い城の覇者 |
| s5Cleared (未撃退) | 黒い城を越えし者 |

### ヒント優先度

| priority | 条件 |
|----------|------|
| 15 | s4Cleared、s5未クリア → ステージ5ガイド |
| 9 | s5Cleared → 全5ステージクリア済み |

ステージ5ゴールモーダルに「🌿 チンパンジーの聖域へ進む」ボタンを追加し、ステージ6へ進入できる。

---

## §58 — ゴール側G/H配置変更「ボス → G → H」(v0.13.1)

### 目的

v0.12.1〜v0.13 では各ステージの終端が `b → H → G` の順 (x=36:ボス, x=37:帰還ゲート, x=38:ゴール) だった。
ボスを倒してゴールへ進もうとするプレイヤーが帰還ゲートを先に踏んでしまう問題を修正し、`b → G → H` の順 (x=37:ゴール, x=38:帰還ゲート) に変更する。

### 変更後のフロー

```
ボス(b) → ゴール🏁(G @x=37) → 帰還ゲート🏠(H @x=38)
```

- G を踏む → ゴールモーダル（報酬 / 「この◯◯に残る」選択肢）
- 「残る」を選択 → 右へ1歩でH (x=38) → 帰還ゲートモーダル → 通常マップへ戻れる

### 変更内容 (全5ステージ)

| ステージ | 主行 | 変更前末尾 | 変更後末尾 | goalX |
|---|---|---|---|---|
| 1 はじまりの草原 | rows[1] | `...bHGg` | `...bGHg` | 38 (変更なし) |
| 2 あやしい森     | rows[1] | `...bgHGg` | `...bgGHg` | 38 (変更なし) |
| 3 古びた町はずれ | rows[2] | `...gg##gHGg` | `...gg##gGHg` | 38 (変更なし) |
| 4 ゴリラ山道     | rows[2] | `...bgggHGg` | `...bgggGHg` | 38 (変更なし) |
| 5 黒い城         | rows[2] | `...bgggHGg` | `...bgggGHg` | 38 (変更なし) |

goalX=38 は「帰還ゲートH (x=38) までカメラが映る」ために維持する。

### 実装場所

- `SIDE_STAGE_DATA[1/2/3/4/5]` の該当メイン行のみ x=37/x=38 の1文字ずつ入れ替え。
- `moveSidePlayer()` の `'G'` / `'H'` 処理は変更不要（共通ロジックがそのまま動く）。
- デバッグボタンのtoastメッセージを新位置に合わせて更新。
- `index.html` ヘルプの帰還ゲート説明を更新。

---

## §59 — 横スクロールステージ6「チンパンジーの聖域」(v0.14)

### 概要

全横スクロールステージの最終ステージ「チンパンジーの聖域」(🌿)。
ステージ5のゴールモーダルに「🌿 チンパンジーの聖域へ進む」ボタンを追加し、任意で進入できる。

### マップ仕様 (40×5)

| row | y | 内容 |
|-----|---|------|
| row0 | 0 | 高路: 聖域の森の天蓋。宝箱2個 (c@x=8, c@x=29)。##木立あり。 |
| row1 | 1 | 上中: 迷い込んだ修行者 NPC (p@x=11)。固定敵(e@x=27)。##@x=15-16, ##@x=23-24。 |
| row2 | 2 | 中央 (startY): H帰還ゲート@x=2、聖域の守護者NPC (n@x=5)、##@x=8-9、固定敵(e@x=13)、商人(m@x=21)、##@x=27-28、究極チンパンジー(b@x=34)、G(ゴール)@x=37、H(帰還ゲート)@x=38 |
| row3 | 3 | 下中: 宝箱 (c@x=16)。固定敵(e@x=23)。##@x=6-7, ##@x=26-27 |
| row4 | 4 | 下路: 宝箱 (c@x=4)。##@x=8-9, ##@x=23-24。水路(~)@x=0-3, x=36-39 |

startX: 1, startY: 2, goalX: 38

#### マップ文字列 (各40文字)

```
row0: "ffff##ffcff##ffffffff##ffffffcff##ffffff"
row1: "gggggggggggpggg##gggggg##ggegggggggggfg"
row2: "ggHggngg##gggegggggggmggggg##gggggbggGHg"
row3: "~~gggg##ggggggggcggggggegg##ggggggggggg~"
row4: "~~~~cggg##ggggggggggggg##ggggggggggg~~~~"
```

### 究極チンパンジー (ultimate_chimpanzee)

| 項目 | 値 |
|------|-----|
| HP | 1500 |
| ATK | 72 |
| DEF | 32 |
| EXP | 3000 |
| 捕獲 | 不可 (canCapture: false) |
| 逃走率 | 5% |
| 登場メッセージ | 「チンパンジーの聖域の奥深くから、神々しい雄叫びが響き渡った……\n究極チンパンジーが降り立った！」 |

### 固定敵 (SIDE_FIXED_ENCOUNTERS)

| キー | 敵ID | 場所 |
|------|------|------|
| "6:13,2" | stranger (異邦人) | row2 中央路 x=13 |
| "6:27,1" | wanderingman (さまようおやじ) | row1 上中路 x=27 |
| "6:23,3" | deathmatch (デスマッチ) | row3 下中路 x=23 |

### ゴール報酬 (stage6RewardLevel)

| 条件 | 報酬 | newRewardLevel |
|------|------|----------------|
| 初回クリア + 究極チンパンジー撃退 | 800G + ラーメン | 2 |
| 初回クリア (未撃退) | 300G | 1 |
| 再クリア + 撃退 (level=1→2) | 500G + ラーメン | 2 |
| その他 | なし | 変化なし |

### NPC

- **n@(5,2) 聖域の守護者**: 究極チンパンジー撃退前後でセリフ分岐
- **p@(11,1) 迷い込んだ修行者**: 撃退前後でセリフ分岐

### ナビゲーション

ステージ5ゴールモーダルの「🌿 チンパンジーの聖域へ進む」ボタンで進入。
ステージ6ゴールモーダルでは通常マップへ戻る / この聖域に残る の2択。

### セーブデータ

- `sideMapStage6Reward` キーで `stage6RewardLevel` を保存
- `state.sideMap.stage6RewardLevel: 0` を初期化

### ステータス称号

| 条件 | 称号 |
|------|------|
| s6Cleared + 究極チンパンジー撃退 | チンパンジーの聖域の覇者 |
| s6Cleared (未撃退) | 聖域を越えし者 |

### ヒント優先度

| priority | 条件 |
|----------|------|
| 16 | s5Cleared、s6未クリア → ステージ6ガイド |
| 9 | s6Cleared（チンパンジー未撃退）→ チンパンジー撃退ガイド |
| 17 | isSideStoryCleared（s6Cleared + チンパンジー撃退済み）→ 究極ゴリラ捕獲誘導 |

---

## §60 — 横スクロール編クリア後導線・究極ゴリラ捕獲誘導 (v0.14.1)

**[実装済み]**

### 概要

横スクロール編6ステージ + 究極チンパンジー撃退を「横スクロール編制覇」として定義し、
その後のプレイヤーをゲーム本編目的「究極ゴリラ捕獲」へ自然に誘導する。

### 横スクロール編制覇条件

```
ステージ6「チンパンジーの聖域」クリア済み (sm.stageCleared["6"] === true)
かつ
究極チンパンジー撃退済み (sm.defeatedEnemies["6:34,2"] === true)
```

`isSideStoryCleared()` ヘルパー関数として実装。

### ステータス称号変更

| 条件 | 称号 (v0.14.1) |
|------|------|
| s6Cleared + 究極チンパンジー撃退 | **ゴリラの世界の外側を見た者** (変更) |
| s6Cleared (未撃退) | 聖域を越えし者 |

### ステータス画面追加行

横スクロール進捗の末尾に「横スクロール編」総合判定行を追加：

- 制覇済み（isSideStoryCleared）: `✅ 制覇済み`
- 未制覇: `進行中`

### NPC反応（チンパンジー撃退後）

| NPC | 追加条件 | 追加内容 |
|-----|----------|----------|
| UMA博士 | isSideStoryCleared | チンパンジーはUMAではない・究極ゴリラが本題のセリフ |
| ゴリラ研究家 | isSideStoryCleared | 究極ゴリラとチンパンジーの役割の違いを説明 |
| 王様の使い | isSideStoryCleared | 究極ゴリラ捕獲への誘導 |
| 旅人 | isSideStoryCleared | 横スクロール制覇への反応・究極ゴリラへの示唆 |

### 実家ヒント追加

HOME_HINTS に横スクロール編制覇後用のヒントを2件追加。
openHomeModal でも isSideStoryCleared + !gameCleared 時に固定ヒントを出す。

### 攻略ペーパービュー屋ヒント (priority 17)

isSideStoryCleared + !gameCleared の場合に priority 17 (究極ゴリラ捕獲誘導) を返す。

| tier | ヒント |
|------|--------|
| 1 (10G) | 「横に長い冒険は一区切りついた。だが、伝説のUMAはまだ森のどこかにいる。」 |
| 2 (50G) | 「究極ゴリラは、ただ弱らせるだけでは捕まえられない。特別なアイテムと、特別な行動が必要らしい。」 |
| 3 (100G) | 「究極ゴリラを捕まえるには、Lv99以上、女神のウクレレ、HP1〜10まで弱らせることが必要だ。最後は「つかまえる」ではなく、「うたう」。」 |

既存 priority 9 は「s6Cleared だが チンパンジー未撃退」の場合のみ適用。

### ゴールモーダル追記

openStage6GoalModal の「撃退済み全取得」ルートに究極ゴリラへの導線テキストを1行追加。

### デバッグボタン追加

| ボタンID | 機能 |
|---------|------|
| btn-debug-side-story-complete | 横スクロール編制覇状態にする（s6クリア＋チンパンジー撃退＋stage6RewardLevel=2） |
| btn-debug-set-capture-ready | 究極ゴリラ捕獲条件セット（Lv99＋ウクレレ所持） |
| btn-debug-clear-gameclear | 究極ゴリラ未捕獲状態に戻す（gameCleared=false） |

### ヘルプセクション追加

「🌿 横スクロール編と本編目的」セクションをステージ6説明の後に追加。


## §61 わざシステム（捕獲支援技） v0.15

**[実装済み]**

戦闘コマンドに「🥊 わざ（捕獲支援）」を追加。
低固定ダメージ技を使ってUMAや究極ゴリラのHPを1〜10に調整しやすくする捕獲支援機能。

### 背景・目的

通常攻撃・まほうはレベルや装備によってダメージが変わるため、
HPを1〜10の範囲に収めるのが難しい。
固定ダメージ技を使えば「残りHP3にしたい」といった繊細なHP調整が可能になる。

### わざコマンド

`#battle-menu` の最後に `🥊 わざ（捕獲支援）` ボタン（`grid-column:span 2` でフル幅）を追加。
選択するとサブメニュー `#waza-menu` が開き、4種類の技から選択できる。

### 低固定ダメージ技（WAZA_DATA）

| id | 名前 | 固定ダメージ |
|---|---|---|
| hazukashigatame | はずかし固め | 1 |
| kidoclutch | キドクラッチ | 2 |
| karitsuo | カリツォー | 3 |
| gupanchi | グーパンチ | 4 |

### 設計方針

- プレイヤーの攻撃力・敵の防御力に関わらず、固定ダメージを与える（防御無視）。
- MPを消費しない（MP0でも使用可能）。
- 使用後は敵のターンに移行する（通常攻撃と同じ流れ）。
- 固定ダメージでもHP0になれば `winBattle()` が呼ばれる（通常攻撃と同じ扱い）。
- `WAZA_DATA` は SPELL_DATA とは別の配列。ID が一部同名でも参照配列が別なので衝突なし。

### 追加変更箇所

| 箇所 | 内容 |
|---|---|
| `WAZA_DATA` | SPELL_DATA の後に配列新設 |
| `openWazaMenu()` | サブメニュー生成・戻るボタン（openMagicMenu() と同パターン） |
| `useWaza(id)` | 固定ダメージ付与・renderEnemy・敵ターン移行 |
| `#btn-waza` (HTML) | `#battle-menu` 最後に追加、`style="grid-column:span 2"` |
| `#waza-menu` (HTML) | `#item-menu` の後に追加、`class="hidden submenu"` |
| `init()` | `btn-waza` → `openWazaMenu` リスナー追加 |
| NPC_DATA UMA博士 | 捕獲率が低い時にわざヒントを追加 |
| HOME_HINTS | わざで HP 調整できる旨のヒント追加 |
| getProgressHint priority 17 | tier3 にわざ言及追加 |
| index.html ヘルプ | 「🥊 わざコマンドについて」セクション追加 |

---

## §62 v0.15.1 — わざコマンド安定化・表示整理・究極ゴリラ捕獲テスト強化

### 概要

v0.15 で追加したわざコマンドのUX改善と捕獲テスト強化。

### 変更内容

| 箇所 | 内容 |
|---|---|
| `WAZA_DATA` | `hazukashigatame` の表示名を「はずかし固め」→「**はずかし固め・小**」に変更（内部IDは変更なし。まほうの同名技と区別） |
| `actuallyStartBattle()` | 戦闘開始時に `#waza-menu` も `hidden` に追加（バグ修正: magic-menu/item-menu と同様の処理を追加） |
| `openWazaMenu()` | メニュー先頭に説明テキスト「UMAを弱らせるための固定ダメージ技です。削りすぎに注意！」を追加 |
| `checkUltimateGorillaHpHint(e)` | 新規ヘルパー関数。究極ゴリラのHPが1〜10の時に「うたう」チャンスをログに表示 |
| `useWaza()` | ダメージログの次行に「残りHP: X / MaxHP」表示追加。`checkUltimateGorillaHpHint()` 呼び出し追加 |
| `doFight()` | winBattle チェック後に `checkUltimateGorillaHpHint()` 呼び出し追加 |
| デバッグ §62 | 「🦍 究極ゴリラ捕獲テスト」セクション追加: HP12/10/1 で戦闘開始ボタン3本 |
| `debugForceUltimateGorillaHP12/10/1()` | HP指定で究極ゴリラ戦闘を直接開始する関数3種追加 |
| `getProgressHint priority17 tier3` | 「はずかし固め・小」の名前を明記 |
| index.html ヘルプ | わざ技名を「はずかし固め・小」に更新。まほうの同名技との違いをノートとして追記 |

### 設計メモ

- `hazukashigatame` の内部IDはセーブデータに影響しないため変更なし（`state.player.spells` に格納されるのはSPELL_DATAのIDのみ）。
- `actuallyStartBattle()` の waza-menu hidden 漏れ: バトル終了後に finishBattle() が battle-screen 全体を hidden にするため視覚的には問題なかったが、次の戦闘開始時に waza-menu が表示状態のまま残るバグがあった。
- `checkUltimateGorillaHpHint(e)` は doFight / useWaza 双方から呼ぶ共有関数として `doFight()` の直前に配置。

---

## §63 v0.16 — 捕獲支援技「ここはひとつガマン」

### 概要

その戦闘中だけ通常攻撃ダメージを大幅に下げる補助技。プレイヤーの永続ステータスは変更しない。

### WAZA_DATA 追加エントリ

```
{ id: "gaman", name: "ここはひとつガマン", type: "weakenAttack", emoji: "😤" }
```

`fixedDmg` はなく、`type: "weakenAttack"` で固定ダメージ技と区別する。

### 効果仕様

- **対象**: 通常攻撃（doFight）のみ
- **効果範囲外**: わざ固定ダメージ / まほう / アイテム / 捕獲 / うたう / 敵の攻撃
- **ダメージ計算**: 通常攻撃ダメージ（会心込み）を最後に 1/4 にする。最低1ダメージ保証
- **会心との関係**: 会心ダメージ計算後にガマン補正をかける（会心でも削りすぎを防ぎやすい）
- **状態フラグ**: `state.gamanActive: boolean`（state 最上位）
- **永続ステータス変更なし**: `state.player.atk` などは一切触らない

### 効果の開始・解除

| タイミング | 処理 |
|---|---|
| わざ「ここはひとつガマン」使用 | `state.gamanActive = true` |
| 再使用（すでにガマン中） | フラグ変更なし（1ターン消費、メッセージ表示） |
| 戦闘終了すべて（勝利/捕獲/逃走/敗北） | `finishBattle()` で `state.gamanActive = false` |

**解除ポイント（すべて `finishBattle()` 経由）:**
- `winBattle()` → `showBattleEnd()` → OKボタン → `finishBattle()`
- 捕獲成功 → `showBattleEnd()` → `finishBattle()`
- 逃走成功 → `showBattleEnd()` → `finishBattle()`
- 敗北 → `handlePlayerDown()` → `finishBattle()` (直接呼び出し)
- 究極ゴリラうたう成功 → `showBattleEnd()` → `finishBattle()`

### ログメッセージ

- 初回使用: 「😤 勇者の子孫は「ここはひとつガマン」した！肩の力が抜けて、通常攻撃の威力が大きく下がった！UMAを削りすぎにくくなった！」
- 再使用: 「😤 すでにガマン中だ！通常攻撃の威力は下がったままだ。」
- ガマン中の通常攻撃ログ: 「⚔ (名前)の攻撃！（ガマン中） (敵)にXのダメージ！」
- ガマン中の会心: 「⚔ (名前)の攻撃！ 💥 会心（ガマン中）！ (敵)にXのダメージ！」

### わざメニュー表示

- ガマン未使用: `😤 ここはひとつガマン（通常攻撃を弱める）`
- ガマン中: `😤 ここはひとつガマン（効果中）`
- メニュー説明文: 「UMA捕獲を助ける技です。固定ダメージで削ったり、ガマンで通常攻撃を弱めたりできます。⚡ガマン中」（ガマン中のみステータス付記）

### 変更箇所

| 箇所 | 内容 |
|---|---|
| `state.gamanActive` | 初期値 `false` で state に追加 |
| `WAZA_DATA` | `gaman` エントリ追加（type: "weakenAttack"） |
| `openWazaMenu()` | 説明文更新 + ガマン技を type で分岐レンダリング |
| `useWaza()` | `type === "weakenAttack"` の場合は固定ダメージなしでフラグ操作 |
| `doFight()` | 会心計算後に `gamanActive` なら 1/4（最低1）、ログ分岐 |
| `finishBattle()` | `state.gamanActive = false` 追加 |
| NPC UMA博士 | capturedCount<4 / Lv50+ のヒントにガマン言及を追加 |
| `getProgressHint priority17 tier3` | 「ここはひとつガマン」を追記 |
| index.html ヘルプ | 補助技セクションに「ここはひとつガマン」説明を追加 |
| デバッグ §63 | ガマン状態での戦闘開始ボタン2本 + 解除ボタン1本追加 |

## §64 v0.16.1 — 究極ゴリラ捕獲チャンス演出・ガマン状態表示

### 概要

究極ゴリラのHPが1〜10になったとき、条件充足状況に応じたメッセージを表示し、
捕獲チャンス時は「🎵 うたう」ボタンを視覚的に強調する。
またガマン中インジケーターを戦闘画面に常時表示する。

### 実装内容

#### ① `#battle-gaman-status` — ガマン中インジケーター（index.html）

`#battle-player-status` の直後に追加。`state.gamanActive` に連動して表示/非表示。

```html
<div id="battle-gaman-status" class="hidden" style="font-size:0.75em;color:#a9e34b;text-align:center;padding:2px 0;">😤 ガマン中：通常攻撃↓</div>
```

`updateBattlePlayerStatus()` でガマン状態を反映。`useWaza()` のガマン分岐でも即時呼び出し。

#### ② `.btn-chance` — うたうボタン演出（style.css）

```css
.btn-chance {
  animation: pulse-chance 1s ease-in-out infinite alternate;
  border-color: #ffd700 !important;
  color: #ffd700 !important;
}
@keyframes pulse-chance {
  from { box-shadow: 0 0 4px #ffd700; }
  to   { box-shadow: 0 0 14px #ffd700; }
}
```

#### ③ `checkUltimateGorillaHpHint(e)` — 条件別4分岐

| 条件 | メッセージ | うたうボタン |
|---|---|---|
| Lv99+ ＆ ウクレレあり | 「🎵 究極ゴリラが歌を待っている！HPは捕獲条件の範囲内だ！今こそ「うたう」チャンス！」 | `btn-chance` クラス付与（金色点滅） |
| Lv不足 ＆ ウクレレなし | 「まだ条件が足りない。もっと成長し、歌を届けるための楽器を探そう。」 | 変化なし |
| Lv不足のみ | 「まだ勇者としての格が足りない気がする……Lv99以上になれば、何かが起きるかもしれない。」 | 変化なし |
| ウクレレなしのみ | 「歌を届けるための大切な楽器が足りない気がする……」 | 変化なし |

#### ④ `updateSingButtonChance(active)` — 新関数

```javascript
function updateSingButtonChance(active) {
  var btn = document.getElementById("btn-sing");
  if (!btn) return;
  if (active) { btn.classList.add("btn-chance"); }
  else        { btn.classList.remove("btn-chance"); }
}
```

呼び出し元: `checkUltimateGorillaHpHint`（チャンス時）、`actuallyStartBattle`（リセット）、`finishBattle`（リセット）。

### デバッグ §64（条件別テスト3本）

| ボタンID | 内容 |
|---|---|
| `btn-debug-gorilla-chance-hp10` | Lv99+ウクレレ+HP10（チャンス表示確認） |
| `btn-debug-gorilla-nolv-ukulele-hp10` | Lv50+ウクレレ+HP10（Lv不足メッセージ確認） |
| `btn-debug-gorilla-lv99-noukulele-hp10` | Lv99+ウクレレなし+HP10（ウクレレ不足確認） |

### 変更箇所一覧

| 箇所 | 内容 |
|---|---|
| `index.html` | `#battle-gaman-status` div 追加 |
| `style.css` | `.btn-chance` + `@keyframes pulse-chance` 追加 |
| `checkUltimateGorillaHpHint()` | 4分岐 + `updateSingButtonChance` 呼び出し |
| `updateSingButtonChance()` | 新関数追加 |
| `updateBattlePlayerStatus()` | `#battle-gaman-status` 表示切替を追加 |
| `useWaza()` | ガマン分岐に `updateBattlePlayerStatus()` 呼び出し追加 |
| `actuallyStartBattle()` | `updateSingButtonChance(false)` リセット追加 |
| `finishBattle()` | `updateSingButtonChance(false)` リセット追加 |
| デバッグ §64 | 3ボタン追加（HTML + ハンドラ） |

## §65 v0.17 — 究極ゴリラ捕獲クライマックス演出・クリア後リアクション強化

### 概要

捕獲成功の達成感と、クリア後の世界の反応を強化する。捕獲条件・BGM制御・エンディング本編は変更しない。

### 捕獲クライマックス演出

`doSingUltimateGorilla()` の成功ログを5行に刷新：

```
🪗 勇者の子孫は、女神のウクレレを静かにかき鳴らした。
🎶 森に、やさしい歌が広がっていく。
🦍 究極ゴリラは暴れるのをやめ、じっとその歌に耳をすませている……
🦍 やがて究極ゴリラは、ゆっくりと近づいてきた。
🎉 究極ゴリラを捕まえた！
```

### 捕獲成功モーダル `#capture-modal`

`openClearModal()` → `openCaptureModal()` → 「王様へ報告する」ボタン → `openEndingModal()`

| 要素 | 内容 |
|---|---|
| タイトル | 「歌が届いた」 |
| 本文 | 「究極ゴリラは、君の歌を聞いている。力ではなく、歌によって心が届いた。伝説のUMAは、ついに君の仲間となった。」 |
| ボタン | 「王様へ報告する」 |

### 総合称号

| 条件 | 称号 |
|---|---|
| クリア済み ＋ 横スクロール制覇済み | 「究極を歌い、聖域を越えし者」 |
| クリア済みのみ | 「森に歌を届けし者」 |

表示箇所: ステータス画面・エンディング最終ページ

`isSideStoryCleared()` で判定（s6クリア＋チンパンジー撃退）。

### クリア後NPC反応

| NPC | クリア後セリフ |
|---|---|
| UMA博士 | 「ついに究極ゴリラを捕まえたのじゃな……！力でねじ伏せるのではなく、歌を届けたから……おぬしは本物のUMAハンターじゃ。図鑑の完成まで目指してみないか。」 |
| ゴリラ研究家 | 「究極ゴリラを捕まえた記録は、歴史に残る。チンパンジーを退かせた強さ。究極ゴリラに歌を届けたやさしさ。伝説の装備をすべて集めたか？」 |
| 王様の使い | 「王はすでに報告を受けている。究極ゴリラを捕まえた者として、おぬしの名は森の歴史に刻まれるだろう。実家で休んでみるとよい。」 |
| 旅人 | 「おいおい、本当に究極ゴリラを捕まえたのか？最初に会った時から、ただ者じゃないとは思ってたけどな。まさかここまでやるとはな。」 |

### 実家ヒント（クリア後）

`openHomeModal()` でクリア後は以下のいずれかをランダム表示:
- 「究極ゴリラを捕まえても、冒険の思い出は消えないよ。図鑑を見たり、横スクロールの世界を歩いたり……」
- 「力だけじゃなく、歌で届くものもあるんだね。」
- 「図鑑をすべて埋めると、何か良いことがあるかもしれないよ。」
- 「横スクロールの世界を全部制覇したかな？チンパンジーもいるよ。」

### 攻略ペーパービュー屋（クリア後）

`getProgressHint()` priority=0 に `if (priority === 0)` 専用ブランチを追加。

| 状態 | tier1 | tier2 | tier3 |
|---|---|---|---|
| 横スクロール制覇済み | 「究極ゴリラを捕まえ、横スクロールの世界も踏破した。」 | 「図鑑をすべて埋めるか伝説装備（全7種）を揃えるか。称号を誇りに。」 | 「図鑑の捕獲数と伝説装備（全7種）を確認しよう。NPCにもクリア後の言葉があるぞ。」 |
| 横スクロール未制覇 | 「究極ゴリラを捕まえた。だが横スクロールの世界にはまだ見ぬ強敵がいるかも。」 | 「横スクロールを制覇してチンパンジーを退かせると新しい称号が得られる。」 | 「ステージ1〜6制覇+チンパンジー撃退で「究極を歌い、聖域を越えし者」が得られる。」 |

### ヘルプ更新

index.html に「🌟 クリア後の遊び方」セクションを追加。

### デバッグ §65

| ボタンID | 内容 |
|---|---|
| `btn-debug-show-capture-modal` | 捕獲成功モーダルを直接表示 |
| `btn-debug-set-postclear-full` | クリア済み+横スクロール制覇状態にする（総合称号確認） |
| `btn-debug-set-postclear-only` | クリア済みのみ（横スクロール未制覇）状態にする |

### 変更箇所一覧

| 箇所 | 内容 |
|---|---|
| `doSingUltimateGorilla()` | 捕獲成功ログを5行クライマックス演出に刷新 |
| `openClearModal()` | `openCaptureModal()` へリダイレクト |
| `openCaptureModal()` | 新関数追加 |
| `#capture-modal` (index.html) | 新モーダル追加 |
| `renderEndingPage()` isFinal | 称号を `isSideStoryCleared()` で分岐 |
| `renderStatus()` playerTitle | 称号を `isSideStoryCleared()` で分岐 |
| UMA博士 `getLines()` | クリア後セリフに交換（`return lines` 追加） |
| 旅人 `getLines()` | 関数先頭でクリア後早期 return 追加 |
| ゴリラ研究家 `getLines()` | クリア後セリフに交換（`return lines` 追加） |
| 王様の使い `getLines()` | クリア後セリフに交換 |
| `openHomeModal()` | 先頭でクリア後ヒント分岐追加 |
| `getProgressHint()` | priority=0 に専用ブランチ追加 |
| index.html ヘルプ | 「🌟 クリア後の遊び方」セクション追加 |
| デバッグ §65 | 3ボタン追加（HTML + ハンドラ） |

---

## §67 v0.18: 冒険の記録・達成状況パネル

### 概要

上部メニューに「📜記録」ボタンを追加し、達成状況を一覧できる `#record-modal` を実装する。
プレイヤーが「今どこまで達成しているか」「あと何が残っているか」を確認しやすくする。

### メニューボタン

`id="btn-record"` を `status-row` の `.small-btn` として追加（btn-statusの後、btn-settingsの前）。

### record-modal 表示項目

| セクション | 内容 |
|---|---|
| 現在の称号 | `getPlayerTitle()` の結果を大きく表示 |
| 本編クリア | 究極ゴリラ捕獲状態（捕獲済み/未捕獲）。未捕獲なら条件ヒント |
| 横スクロール編 | 制覇状態 + 各ステージ（1〜6）のクリア/ボス撃退状態 |
| UMA図鑑 | 捕獲数/総数 + コンプリートか否か |
| 図鑑コンプリート報酬 | 受取済み/未受取。未受取は「図鑑を開くと受け取れます」ヒント |
| 次の目標 | 現在の状態に応じた最優先ガイダンス |
| 称号条件一覧 | 6段階称号の全条件を一覧表示 |

### 称号集中管理: `getPlayerTitle()`

`renderStatus()` と `renderEndingPage()` の両箇所に散在していた6段階称号判定を
`getPlayerTitle()` として共通関数化する。

```js
function getPlayerTitle() {
  var p = state.player;
  if (state.gameCleared && isSideStoryCleared() && isUmaDexComplete())
    return "究極とUMA図鑑を極めし者";
  if (state.gameCleared && isSideStoryCleared())
    return "究極を歌い、聖域を越えし者";
  if (isUmaDexComplete())
    return "UMA図鑑を極めし者";
  if (state.gameCleared)
    return "森に歌を届けし者";
  if (p.level >= 99 || p.level99Shown)
    return "究極に近づきし者";
  return "勇者の子孫";
}
```

### 次の目標ロジック

| 状態 | 表示 |
|---|---|
| 究極ゴリラ未捕獲 | Lv99・女神のウクレレ・HP1〜10・うたうを案内 |
| クリア済み・横スクロール未制覇 | 🌀ゲートから横スクロールへ案内 |
| 両方クリア・図鑑未コンプ | 未捕獲UMAを探す案内 |
| 全達成 | 達成済みメッセージ |

### デバッグ §67

| ボタンID | 内容 |
|---|---|
| `btn-debug-open-record` | 冒険の記録モーダルを開く |

## §68 v0.18.1: 冒険の記録UI改善・達成率プログレスバー追加

### 概要

記録モーダル（§67）に総合達成率と各カテゴリのプログレスバーを追加し、
達成画面としての視覚的インパクトを強化する。

### スコア計算方式

| カテゴリ | 最大pt | 計算方法 |
|---|---|---|
| 本編（究極ゴリラ） | 1pt | 捕獲済み = 1、未捕獲 = 0 |
| 横スクロール編 | 12pt | ステージクリア×6 + ボス撃退×6 |
| UMA図鑑 | 12pt | capturedDexCount / totalUma × 12 |
| **合計** | **25pt** | 総合達成率 = 合計 / 25 × 100（%） |

### 追加CSS

| クラス | 役割 |
|---|---|
| `.record-progress` | バー外枠（半透明グレー、高さ10px） |
| `.record-progress-fill` | バー塗り（グラデーション） |
| `.record-section-goal` | 「次の目標」セクション強調（金色ボーダー） |

### 変更点

- `.record-section` を背景 `rgba(0,0,0,0.2)` のカードスタイルに更新
- 総合達成率セクション（金色ヘッダー）を記録モーダル最上部に配置
- 本編/横スクロール/UMA図鑑 の各セクションにカテゴリ別バーを追加
- 「次の目標」セクションに `.record-section-goal` で金色強調ボーダー

## §69 v0.19: NPC固有イベント深化・クリア後世界の会話強化

### 完全達成条件

```
state.gameCleared === true
isSideStoryCleared() === true  (横スクロール全6ステージ制覇+チンパンジー撃退)
isUmaDexComplete() === true    (全9種UMA捕獲済み)
```

### 追加ヘルパー関数

| 関数 | 判定内容 |
|---|---|
| `isUltimateGorillaCaptured()` | `state.gameCleared` と同義（捕獲=クリア） |
| `isFullyCompleted()` | 上記3条件すべて true |

### NPC会話分岐 (優先度順)

| NPC | 最高優先 | 次の優先 | 通常 |
|---|---|---|---|
| UMA博士 D | `isFullyCompleted()` → 「ついにやり遂げた」 | `isUmaDexComplete()` → 既存 | クリア後+横スクロール誘導または図鑑誘導 |
| 旅人 R | `isFullyCompleted()` → 「案内する道は残ってない」 | クリア後 + !side → 横スクロール言及 | 既存 |
| ゴリラ研究家 E | `isFullyCompleted()` → 「伝説そのものだ」 | クリア後 + !side → 横スクロール誘導 | 既存 |
| 王様の使い S | `isFullyCompleted()` → 「真の英雄だ」 | クリア後 + !side → 横スクロール言及 | 既存 |

### 中間達成時の誘導セリフ追加

- **横スクロール制覇済み・究極ゴリラ未捕獲** (UMA博士 D): 「チンパンジーの聖域を越えた力があるなら、あとは歌を届けるだけじゃ」
- **クリア済み・横スクロール未制覇** (全4NPC): 横スクロール方面への誘導セリフ追加

### 仲間セリフ仕様

| 仲間 | `clearLine`（クリア後・緑色） | `fullClearLine`（完全達成後・金色） |
|---|---|---|
| ジュリタニ | 「歌で究極ゴリラを止めるなんて、一曲で決めたのが、あんたらしいよ」 | 「会心の一撃でも届かない場所に、歌で届いたんだな」 |
| シュリタニ | 「究極ゴリラまで捕まえるなんて、捕獲の極意が身についたみたい」 | 「図鑑まで全部埋まったんだね。一匹ずつ向き合ってきた証だよ」 |
| ノリオ | 「経験値だけじゃ測れない冒険だったな。EXPもだいぶ稼いだだろ？」 | 「完全達成か。経験値2倍でも足りないくらい、濃い旅だったな」 |
| ハルミ | 「最後は魔法じゃなくて歌だったのね。そういう力も私は好きよ」 | 「森も聖域も図鑑も、全部つながったのね。ちゃんと物語になったわ」 |

表示: `isFullyCompleted() && c.fullClearLine` → 金色斜体 / `state.gameCleared && c.clearLine` → 緑色斜体

### v0.24 仲間セリフ多段階バリエーション [実装済み]

優先順位: 1.伝説装備コンプ → 2.完全達成 → 3.クリア+図鑑 → 4.クリア+横スクロール → 5.クリアのみ → 6.通常

`getCompanionQuote(c)` ヘルパーで1箇所にまとめ、両render関数から呼び出す。

| 状態 | フィールド | 色 |
|---|---|---|
| `isFullyCompleted() && isLegendaryEquipmentComplete()` | `legendaryLine` | `#ffd700` 明金 |
| `isFullyCompleted()` | `fullClearLine` | `#ffd166` 金 |
| `state.gameCleared && isUmaDexComplete()` | `dexLine` | `#74c0fc` 水色 |
| `state.gameCleared && isSideStoryCleared()` | `sideClearLine` | `#c8b4ff` 薄紫 |
| `state.gameCleared` | `clearLine` | `#a9e34b` 緑 |

| 仲間 | `sideClearLine` | `dexLine` | `legendaryLine` |
|---|---|---|---|
| ジュリタニ | 「チンパンジーまで退かせるなんて、ずいぶん遠くまで来たな。横に長い旅も、なかなか悪くなかったぜ。」 | 「UMAを全部記録したのか。会心の一撃だけじゃなく、根気も必要だったな。」 | 「伝説装備まで全部そろえたのか。見た目も中身も、もう完全に勇者だな。」 |
| シュリタニ | 「強い相手を倒すだけじゃなくて、ちゃんと向き合って進んできたんだね。」 | 「図鑑が全部埋まったんだね。一匹ずつ見つけて、弱らせて、向き合ってきた証だよ。」 | 「伝説装備も全部そろったんだね。道具も、仲間も、思い出も、ちゃんと積み重なってる。」 |
| ノリオ | 「横スクロール編、経験値的にもだいぶおいしかったな。いや、もちろん思い出もだけどな。」 | 「図鑑コンプリートか。経験値には出ないけど、こういう達成感も悪くないな。」 | 「伝説装備まで全部そろえたのか。もう装備欄だけで経験値がにじみ出てるぞ。」 |
| ハルミ | 「聖域まで越えたのね。森の外側にも、ちゃんと物語があったんだわ。」 | 「図鑑が完成したのね。名前を記録するって、その存在を忘れないってことなのかもしれない。」 | 「伝説装備が全部そろったのね。武器や防具も、旅の記憶をまとっているみたい。」 |

冒険の記録「次の目標」最終メッセージ: 「酒場で仲間たちの言葉を聞いてみるのもよいでしょう。」を含める。

### v0.25 酒場UI改善・仲間カード整理 [実装済み]

酒場の3画面（仲間を探す / 仲間を見る / 仲間を外す）をカード形式に統一。

**CSS クラス（style.css §77）**:
- `.companion-card`: 角丸ボーダー・半透明背景のカードコンテナ
- `.companion-card-header`: 名前と状態を両端配置
- `.companion-name`: 太字 1.0em
- `.companion-ability`: 黄色 (#ffd166) 0.82em — `effectDesc` 表示
- `.companion-status`: 右端の小ラベル（パーティ中 `#06d6a0` / 待機中 `#adb5bd`）
- `.companion-quote`: 左ボーダー + 薄背景 + 斜体。`color` は `getCompanionQuote()` の返り値 (`currentColor` で左ボーダーも同色)
- `.companion-action` / `.companion-action button`: フルwidth ボタン
- `.companion-action .leave-btn`: 「外す」ボタン用 赤ボーダー

**仲間を探す (renderTavernRecruit)**: カード表示。パーティ中は ✓ パーティ中。待機中でパーティ空きあれば「🤝 仲間にする」ボタン。
**仲間を見る (renderTavernViewParty)**: カード表示。能力 + セリフのみ（アクションボタンなし）。
**仲間を外す (renderTavernLeave)**: カード表示。「👋 外す」ボタン（赤ボーダー）。

### v0.24.1 getCompanionQuote 安定化・優先順位拡張 [実装済み]

`getCompanionQuote(c)` の完全な優先順位（§76）:

| 優先 | 条件 | フィールド | 色 |
|---|---|---|---|
| 1 | `isFullyCompleted() && isLegendaryEquipmentComplete()` | `legendaryLine` | `#ffd700` 明金 |
| 2 | `isFullyCompleted()` | `fullClearLine` | `#ffd166` 金 |
| 3 | `isLegendaryEquipmentComplete()` (未完全達成) | `legendaryLine` | `#ffb347` 橙 |
| 4 | `state.gameCleared && isUmaDexComplete()` | `dexLine` | `#74c0fc` 水色 |
| 5 | `state.gameCleared && isSideStoryCleared()` | `sideClearLine` | `#c8b4ff` 薄紫 |
| 6 | `state.gameCleared` | `clearLine` | `#a9e34b` 緑 |
| 7 | `isSideStoryCleared()` (未クリア) | `sideClearLine` | `#c8b4ff` 薄紫 |
| - | 上記以外 | null（セリフなし） | — |

安全化: `if (!c) return null` ガード追加。`c.legendaryLine` 等は undefined でも `&&` で安全。

### 実家ヒント優先度 (openHomeModal)

1. `isFullyCompleted()` → 余韻ヒント（最高優先）
2. `gameCleared && isUmaDexComplete()` → 既存 postDexHints
3. `gameCleared && isSideStoryCleared()` → 横スクロール制覇後ヒント（新規）
4. `gameCleared` → postClearHints（「森の空気」ヒント追加）
5. `!gameCleared && Lv99 && hasUkulele` → 既存
6. `!gameCleared && Lv99` → 既存
7. `!gameCleared && isSideStoryCleared()` → 既存
8. else → HOME_HINTS ランダム

### 攻略ペーパー priority=0 分岐 (getProgressHint)

| ケース | tier1 | tier2 | tier3 |
|---|---|---|---|
| `isFullyCompleted()` | 「余韻の時間だ」 | 「伝説の冒険は完結した」 | 「伝説装備はすべて揃えたか？」 |
| `isUmaDexComplete()` | 既存 | 既存 | 既存 |
| `isSideStoryCleared()` | 「図鑑に空きがあるなら探そう」 | 「称号のために図鑑を」 | 「UMA博士・研究家にも言葉がある」 |
| else (横スクロール未制覇) | 「横スクロールへ進もう」 | 「新しい称号が得られる」 | 「称号詳細と図鑑案内」 |

### デバッグ §69

| ボタンID | 動作 |
|---|---|
| `btn-debug-npc-full-complete` | 完全達成状態にしてUMA博士を開く |
| `btn-debug-npc-cleared-only` | クリアのみ(横スクロール未制覇)にしてUMA博士を開く |
| `btn-debug-npc-side-cleared` | 横スクロール制覇済み・未クリアにしてUMA博士を開く |

---

## §70 v0.20 — 伝説装備コンプリート報酬

**[実装済み]**

### 概要

伝説装備7種（`LEGEND_EQUIPS`）をすべて入手した際に報酬を付与し、最高位称号「すべての伝説を集めし者」を解放するシステム。

### 伝説装備一覧（LEGEND_EQUIPS 7種）— §71 v0.20.1 で slot/itemId 追加

| 装備名 | フラグ | slot | itemId |
|---|---|---|---|
| ペガサスのよろい | `pegasusArmorGot`  | armor  | pegasusarmor   |
| 六連のたて       | `sixfoldShieldGot` | shield | sixfoldshield  |
| 宇宙のかぶと     | `cosmicHelmetGot`  | helmet | cosmickabuto   |
| 如意棒           | `nyoiboGot`        | weapon | nyoibo         |
| アンドロメダの鎖 | `andromedaGot`     | weapon | andromedachain |
| キグナスのかぶと | `cygnusHelmetGot`  | helmet | cygnuskabuto   |
| ドラゴンのたて   | `dragonShieldGot`  | shield | dragonshield   |

### 判定ヘルパー（§71 v0.20.1 更新）

| 関数 | 判定内容 |
|---|---|
| `isLegendaryEquipmentComplete()` | フラグ確認 + `isEquipOwned()` 二段構成。旧セーブ互換のため装備中/所持中でも入手済み扱い。 |

### セーブデータ

| フラグ | 初期値 | 説明 |
|---|---|---|
| `state.legendaryRewardClaimed` | `false` | 報酬受取済みフラグ。`saveGame/loadGame` に追加。 |

### 報酬モーダル（`#legendary-complete-modal`）

- トリガー: `openEquipModal()` で `isLegendaryEquipmentComplete() && !state.legendaryRewardClaimed`
- 報酬: 2000G ＋ ラーメン×2
- 称号取得時（`isFullyCompleted()` も true の場合）: 称号メッセージ追加表示
- ボタン「装備を確認する」→ `closeModal` → `openModal("equip-modal")` + `renderEquipBody()`

### 称号（`getPlayerTitle()` 優先順）

| 優先 | 称号 | 条件 |
|---|---|---|
| 1 | すべての伝説を集めし者 | `isFullyCompleted() && isLegendaryEquipmentComplete()` |
| 2 | 究極とUMA図鑑を極めし者 | `isFullyCompleted()` |
| 3 | 究極を歌い、聖域を越えし者 | `gameCleared && isSideStoryCleared()` |
| 4 | UMA図鑑を極めし者 | `isUmaDexComplete()` |
| 5 | 森に歌を届けし者 | `gameCleared` |
| 6 | 究極に近づきし者 | `level >= 99 or level99Shown` |
| 7 | 勇者の子孫 | （初期） |

### 冒険の記録（`renderRecordBody()`）

- `⚔️ 伝説装備` セクション: N/7進捗バー・各装備入手状況リスト
- `✨ 伝説装備コンプリート報酬` セクション: 受取済み/未受取/未解放
- 総合達成率スコア: 最大32pt（本編1 + 横スクロール12 + UMA図鑑12 + 伝説装備7）
- 称号条件一覧: 最上位に「すべての伝説を集めし者」を追加

### NPC K（鍛冶屋）分岐

| 状態 | セリフ |
|---|---|
| コンプリート済み | 「すべての伝説装備をそろえたのか……。武具に選ばれたというより、お前の旅が武具を目覚めさせたんだな。」 |
| 一部入手済み | 「★伝説の装備は、商人には売れんぞ。大切にな。まだN種類残っているぞ。」（残数はフィルタで計算） |
| 未入手（Lv30+） | 「フィールドには商人では買えない伝説の装備が眠っている。探してみな。」 |

### デバッグ §70

| ボタンID | 動作 |
|---|---|
| `btn-debug-legend-all` | 伝説装備7種の全フラグを true に |
| `btn-debug-legend-reward-reset` | `legendaryRewardClaimed = false` |
| `btn-debug-legend-reward-modal` | 全7種入手+未受取にしてモーダル表示 |

---

## §72 v0.21 — UMA図鑑コンプリート演出強化

**[実装済み]**

### `openDexCompleteModal()` 本文

| パート | 内容 |
|---|---|
| 1行目 | 「すべてのUMAが図鑑に記録された！」 |
| 2行目 | 「スカイフィッシュも、ツチノコも、そして究極ゴリラも。」 |
| 3行目 | 「一匹ずつ向き合い、積み重ねてきた記録は、力だけではたどり着けない冒険の証だ。」 |
| 報酬 | 「報酬：3000G ＋ ラーメン×3」 |
| 称号（条件分岐） | クリア＋横スクロール制覇済みなら「究極とUMA図鑑を極めし者」、それ以外なら「UMA図鑑を極めし者」 |

### UMA博士（NPC D）図鑑コンプリート反応

| 条件 | セリフ概要 |
|---|---|
| `isUmaDexComplete() && !gameCleared` | 「見事じゃ → 歌が必要」。capturedCount 分岐より優先、早期 return。 |
| `gameCleared && isFullyCompleted()` | v0.19 完全達成セリフが最高優先（変更なし） |
| `gameCleared && isUmaDexComplete()` | 「すべてのUMAを記録し、究極ゴリラに歌を届けたのじゃな。」改定版 |

### 冒険の記録 UMA図鑑セクション

- コンプリート時：「✅ コンプリート！すべてのUMAが図鑑に記録された。」
- 未コンプ時：「あとN種類 — 未捕獲のUMAを探してみよう。」

### デバッグ §72

| ボタンID | 動作 |
|---|---|
| `btn-debug-open-record-dex` | 冒険の記録モーダルを開いてUMA図鑑セクション確認 |

---

## §73 v0.22 — 図鑑未捕獲UMAヒント強化

**[実装済み]**

### UMA_DATA ヒントフィールド（全9種）

| フィールド | 内容 |
|---|---|
| `hintArea` | 出現エリアの概要（例: "水辺・草むら"、"フィールド全域"、"森の最深部"） |
| `hintText` | 発見ヒント（どのレベル帯で出現するか、特徴など） |
| `hintCatch` | 捕獲ヒント（捕獲率・逃げやすさ・特殊条件など）。究極ゴリラは「HP1〜10＋うたう」特化。 |

### 図鑑モーダル（renderDexBody）ヒント表示

| 状態 | 表示 |
|---|---|
| 未発見 (`!st`) | カード内に `📍hintArea` を小フォントで表示 |
| 発見済み未捕獲 (`st === "seen"`) | カード内に `💡hintText` を小フォントで表示 |
| 捕獲済み | ヒント非表示（変更なし） |

### 図鑑詳細モーダル（openMonsterDetailModal）捕獲ヒント

- 通常UMA（非メタル・非伝説）が発見済み未捕獲の場合: 「💡 捕獲ヒント」セクションに `hintCatch` を表示
- 究極ゴリラは従来の「⚠️ 捕獲について」表示を維持

### UMA博士（NPC D）未捕獲ヒント

- 図鑑未コンプ時に最初の未捕獲UMAを特定し、名前 + hintArea / hintText をセリフに組み込む
- 捕獲数 ≥8 の場合: 「[UMA名]はまだ捕まえていないようじゃ。[hintText]」
- 捕獲数 ≥4 の場合: 「[UMA名]はまだ記録されていない。[hintArea]を探してみるとよいぞ。」

### 攻略ペーパービュー屋 getProgressHint() 強化

- クリア後・横スクロール制覇・図鑑未コンプの priority=0 ブランチを強化
- tier1: 「図鑑にまだ空きがある。[UMA名]はまだ捕まえていないぞ。」
- tier2: 「[UMA名] — [hintText]」
- tier3: 「[UMA名]の捕獲方法：[hintCatch]　図鑑を開いてヒントを確認しよう。」

### 冒険の記録 図鑑セクション

- 未コンプ時に「📖 図鑑でヒントを確認」ボタン（btn-record-open-dex）を追加
- クリック: 記録モーダルを閉じ → 図鑑モーダルを開く

### デバッグ §73

| ボタンID | 動作 |
|---|---|
| `btn-debug-dex-one-uncaptured` | 最初のUMA（カッパ）だけ未捕獲、他を捕獲済みにして図鑑を開く |
| `btn-debug-dex-one-seen` | 最初のUMA（カッパ）だけ「発見済み（未捕獲）」、他を未発見にして図鑑を開く |
| `btn-debug-dex-reset` | 図鑑を全リセット（全UMA未発見状態）にして図鑑を開く |

---

## §74 v0.23 — クリア後フィールドBGM軽量メロディ調整

**[実装済み]**

### 方針

- **BGM制御は一切変更しない**: `stopBGMHard()` / `startBGM()` / `stopBGM()` / `updateBGM()` / `_scheduleBGMLoop()` / `bgmSessionId` / `activeBgmNodes` / `activeBgmTimers` / `bgmMasterGain` / audioContext 初期化はすべて変更なし。
- **変更対象**: `BGM_DATA` のメロディ配列のみ。新エントリ `fieldClear` を追加。

### `getFieldBgmType()` ヘルパー

```javascript
function getFieldBgmType() {
  return (state && state.gameCleared) ? "fieldClear" : "field";
}
```

`updateBGM("field")` の全呼び出し箇所を `updateBGM(getFieldBgmType())` に置換。BGM制御ロジック自体は変更なし。

### `BGM_DATA.fieldClear` メロディ仕様

| 項目 | 値 |
|---|---|
| waveType | "triangle"（squareより柔らかい音色） |
| vol | 0.05（fieldと同じ音量） |
| キー | Cメジャー（fieldと同じ） |
| テンポ | 0.25s/音符（fieldと同じ） |
| 長さ | 約7.75秒/ループ |
| 雰囲気 | 明るく穏やか・達成感・散歩感 |

メロディパターン（ユーザー指定）：
```
C  E  G  A  G  E（上昇→折り返し）
D  F  A  G  E  D（サブドミナント経由）
C  E  G  C5 B  A  G（高音へ展開）
E  G  A  G  E  D  C（解決）
```

### フィールドBGMの使い分け

| 状態 | BGMタイプ |
|---|---|
| `state.gameCleared === false` | `"field"`（既存メロディ） |
| `state.gameCleared === true` | `"fieldClear"`（余韻メロディ） |
| 戦闘中 | `"battle"`（変更なし） |
| エンディング | `"ending"`（変更なし） |
| 横スクロールマップ | `getFieldBgmType()`（通常/クリア後で自動分岐） |

### デバッグ §74

| ボタンID | 動作 |
|---|---|
| `btn-debug-bgm-field` | 通常フィールドBGM（field）を再生 |
| `btn-debug-bgm-field-clear` | クリア後フィールドBGM（fieldClear）を再生 |
| `btn-debug-bgm-hard-stop` | `stopBGMHard()` で完全停止（変更なし） |

### 触ってはいけないもの（保護対象）

- `stopBGMHard()` の実装
- `startBGM()` の実装
- `updateBGM()` の実装
- `_scheduleBGMLoop()` の実装
- `bgmSessionId` / `activeBgmNodes` / `activeBgmTimers` / `bgmMasterGain`
- BGM切り替え条件（戦闘→フィールド、エンディング等）

---

## §78 v0.26 — フィールド仲間追従表示 [実装済み]

パーティ中の仲間をフィールドで主人公の後ろにドラクエ風に並べて歩かせる。通常マップ専用。

**実装仕様**:
- `state.partyTrail: []` — 最大2エントリの `{x, y}` 配列。インデックス0が1歩前（仲間1）、1が2歩前（仲間2）
- `movePlayer(dx, dy)` — 境界チェック通過後、`p.x/y` 更新前に現在位置を `unshift()`。2件超で `pop()`
- `renderField()` — タイルループ前に `trailMap` を構築（`companions[i]` → `COMPANION_DATA` のemoji を `"x,y"` キーで保持）。仲間0優先のため逆順ループ（仲間1→0 の順で trailMap に書き込み、仲間0が上書き）
- タイル描画優先順: プレイヤー → 仲間追従 → アイテム → 地形
- 仲間がプレイヤーと同座標になる場合は表示しない（`tp.x === p.x && tp.y === p.y` ガード）

**リセット関数** (§79 v0.26.1):
- `resetPartyTrail()` — `state.partyTrail = []` を一本化した共通関数。ES5関数宣言でホイスト済み。

**リセット条件**:
- `switchToSideMap()` — サイドマップ入場時
- `switchToNormalMap()` — 通常マップ帰還時
- `recruitCompanion()` / `dismissCompanion()` — 仲間加入・離脱時（§79 v0.26.1）
- `loadGame()` — セーブロード時

**保護対象（変更なし）**: 仲間能力・加入/離脱処理・パーティ上限・戦闘・BGM・横スクロールマップ描画

## §80 v0.27 — 仲間の戦闘自動参加 [実装済み]

主人公の攻撃行動後に仲間が自動で1回ずつ行動する。仲間コマンド選択はなし。

**トリガーコマンド**（仲間行動が発生するもの）:
- `doFight()` — たたかう
- `castSpell()` — まほう（攻撃呪文ブランチのみ）
- `useWaza()` — わざ（固定ダメージブランチのみ）

**除外コマンド**（仲間行動なし）:
- にげる / アイテム / うたう / つかまえる / 回復まほう / ガマンわざ

**フロー**:
1. 主人公行動 → 敵HP確認 → 生存なら `scheduleAfterPlayerAttack()`
2. 600ms後 → `runCompanionAutoActions()`（各仲間を順番に処理）
3. 仲間全員行動後 → 敵HP≤0なら `winBattle()` → そうでなければ 400ms後 → `enemyTurn()`

**究極ゴリラ保護**: `e.final === true` の場合、`runCompanionAutoActions()` は即リターン（仲間は見守るだけ）。

**仲間ごとの行動**:
- ジュリタニ 💪: `Math.min(20, 5 + floor(lv/8))` 物理ダメージ、25%で会心x1.5
- シュリタニ 🪤: `Math.min(5, 1 + floor(lv/20))` 微ダメージ + 捕獲フレーバーメッセージ
- ノリオ 📈: `Math.min(15, 4 + floor(lv/10))` ダメージ + EXPフレーバーメッセージ
- ハルミ ✨: `Math.min(18, 5 + floor(lv/9))` 魔法ダメージ

**保護対象**: `finishBattle()` / `gainExp()` / `winBattle()` / `enemyTurn()` / BGM制御 は変更なし。

## 将来の実装候補（v0.28〜）— プレイヤーフィードバックより [未実装・将来機能]

### v0.28 候補 — 仲間ごとの戦闘コマンド選択 [未実装・将来機能]

主人公 → 仲間1 → 仲間2 → 敵 の行動順で仲間それぞれにコマンド選択できるようにする。

**概要**:
- 戦闘UIの大改修（コマンドパネルを仲間分繰り返す）
- 仲間のHP/MP管理・状態異常・死亡処理も追加が必要

**前提条件**: v0.27 が安定してから着手。GAME_DESIGN.md に詳細仕様必須。

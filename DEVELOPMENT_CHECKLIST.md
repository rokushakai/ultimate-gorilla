# DEVELOPMENT_CHECKLIST.md — 開発作業チェックリスト

毎回の実装作業で確認する。大きな機能追加の前後に必ず使うこと。

---

## 実装前チェック

- [ ] HANDOFF.md を読んで現在の状態を把握した
- [ ] GAME_DESIGN.md の関連セクションを確認した
- [ ] TODO.md の実装予定に今回の作業を登録した（または確認した）
- [ ] 今回の変更が既存機能に影響しないか考えた
- [ ] 大型改修（横スクロールマップ等）ではなく、小さい変更から始める

---

## 実装中チェック

- [ ] ES5 で書いている（`var`、アロー関数なし、テンプレートリテラルなし）
- [ ] 新しいモーダルは `.modal` / `.modal-box` クラスを使っている
- [ ] 新しいボタンは `.modal-btn` または `.shop-menu-btn` を使っている
- [ ] 新しいフラグを `saveGame()` / `loadGame()` / `newGame()` 全部に追加した
- [ ] `loadGame()` の新フラグ読み込みは `!!data.flag` で古いセーブ対応済み
- [ ] デバッグボタンは `if (DEBUG_MODE)` ブロック内か `?debug=1` 条件下に書いた

---

## 実装後チェック（機能確認）

### ゲーム動作
- [ ] index.html を直接ダブルクリックして起動できる（file:// 対応）
- [ ] コンソールエラーが出ていない
- [ ] 通常プレイの移動・戦闘・捕獲・商人・酒場・装備・宝箱・実家が動く
- [ ] レベルアップが正常に機能する
- [ ] セーブ・ロードが正常に動く（リロード後に状態が復元される）
- [ ] エンディング（クリア済み状態）が壊れていない

### 今回追加した機能
- [ ] 追加した機能が意図通り動く
- [ ] 初回のみ表示するものは2回目以降に表示されない
- [ ] ニューゲームで新規フラグがリセットされる
- [ ] 古いセーブデータ（新フラグなし）で落ちない

### デバッグ確認
- [ ] **通常URL** でデバッグメニューが表示されない
- [ ] **`?debug=1` URL** でデバッグメニューが表示される
- [ ] デバッグボタンで追加した機能をテストできる

### スマホUI確認
- [ ] スマホ縦画面（幅480px前後）で文字が読める・ボタンが押せる
- [ ] 長押しでコピー/翻訳メニューが出にくい（既存の抑制が壊れていない）
- [ ] モーダルが画面からはみ出していない
- [ ] 十字キーが正常に機能する

### GitHub Pages 確認
- [ ] 相対パス（`./style.css`、`./script.js`）が壊れていない
- [ ] `<script src="script.js">` のパス指定が変わっていない

---

## ドキュメント更新チェック

- [ ] GAME_DESIGN.md を更新した（新機能の仕様を追記）
- [ ] TODO.md の実装済みリストに追記した
- [ ] CHANGELOG.md に `[バージョン]` エントリを追加した
- [ ] README.md の「今後追加予定」や「デバッグモード」が最新状態か確認した
- [ ] 必要なら README.txt も更新した

---

## commit/push 前チェック

- [ ] `git status` で意図しないファイルが含まれていない
- [ ] コミットメッセージが `feat:` / `fix:` / `docs:` + 機能名 + バージョン の形式
- [ ] `git push origin main` を実行した
- [ ] GitHub Pages にデプロイされるまで数分待って確認した（任意）

---

## よくあるミス

| ミス | 対策 |
|---|---|
| `let` / `const` を使ってしまった | `var` に直す（ES5必須） |
| アロー関数 `=>` を使った | `function()` に直す |
| テンプレートリテラル `` ` `` を使った | 文字列連結 `+` に直す |
| 新フラグを `newGame()` でリセットし忘れた | `state.player` の初期化部分を確認 |
| デバッグボタンを通常UIに出してしまった | `DEBUG_MODE` 条件で囲む |
| `loadGame()` の新フラグで古いセーブが落ちた | `!!data.flag` / `data.val \|\| defaultVal` 形式に直す |
| `openModal()` / `closeModal()` を使わず直接 `hidden` を操作した | 既存の関数を使う |
